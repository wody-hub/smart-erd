# RIS-301 QA 결과

## 테스트 환경

- Date: 2026-04-28
- Workspace: `/Users/j.jaeyo/Project/ETC/smart-erd`
- Backend test runner: Gradle (`./gradlew test`)
- Frontend unit runner: Node test pipeline (`client/scripts/run-unit-tests.mjs`)
- Manual preview: `http://127.0.0.1:58592`

## 데이터 셋

- 자동화: 기존 테스트 픽스처/목 데이터 사용
- 수동(초기 복구 확인): 프로젝트 `577` (empty-state 중심)
- 수동(positive-path): 프로젝트 `RIS295-QA-224752` (`projectId=585`)
  - milestone 2건: `요구사항 승인(APPROVAL)`, `1차 릴리즈(RELEASE)`
  - WBS 6건: `기획`, `설계`, `개발`, `QA`, `API 구현`, `UI 구현`
  - dependency 5건(FS):
    - `기획 -> 설계`
    - `설계 -> API 구현`
    - `설계 -> UI 구현`
    - `API 구현 -> QA`
    - `UI 구현 -> QA`

## 절차

1. 백엔드 도메인/서비스 검증
   - `./gradlew test --tests "com.smarterd.domain.pm.wbs.service.WbsDependencyServiceTest" --tests "com.smarterd.domain.pm.wbs.service.WbsServiceTest" --tests "com.smarterd.domain.pm.milestone.service.MilestoneServiceTest"`
2. 백엔드 API 레이어 검증
   - `./gradlew test --tests "com.smarterd.api.project.WbsControllerMvcTest" --tests "com.smarterd.api.project.MilestoneControllerMvcTest"`
3. 프론트 유닛 회귀 검증
   - `cd client && npm run -s test:unit`
4. 수동 프리뷰 검증 A (empty-state, project 577)
   - `/wbs`, `/milestones`, `/wbs/dependencies` 응답 `200`
   - WBS/dependency panel/Gantt/milestone·rolling-wave empty/filter surface 정상
5. 수동 프리뷰 검증 B (seeded positive-path, project 585)
   - WBS 탭: 트리 6건, milestone 라벨, dependency panel(선행/후속/blocking chain/다음 milestone), milestone panel(게이트 2건/차단 의존성 수/readiness note) 정상
   - 필터: `승인만` 필터 정상, `지연만` 필터 empty-state 정상
   - Gantt 탭: `현재 wave/다음 마일스톤/후속 후보` 요약 정상, WBS 6/dependency 5/milestone 2 통계 정상, milestone row+WBS row 동시 렌더 정상
   - 네트워크: `/wbs` `200`, `/milestones` `200`, `/wbs/dependencies` `200`

## 기대 결과 / 실제 결과

- 기대: dependency/WBS/milestone 통합 변경에 대해 도메인/API/회귀/시각 검증이 모두 정상 동작한다.
- 실제:
  - Gradle 대상 테스트 2회 모두 `BUILD SUCCESSFUL`
  - Frontend unit test `pass 336 / fail 0`
  - preview 500 blocker 재현 없음
  - empty-state + seeded positive-path 수동 검증 모두 통과

## 실패 케이스

- 자동화/수동 실행 범위에서 실패 케이스 없음

## 회귀 여부

- 자동화 범위 내 회귀 징후 없음
- 수동 UI 범위(Empty-state + Positive-path)에서도 회귀 징후 없음

## 최종 판정

- phase 9 범위의 WBS / milestone / dependency / gantt 탭 동작은 검증 범위 내 정상
- RIS-301 완료 기준(체크리스트 기반 결과 기록 + 실패/회귀 분리 기록) 충족
