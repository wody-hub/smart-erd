# TODO WBS 계층형 선택 UX 개선 실행 분할

## 대상 이슈

- parent issue: [RIS-295](/RIS/issues/RIS-295)

## 범위 재확인

- 개인 TODO create/detail의 `연결 WBS` 선택 UX 개선
- 검색 가능 + 계층 인지 가능한 picker 도입
- 기존 TODO-WBS link API/상태 흐름 재사용

비포함:

- WBS API 변경
- TODO 목록 자체 검색
- WBS 화면 구조 변경

## 권장 child issue 분할

### Child A. 프런트엔드 구현

제목 초안:

- `TODO WBS 계층형 검색 선택기 구현`

범위:

- `WbsHierarchySelect` 공용 컴포넌트 추가
- create/detail 패널의 flat select 교체
- path-aware option builder 및 검색 로직 추가
- i18n 키 추가

완료 기준:

- TODO에서 WBS를 검색으로 찾을 수 있다.
- 같은 이름의 WBS도 계층 경로로 구분 가능하다.

### Child B. QA

제목 초안:

- `TODO WBS 계층형 검색 선택기 QA`

범위:

- 검색 시나리오 검증
- 중복 이름 branch 구분 검증
- create/detail 저장 연동 검증
- 기존 TODO 기능 회귀 검증

완료 기준:

- 계층 가독성과 검색성이 실제 프로젝트 데이터에서 확인된다.
- 기존 TODO 생성/수정/WBS 링크 흐름이 깨지지 않는다.

## 선행 순서

1. Child A 프런트엔드 구현
2. Child B QA

## PM 코멘트에 반드시 남길 내용

- 현재 flat select가 문제인 근거 파일
- 새 picker가 적용될 화면 두 군데
- search scope가 `이름 + 조상 경로`라는 결정
- QA가 확인해야 할 중복 이름 branch 시나리오
