# RIS-305 QA 결과

## 실행 일시

- 2026-04-28 ~ 2026-04-29

## 실행 범위

- 체크리스트 기준 자동 검증 우선 수행
- 핵심 포인트:
  - create/detail 공통 picker 적용
  - 중복 이름 WBS branch 구분
  - TODO-WBS link/unlink 경로 회귀

## 수행 결과

### 1) 프론트엔드 단위 테스트

- 명령: `cd client && npm run test:unit`
- 결과: pass 337 / fail 0
- 확인 포인트:
  - `buildWbsHierarchyOptions exposes full paths for duplicate-name branches` 통과

### 2) 백엔드 TODO 관련 테스트

- 명령: `./gradlew test --tests "*ProjectTodo*" --tests "*Todo*"`
- 결과: BUILD SUCCESSFUL
- 확인 포인트:
  - TODO 생성/조회/수정 및 WBS link/unlink API 경로 회귀 테스트 포함 스위트 통과

### 3) 구현 교차 확인

- `ProjectTodoCreateDialog`와 `ProjectTodoDetailPanel` 모두 `WbsHierarchySelect`를 사용
- create/detail 양쪽에서 동일 picker 컴포넌트 경로로 동작함

### 4) 수동 QA 시나리오 (Playwright 기반 재현)

- 명령: `cd client && SMART_ERD_INCLUDE_TMP_E2E=1 npx playwright test e2e/tmp/ris-305-wbs-picker.spec.ts`
- 결과: 1 passed
- 준비 데이터:
  - 테스트 실행 중 전용 사용자/팀/프로젝트/다이어그램 자동 생성
  - WBS 3단계 구조 + 중복 branch 이름 데이터 자동 생성
    - `플랫폼 / 백엔드 / API`
    - `운영 / 백엔드 / API`
- 확인 포인트:
  - create dialog의 Linked WBS picker에서 중복 branch 경로가 구분되어 노출됨
  - 검색어 `API` 및 경로 검색(`플랫폼 / 백엔드`)으로 대상 항목 식별 가능
  - create에서 선택한 WBS가 detail의 동일 picker에 full path로 반영됨
  - detail에서 다른 branch WBS로 재연결(link) 및 `Not linked`(unlink) 전환 동작 확인
  - unlink 후 private hint, link 후 shared hint 노출 전환 확인

## 결론

- 자동/수동 QA 모두 통과
- RIS-305 검증 범위(검색, 중복 이름 branch 구분, create/detail 저장 연동, link/unlink 회귀) 기준으로 done 처리 가능
