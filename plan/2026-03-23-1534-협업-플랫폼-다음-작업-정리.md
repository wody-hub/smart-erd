# 협업 플랫폼 다음 작업 정리

작성 시각: 2026-03-23 15:34 KST
기준 브랜치: `feature/refact`
기준 커밋: `4b998c6` (`refactor: extract collaboration platform core`)

## 1. 현재까지 완료된 범위

### Phase 0
- 기준선 검증 완료
- 핵심 기준:
  - backend compile
  - frontend build
  - frontend unit test
  - 3계정 협업 smoke

### Phase 1
- 협업 플랫폼 공통 core 도입
  - `src/main/java/com/smarterd/collaboration/*`
  - `client/src/collaboration/*`
- 공통 세션 메타데이터 도입
  - `CollaborationAuthenticatedSession`
  - `AuthenticatedSession` 브리지
- diagram 채널 plugin 초안 도입
  - `DiagramCollaborationChannelPlugin`
  - `DiagramCollaborationSnapshotStore`
  - `DiagramCollaborationHandoffPolicy`
  - `DiagramCollaborationSessionMetadataPolicy`

### Phase 2
- application 유스케이스 도입
  - `LoadCollaborationHandoffUseCase`
  - `PersistCollaborationSnapshotUseCase`
  - `PersistDiagramSnapshotUseCase`
  - `SaveDiagramAuthoritativeContentUseCase`
  - `SaveDiagramUseCase`
- controller/save 경계 재정리
- `persisted=false` 응답 의미 보존
- keepalive 저장 경로 보수화

### 안정화 패치
- `content-only + no snapshot` 경로에서 첫 연결 전 snapshot seed 수행
- `PreviewCanvas` drag 종료 위치 commit 안정화
- 3계정 협업 smoke를 테스트 전용 seeding 없이 실제 제품 경로로 다시 green 확보

## 2. 현재 확인된 상태

### 통과한 검증
- `./gradlew test --tests ...DiagramCollaborationPluginTest ...PersistDiagramSnapshotUseCaseTest ...AuthenticatedSessionTest ...DiagramWebSocketFlowIntegrationTest`
- `client/npm run build`
- `client/npm run test:unit`
- `client/npx playwright test e2e/smoke/diagram-work-mode-three-account-collaboration.spec.ts --workers=1 --retries=0 --reporter=line`
  - 연속 2회 통과

### 현재 판단
- blocking 이슈 없음
- 지금부터는 “core 추출” 다음 단계인 “진짜 채널 플러그인 구조 확장”과 “운영 회귀 범위 확대 검증”으로 넘어가면 됨
- `smart-erd` 기준 overwrite hotfix는 코드 반영 완료
  - 늦게 도착한 hydration/bootstrap overwrite 차단
  - `userStartedLocalEditRef` 기반 pristine-only hydration
  - IME/입력 시작 보호
- `smart-erd-view` 반영은 최종 작업 완료 후 머지로 처리

## 3. 우선순위 재조정

### 완료. 코드 위주 모드 hydration/bootstrap 덮어쓰기 이슈 (`smart-erd`)
배경:
- 사용자 제보 기준으로 `코드 위주` 모드에서 DSL/코드 입력 중 늦게 도착한 draft/bootstrap hydration이 editor text를 통째로 덮어쓰고 있음
- 그 결과 커서가 아래로 튀고, 이어지는 입력이 엉뚱한 위치에 들어가며 오류가 발생함
- 이 문제는 현재 진행 중인 협업 플랫폼 구조 작업보다 우선 처리해야 하는 사용자 체감 장애임

현재 확인:
- 작업 모드 capability 자체는 `code` 모드에서 `enableCodeToErdAutoSync=false`, `enableErdToCodeAutoSync=false`로 맞아 있음
- 하지만 아래 hydration 경로가 auto sync와 별개로 `syncCodeChange(...)`를 직접 호출함
  - `sharedSchemaDraftBootstrap`
  - `storedDraft`
  - `generated fallback`
- 두 저장소 모두 같은 구조를 가지고 있음
  - `/Users/j.jaeyo/Project/ETC/smart-erd`
  - `/Users/j.jaeyo/Project/ETC/smart-erd-view`

핵심 파일:
- `client/src/components/erd/DslCodeEditorPanel.tsx`
- `client/src/components/erd/DdlCodeEditorPanel.tsx`
- `client/src/hooks/useBidirectionalCodeSync.ts`
- `client/src/hooks/useEditorCursorGuard.ts`
- `client/src/pages/diagram/DiagramPage.tsx`
- `client/src/lib/diagram-work-mode.ts`

완료된 일:
1. `smart-erd`를 기준 저장소로 보고 재현/수정 진행
2. hydration/bootstrap 적용 전에 `user started local edit` 가드 추가
3. `delayDraftHydration` 이후 늦게 도착한 bootstrap은 pristine 상태에서만 허용
4. IME/입력 시작 시점 보호 추가
5. 동일 수정 사항의 `smart-erd-view` 반영은 마지막 머지 단계로 이연

현재 관계:
- overwrite 이슈 수정 후 협업 플랫폼 Phase 3/4/5 작업을 재개함
- 현재 워크트리의 주 변경은 아래 두 축임
  - backend: channel/runtime/ticket/handshake 일반화
  - frontend: `useYjsCollaboration` 책임 분리와 runtime lifecycle 추출

## 4. 다음 우선순위

### 우선순위 1. Phase 3 backend 일반화 마무리
목표:
- diagram 채널 plugin 구조를 실제 확장 가능한 형태로 한 단계 더 일반화

해야 할 일:
- `application/diagram/*`에 남아 있는 diagram 전용 channel 하드코딩 추가 축소
- `CollaborationChannelPlugin`가 직접 `snapshot/handoff/access`만 노출하는 현재 구조에서
  - 향후 `ticket`, `bootstrap`, `presence`, `preview policy` 확장을 고려한 계약 점검
- `DiagramCollaborationResourceKeyFactory` 기준으로 diagram channel key 생성 경로 통일
- `DiagramWebSocketHandler`에서 남은 diagram-specific message/context/lifecycle 배선 추가 축소

주의:
- 지금 단계에서는 WebSocket 메시지 타입, `/ws/diagram/*` 경로, Yjs binary 포맷은 바꾸지 않음

### 우선순위 2. Phase 4 backend runtime 연결 마무리
목표:
- backend 진입점 연결을 더 플랫폼 구조에 가깝게 정리

해야 할 일:
- `WsTicketHandshakeInterceptor`
- `DiagramWebSocketHandler`
- `SnapshotRequestMessageHandler`
- room/presence/flush lifecycle helper
위 경로에서 남아 있는 diagram 전용 배선/해석 로직을 다시 점검

특히 확인할 것:
- legacy `AuthenticatedSession` 제거 가능 시점
- common session key만으로 운영 가능한지
- room lifecycle, peer-left, flush, reconnect가 계속 보존되는지

### 우선순위 3. Phase 5 frontend runtime/state 정리
목표:
- 프론트 협업 runtime/state machine 정리

해야 할 일:
- `useYjsCollaboration`를 더 얇은 wrapper로 축소
- provider lifecycle / preview hydration / transport / seed policy 분리 마무리
- `useCollaborationSession`/runtime state machine 활용 범위 확대
- 상태를 아래 기준으로 명확화
  - `preview`
  - `hydrating`
  - `live`
  - `degraded`
  - `reconnecting`

주의:
- `useBidirectionalCodeSync`
- `useEditorCursorGuard`
- footer/status UI
는 editor-local UX이므로 계속 collaboration core 밖에 둔다

## 5. 현재 작업 순서

1. backend channel/runtime/ticket 일반화 계속
2. `DiagramWebSocketHandler` / dispatcher / lifecycle 배선 정리
3. `useYjsCollaboration`를 provider/runtime orchestration 위주로 더 축소
4. `smart-erd` 기준 검증 완료
5. overwrite hotfix와 협업 플랫폼 리팩터링을 정리해 커밋 단위 확정
6. 마지막에 `smart-erd-view` 반영

## 6. 다음 회귀 검증 우선순위

다음 구현부터는 아래를 반드시 같이 확인한다.

### 1순위
- 3계정 협업 smoke
- code-first / sync / erd-only 간 위치 전파
- apply 후 persisted 승격

### 2순위
- restart recovery
- preview lock
- save/reload/reconnect

### 3순위
- content-only 진입 경로
- keepalive 이후 visible 복귀 재시도
- persisted=false 처리 UI 상태

## 7. 다음 작업 시작 순서

1. `smart-erd`에서 overwrite 재현 경로와 실제 덮어쓰기 지점 고정
2. hydration/bootstrap overwrite 차단 패치
3. `useEditorCursorGuard` / `useBidirectionalCodeSync` 보강
4. 작업 완료 후 동일 수정 사항을 `smart-erd-view`에 이식
5. overwrite 이슈 검증 종료
6. 그 다음 `Phase 3` backend 일반화 재개
7. frontend runtime/state 정리
8. smoke + recovery 재검증

## 8. 커밋 전략

다음부터는 아래 단위로 나눠 커밋한다.

### 커밋 A
- overwrite hotfix + backend channel/runtime 일반화 (`smart-erd`)

### 커밋 B
- frontend runtime/state 정리 (`smart-erd`)

### 커밋 C
- smoke/recovery 테스트 보강 및 flaky 감소

### 커밋 D
- 최종 `smart-erd-view` 이식/머지

## 9. 메모

- 현재 설계/구현 문서는 유지:
  - `2026-03-20-1542-협업-모듈화-리팩터링-설계-계획.md`
  - `2026-03-20-1613-협업-모듈화-리팩터링-구현-계획.md`
- Phase 0 리포트 유지:
  - `2026-03-23-1145-협업-플랫폼-Phase0-기준선-검증-리포트.md`
- 지금 문서는 “바로 다음 작업을 시작하기 위한 실행 메모” 역할이다.
