# RIS-323 1차 자동화 QA 결과

- 작성일: 2026-05-06
- 이슈: RIS-323
- 범위: WBS 2차 기능의 코드 기반 자동화 검증(단위/컨트롤러/TMP E2E)

## 실행 결과 요약

- 결과: `PARTIAL PASS` (자동화 단위 검증 통과, E2E 환경 미충족)

## 1) 백엔드 검증

실행:

```bash
./gradlew test \
  --tests 'com.smarterd.domain.pm.wbs.service.WbsPlanningServiceTest' \
  --tests 'com.smarterd.domain.pm.wbs.service.WbsDependencyServiceTest' \
  --tests 'com.smarterd.api.project.WbsControllerMvcTest'
```

결과:

- `BUILD SUCCESSFUL`
- WBS 2차 핵심 범위 커버 확인:
  - subtree duplicate (`duplicateSubtree_resetsOptionalFields`)
  - template 저장/적용 round-trip (`saveTemplateAndInstantiate_roundTrip`)
  - bulk create 유효성(순환 parent 차단)
  - dependency shift preview/apply
  - API contract (`/wbs/templates`, `/wbs/bulk-create`, `/wbs/dependencies/shift:*`)

## 2) 프론트엔드 단위 검증

실행:

```bash
cd client && npm run test:unit
```

결과:

- `pass 347, fail 0`
- WBS 2차 관련 커버 확인:
  - outline paste 파싱(`parseBulkCreateOutline`)
  - dependency shift preview 계산(`buildDependencyShiftPreview`)
  - 트리 이동/깊이 제한/inline create payload 회귀(`wbs-tree-utils`)

## 3) TMP E2E 검증 시도

실행:

```bash
cd client && SMART_ERD_INCLUDE_TMP_E2E=1 npx playwright test e2e/tmp/ris-318-wbs-structure-authoring.spec.ts --workers=1
```

결과:

- 실패: `TypeError: fetch failed`
- 실패 지점: `provisionCollaborationFixture` 단계(API endpoint 접근 실패)
- 해석: 로컬 E2E 서버/API/계정 프리셋이 준비되지 않아 시나리오 실행 불가

## 4) 제공된 QA 환경으로 TMP E2E 재실행

입력 환경:

- 프론트: `http://127.0.0.1:4503`
- 백엔드: `http://localhost:9503`

실행:

```bash
cd client && \
SMART_ERD_INCLUDE_TMP_E2E=1 \
SMART_ERD_E2E_BASE_URL=http://127.0.0.1:4503 \
SMART_ERD_E2E_API_URL=http://localhost:9503/api \
npx playwright test \
  e2e/tmp/ris-318-wbs-structure-authoring.spec.ts \
  e2e/tmp/ris-305-wbs-picker.spec.ts \
  --workers=1
```

결과:

- `ris-305` 통과
- `ris-318` 실패(180s timeout)
  - 실패 단계: `rowByName('기획').getByRole('button', { name: /0%/ }).click()`
  - 아티팩트 기준 관찰: Structure preset 테이블에는 `Item/Milestone/Actions`만 노출되고, progress는 우측 detail panel에 표시됨
  - 판단: 제품 결함보다 TMP E2E 시나리오의 구가정(progress inline column 기본 노출) 불일치 가능성이 높음

후속 조치:

- child issue 생성: `RIS-325`
  - 목적: `ris-318` TMP E2E를 현재 view preset/column visibility 정책에 맞게 갱신

## 미완료 검증 항목 (환경 필요)

- dedicated WBS page에서 실제 UI 플로우 수동/시나리오 검증
- view preset / column visibility 실사용 동작 확인
- 1차 기능 회귀를 브라우저 상호작용 기준으로 최종 확인

## 다음 액션

- 실행 가능한 QA 환경(접속 URL, QA 계정, 대상 team/project) 확보 즉시
  - TMP E2E 2종(`ris-318`, `ris-305`) 재실행
  - 미완료 항목 수동 점검
  - 최종 PASS/FAIL 및 재현 경로를 RIS-323 코멘트에 확정 보고
