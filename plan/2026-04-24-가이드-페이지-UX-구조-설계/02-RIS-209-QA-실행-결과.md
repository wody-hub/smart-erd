# RIS-209 가이드 페이지 QA 실행 결과

> 실행일: 2026-04-24
> 대상 이슈: [RIS-209](/RIS/issues/RIS-209)
> 기준 체크리스트: [RIS-208](/RIS/issues/RIS-208)
> 구현 이슈: [RIS-207](/RIS/issues/RIS-207)

## 1) 실행 환경

- Frontend: `npm --prefix client run dev -- --host 127.0.0.1 --port 4502`
- Backend(test): `./bootRun-test.sh` (API `http://127.0.0.1:9502`)
- Browser QA: Playwright MCP (manual scenario replay)
- 실행 계정: 신규 가입 계정 1개 생성 후 팀/프로젝트/문서 허브 진입 검증

## 2) 자동 검증 결과

- `npm --prefix client run build`: **FAIL**
- `npm --prefix client run test:unit`: **PASS** (326/326)
- `npm --prefix client run test:e2e -- e2e/smoke/diagram-quick-term-domain-scroll.spec.ts --workers=1 --retries=0`: **FAIL** (timeout)

### 2.1 빌드 실패 상세

- 에러: `src/pages/guide/GuidePage.tsx(374,26)` TS2345
- 요약: `t(\`guide.hero.${key}\`)` 동적 key 타입 불일치로 빌드 차단
- 후속 이슈: [RIS-210](/RIS/issues/RIS-210)

### 2.2 DSL 회귀 스모크 실패 상세

- 실패 단계: `openCodeEditor` (`코드/Code` 버튼 locator 대기)
- 에러: `Test timeout of 180000ms exceeded`
- 아티팩트:
- `client/test-results/smoke-diagram-quick-term-d-d753a-the-diagram-code-flow-smoke/test-failed-1.png`
- `client/test-results/smoke-diagram-quick-term-d-d753a-the-diagram-code-flow-smoke/trace.zip`
- 후속 이슈: [RIS-212](/RIS/issues/RIS-212)

## 3) 체크리스트 결과 (RIS-208 매핑)

| ID | 결과 | 근거 |
|---|---|---|
| NAV-01 | FAIL | 전역 헤더 우측 유틸 영역에 상시 `가이드` 버튼 없음. 팀/프로젝트/문서 허브의 hero utility 영역에서만 노출됨. |
| NAV-02 | FAIL | Empty/허브에서 `가이드 열기` 동작은 있으나 모두 `/guide`로만 이동. 맥락 섹션(anchor/state) 전달 없음. |
| NAV-03 | N/A (요구사항 충돌) | 체크리스트는 보호 라우트 기대지만, 현 요구사항은 공개 라우트 접근성 검증. 실제 `/guide`는 비인증 접근 가능. |
| NAV-04 | FAIL | `/guide#erd-dsl` 진입 시 대상 anchor(`id=erd-dsl`) 부재. 자동 섹션 이동 불가. |
| NAV-05 | PASS | `teams/projects/diagrams -> guide -> browser back` 시 이전 화면 복귀 확인. |
| UI-01 | PASS | Paper/Graphite/Midnight 모두 테마 클래스/색상 적용 및 가독성 확인. |
| UI-02 | PASS | 390/768/1366 뷰포트에서 수평 오버플로우 없음. |
| UI-03 | PASS | H1/H2 계층 및 상단 CTA 노출 확인. |
| UI-04 | PASS | 상단 섹션 인덱스(제품 소개/빠른 시작/작업 공간 지도/...) 버튼 존재. |
| FUNC-01 | FAIL | 온보딩 체크리스트(완료/닫기 상태 유지 포함) 기능 미구현. |
| FUNC-02 | FAIL | 진입 컨텍스트 표시 + 전용 `돌아가기` CTA 미구현. |
| FUNC-03 | FAIL | 가이드 카드 내부 작업 시작 링크/버튼 부재 (정적 카드). |
| FUNC-04 | FAIL | 가이드 페이지 내부에서 DSL 상세 가이드로 연결되는 명시 링크 미구현. |
| PERF-01 | PASS (로컬 기준) | Navigation Timing 5회 측정 `domContentLoaded 24~45ms`, `load 29~52ms`. |
| ACC-01 | PASS (기본 확인) | 섹션 heading 구조 확인, Tab 이동으로 상단 CTA/인덱스 포커스 순회 확인. |

## 4) 결론

- 현재 상태는 "가이드 페이지 노출/기본 탐색"은 동작하나, 체크리스트의 핵심 허브 기능(맥락 앵커/복귀/작업 전환) 기준으로는 **FAIL**.
- 수정 필요 항목을 하위 이슈로 분리:
- [RIS-210](/RIS/issues/RIS-210): FE 빌드 blocker
- [RIS-211](/RIS/issues/RIS-211): 체크리스트 핵심 기능 미충족 묶음
- [RIS-212](/RIS/issues/RIS-212): DSL 회귀 스모크 타임아웃

## 5) 재검증 제안

- [RIS-210](/RIS/issues/RIS-210), [RIS-211](/RIS/issues/RIS-211), [RIS-212](/RIS/issues/RIS-212) 해결 후 `RIS-209`에서 동일 시나리오 재실행
- 특히 `NAV-02/NAV-04/FUNC-02/FUNC-03/FUNC-04`를 우선 재확인

## 6) 2차 재검증 (RIS-210/211/212 완료 후)

> 실행일: 2026-04-24 (same day rerun)
> 트리거: `issue_children_completed` wake

### 6.1 자동 검증 재실행

- `npm --prefix client run build`: **PASS**
- `npm --prefix client run test:unit`: **PASS** (326/326)
- `npm --prefix client run test:e2e -- e2e/smoke/diagram-quick-term-domain-scroll.spec.ts --workers=1 --retries=0`: **PASS** (7.1s, backend/frontend 기동 후 재실행)

### 6.2 가이드 회귀 재검증(체크리스트 우선 항목)

- 재현 방식: Playwright 임시 스펙으로 `/guide` 진입/복귀/딥링크/카드 액션/반응형/테마를 순차 검증 후 임시 스펙 삭제
- 확인된 PASS:
- 공개 `/guide` 접근 시 로그인 리다이렉트 없이 진입 가능
- Teams/Projects/Document hub 경로의 가이드 진입 시 `source/teamId/projectId/hash` 맥락 파라미터 전달
- 가이드 컨텍스트 배너의 복귀 CTA 동작(원 진입 경로 복귀)
- 가이드 카드 액션에서 실제 작업 라우트(`.../diagrams`) 연결
- 핵심 섹션 ID 존재(`guide-*`, `erd-dsl`)
- 모바일/태블릿/데스크톱(390/768/1366) 가로 오버플로우 없음
- `paper/graphite/midnight` 테마 클래스 적용 정상

### 6.3 신규 결함 (딥링크 상단 정렬)

- FAIL 항목: `NAV-04` deep-link 앵커 "상단 정렬" 기준
- 재현 URL: `http://127.0.0.1:4502/guide#erd-dsl`
- 관측값: `document.getElementById('erd-dsl').getBoundingClientRect().top = 406.734375`
- 해석: 섹션 존재/가시 상태는 맞지만, 체크리스트의 "해당 섹션이 화면 상단에 위치" 기준은 미충족
- 후속 이슈: [RIS-213](/RIS/issues/RIS-213)

## 7) 최종 상태 (2차 재검증 기준)

- 이전 blocker였던 build/DSL 스모크는 해소됨([RIS-210](/RIS/issues/RIS-210), [RIS-212](/RIS/issues/RIS-212) 결과 반영).
- 가이드 핵심 회귀 항목 대부분은 통과했으나, `#erd-dsl` 딥링크 상단 랜딩 실패로 최종 판정은 **FAIL(잔여 1건)**.
- `RIS-209`는 [RIS-213](/RIS/issues/RIS-213) 수정 완료 후 해당 항목 재검증이 필요함.

## 8) 3차 재검증 (RIS-213 완료 후)

> 실행일: 2026-04-24 (final rerun)
> 트리거: `issue_children_completed` wake (RIS-213 done)

- `npm --prefix client run build`: **PASS**
- `npm --prefix client run test:unit`: **PASS** (326/326)
- `npm --prefix client run test:e2e -- e2e/smoke/guide-deep-link-landing.spec.ts --workers=1 --retries=0`: **PASS**
- `npm --prefix client run test:e2e -- e2e/smoke/diagram-quick-term-domain-scroll.spec.ts --workers=1 --retries=0`: **PASS**

핵심 확인:
- `#erd-dsl` deep-link 랜딩 스모크 통과(섹션이 sticky header 바로 아래로 정렬되는 조건 검증)
- 기존 DSL 오류 가이드 회귀 스모크도 계속 통과

## 9) 최종 상태 (3차 재검증 기준)

- [RIS-210](/RIS/issues/RIS-210), [RIS-211](/RIS/issues/RIS-211), [RIS-212](/RIS/issues/RIS-212), [RIS-213](/RIS/issues/RIS-213) 반영본 기준으로 재검증 완료
- `RIS-209` 범위의 재현 가능한 blocker/회귀 이슈 없음
- 최종 판정: **PASS**
