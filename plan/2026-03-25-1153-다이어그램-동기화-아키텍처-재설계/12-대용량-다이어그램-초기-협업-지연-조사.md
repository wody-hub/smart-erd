# 12. 대용량 다이어그램 초기 협업 지연 조사

## 목적

이 문서는 2026-03-27 기준 local 환경에서 확인한
`GH도급사업 > GH 도급` 다이어그램의 초기 협업 지연 현상을
다음 작업으로 정리한다.

목표는 아래 두 가지다.

- 현상을 재현 가능한 형태로 기록한다.
- 원인과 조치 방향을 코드 수정 전에 명확히 고정한다.

## 대상 환경

- frontend: `http://localhost:4501`
- backend: `http://localhost:9501`
- 대상 프로젝트: `GH도급사업`
- 대상 다이어그램: `GH 도급`
- 측정 일시: `2026-03-27`

## 현상

대상 다이어그램에 처음 진입하면 화면 자체는 비교적 빨리 뜬다.  
하지만 협업 ready 상태까지 가는 시간이 비정상적으로 길다.

관찰된 증상:

- preview가 먼저 뜬다.
- 첫 WebSocket 연결은 실패한다.
- fallback 이후 두 번째 연결에서야 `ws-connected`가 된다.
- 체감상 "다이어그램은 보이는데 협업이 늦게 붙는" 상태가 길게 지속된다.

## 정량 측정

### 화면/렌더 관찰

- 렌더된 노드 수: 약 `97`

### 협업 ready 시점 3회 측정

1. run 1
- 첫 `ws-ticket` 완료: `41.1s`
- preview fallback unlock: `42.4s`
- 최종 `ws-connected`: `44.3s`

2. run 2
- 첫 `ws-ticket` 완료: `44.5s`
- preview fallback unlock: `46.0s`
- 최종 `ws-connected`: `48.0s`

3. run 3
- 첫 `ws-ticket` 완료: `46.7s`
- preview fallback unlock: `47.7s`
- 최종 `ws-connected`: `50.8s`

### 평균

- 첫 `ws-ticket` 완료 평균: 약 `44.1s`
- preview fallback unlock 평균: 약 `45.4s`
- 최종 `ws-connected` 평균: 약 `47.7s`

### navigation timing 참고값

정적 page load timing은 편차가 컸고, 이번 이슈의 핵심 병목으로 보이지 않았다.

- `responseEnd`: `386ms ~ 30.6s`
- `domContentLoaded`: `2.0s ~ 32.0s`
- `load`: `2.0s ~ 32.0s`

이번 이슈에서 중요한 값은 위 수치보다 `협업 ready 시점`이다.

## 분석 내용

### 핵심 관찰

브라우저 메인 스레드에서 매우 긴 long task가 발생했다.

- long task count: `15`
- total long task time: `54,968ms`
- 최대 long task:
  - 시작: 약 `1.4s`
  - 지속 시간: 약 `41,199ms`

이 long task가 발생하는 동안 첫 협업 초기화도 같이 밀린다.

### 첫 연결 실패가 생기는 구조

관찰상 흐름은 아래와 같다.

1. 다이어그램 진입
2. 메인 스레드 장기 점유 발생
3. 첫 `ws-ticket` 처리와 preview fallback timer가 함께 지연
4. ticket이 실제로 사용되기 전에 유효 시간이 지나감
5. 첫 WebSocket handshake 실패
6. 두 번째 ticket 발급은 즉시 완료
7. 두 번째 WebSocket은 빠르게 연결

즉 "WebSocket이 느리다"가 아니라,
"첫 협업 초기화가 메인 스레드 장기 점유에 막혀 ticket 사용 시점을 놓친다"가 더 정확한 설명이다.

## 원인

현재까지 확보한 증거 기준의 루트코즈는 아래다.

- 다이어그램 초기 진입 직후 클라이언트 메인 스레드에서 약 `41초` 규모의 장기 동기 작업이 발생한다.
- 이 장기 작업 때문에 첫 `ws-ticket` 요청/소비와 preview fallback unlock이 함께 밀린다.
- [WsTicketService.java](/Users/j.jaeyo/Project/ETC/smart-erd/src/main/java/com/smarterd/domain/diagram/websocket/ticket/WsTicketService.java) 의 ticket TTL은 `30초`다.
- 따라서 첫 ticket은 실제 handshake 시점에는 stale/expired 상태가 되고, 첫 WebSocket handshake는 `200` 응답으로 실패한다.
- 이후 두 번째 ticket은 즉시 발급되고, 두 번째 WebSocket은 정상 연결된다.

한 줄로 요약하면:

**첫 협업 연결 실패의 직접 원인은 WebSocket transport가 아니라, 초기 다이어그램 runtime이 만든 메인 스레드 장기 점유다.**

## 배제된 가설

아래 항목들은 별도 실험으로 병목이 아님을 확인했다.

1. backend `/api/ws-ticket` 자체가 느리다
- teams 페이지에서 수동 `requestWsTicket()` 호출: 약 `61ms`
- diagram 페이지 수동 호출: 약 `41ms`
- 별도 probe 페이지 호출: 약 `38ms`

2. Vite proxy 자체가 느리다
- proxy 경유 수동 WS open: 약 `6ms`

3. backend WS transport 자체가 느리다
- backend 직결 수동 WS open: 약 `2ms`

4. 일반 fetch/XHR이 막힌다
- 동일 페이지 수동 `fetch('/api/ws-ticket')`: 약 `433ms`
- 동일 페이지 수동 `XMLHttpRequest`: 약 `417ms`
- document start 초기 XHR: 약 `94ms`

5. API 동시성 자체가 병목이다
- `me`, `bootstrap`, `diagram`, dictionary API를 섞어 호출한 뒤 `ws-ticket`을 호출해도
  `ws-ticket`은 약 `98ms` 수준이었다.

즉 서버, 프록시, transport, 일반 API 호출이 아니라
**실제 diagram runtime 진입 직후의 클라이언트 메인 스레드 장기 점유**가 문제다.

## 관련 코드

이번 조사에서 직접 봐야 하는 지점은 아래다.

- [use-diagram-collaboration-runtime.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/use-diagram-collaboration-runtime.ts)
- [diagram-collaboration-provider-connection.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-collaboration-provider-connection.ts)
- [YjsProvider.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/YjsProvider.ts)
- [diagram-preview-hydration-controller.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-preview-hydration-controller.ts)
- [WsTicketService.java](/Users/j.jaeyo/Project/ETC/smart-erd/src/main/java/com/smarterd/domain/diagram/websocket/ticket/WsTicketService.java)

## 조치 방법

다음 수정은 WebSocket retry를 먼저 건드리기보다, 아래 순서로 가는 것이 맞다.

### 1. 초기 long task의 실제 작업 단위 식별

- 다이어그램 진입 직후 메인 스레드에서 어떤 동기 작업이 `40초+`를 점유하는지 찾는다.
- 후보:
  - 대량 노드/엣지 projector materialize
  - preview hydration
  - code/runtime 초기 계산
  - heavy selector/store recompute

### 2. 장기 작업 분할 또는 defer

- 첫 협업 ticket이 `30초 TTL` 안에서 소비되도록
  초기 runtime 작업을 분할하거나 뒤로 미룬다.
- 목표는 "preview-first"보다 앞서
  "first ticket usable"를 확보하는 쪽이다.

### 3. fallback timer 기준 재검토

- 현재 fallback timer는 메인 스레드 장기 점유와 함께 밀린다.
- 필요하면 "경과 시간 기준"이 아니라
  "실제 협업 초기화 단계 기준"으로 다시 설계한다.

### 4. large diagram 회귀 측정 고정

- 같은 다이어그램을 대상으로 3회 평균 측정을 자동화하거나 반자동화한다.
- 추적 지표:
  - first ticket 완료
  - preview fallback unlock
  - final ws-connected
  - long task 최대값

## 다음 작업 정의

이 항목의 다음 구현 목표는 아래다.

1. `GH 도급` 다이어그램 초기 진입 직후의 long task 구간을 코드 레벨로 분해한다.
2. 가장 큰 동기 작업을 분할/defer해서 first ticket이 `30초` 안에 소비되게 만든다.
3. 같은 다이어그램으로 3회 재측정해서 `ws-connected` 평균 시간을 의미 있게 낮춘다.

## 판단

- 현재 상태는 성능/초기화 이슈다.
- 기능이 완전히 깨진 것은 아니지만, large diagram 체감 품질에는 직접 영향이 있다.
- 따라서 이 항목은 deferred 관측이 아니라 **실제 다음 최적화 작업**으로 승격하는 것이 맞다.
