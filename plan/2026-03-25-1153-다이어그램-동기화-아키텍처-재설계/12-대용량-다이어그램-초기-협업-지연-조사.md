# 12. 대용량 다이어그램 초기 협업 지연 조사 및 2차 조치

## 목적

이 문서는 `GH도급사업 > GH 도급` 다이어그램에서 확인한
대용량 sync 캔버스 성능 이슈를 조사하고, 1차 조치 결과와 남은 2차 작업을 정리한다.

목표는 아래 두 가지다.

- 현상을 재현 가능한 형태로 기록한다.
- 원인, 1차 조치 결과, 남은 후속 최적화 범위를 명확히 고정한다.

## 대상 환경

- frontend: `http://localhost:4501`
- backend: `http://localhost:9501`
- 대상 프로젝트: `GH도급사업`
- 대상 다이어그램: `GH 도급`
- baseline 조사: `2026-03-27`
- 1차 수정 검증: `2026-03-30`
- 2차 수정 검증: `2026-03-30`
- 3차 수정 검증: `2026-03-30`

## 현재 상태

- 진단 단계는 완료했다.
- 1차, 2차, 3차 수정도 완료했다.
- hard target 중 `first ws-ticket <= 3s`, `final ws-connected <= 5s`, `node/preview 회귀 없음`은 충족했다.
- `max long task <= 500ms` 는 아직 미달이지만, `~600ms`대까지 내려온 상태다.

## What already exists

이번 작업은 새 성능 프레임워크를 만드는 것이 아니라, 이미 있는 협업 초기화 경로를 계측하고
그 안의 hotspot을 줄이는 작업으로 본다.

- 협업 runtime 진입 경로는 이미 분리돼 있다.
  - [use-diagram-collaboration-runtime.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/use-diagram-collaboration-runtime.ts)
  - [diagram-collaboration-provider-connection.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-collaboration-provider-connection.ts)
- preview fallback 경로도 이미 분리돼 있다.
  - [diagram-preview-hydration-controller.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-preview-hydration-controller.ts)
- Yjs provider와 ws-ticket 경계도 이미 분리돼 있다.
  - [YjsProvider.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/YjsProvider.ts)
  - [WsTicketService.java](/Users/j.jaeyo/Project/ETC/smart-erd/src/main/java/com/smarterd/domain/diagram/websocket/ticket/WsTicketService.java)

즉 이번 작업의 중심은 새 abstraction이 아니라,
기존 초기화 파이프라인을 코드 레벨로 분해하고 가장 큰 동기 작업을 줄이는 것이다.

## baseline 현상

대상 다이어그램에 처음 진입하면 화면 자체는 비교적 빨리 뜬다.  
하지만 협업 ready 상태까지 가는 시간이 비정상적으로 길다.

관찰된 증상:

- preview가 먼저 뜬다.
- 첫 WebSocket 연결은 실패한다.
- fallback 이후 두 번째 연결에서야 `ws-connected`가 된다.
- 체감상 "다이어그램은 보이는데 협업이 늦게 붙는" 상태가 길게 지속된다.

## baseline 정량 측정

### 화면/렌더 관찰

- 렌더된 노드 수: 약 `97`

### 협업 ready 시점 3회 측정

1. run 1
- 첫 `ws-ticket` 완료: `41.1s`
- preview fallback unlock: `42.4s`
- 최종 `ws-connected`: `44.3s`

2. run 2
- 첫 `ws-ticket` 완료: `44.5s`
- preview fallback unlock: `46.0s`
- 최종 `ws-connected`: `48.0s`

3. run 3
- 첫 `ws-ticket` 완료: `46.7s`
- preview fallback unlock: `47.7s`
- 최종 `ws-connected`: `50.8s`

### 평균

- 첫 `ws-ticket` 완료 평균: 약 `44.1s`
- preview fallback unlock 평균: 약 `45.4s`
- 최종 `ws-connected` 평균: 약 `47.7s`

### navigation timing 참고값

정적 page load timing은 편차가 컸고, 이번 이슈의 핵심 병목으로 보이지 않았다.

- `responseEnd`: `386ms ~ 30.6s`
- `domContentLoaded`: `2.0s ~ 32.0s`
- `load`: `2.0s ~ 32.0s`

이번 이슈에서 중요한 값은 위 수치보다 `협업 ready 시점`이다.

## baseline 분석 결과

### sync 캔버스 구조 문제

조사 과정에서 `sync` 모드가 persisted canvas 위에 shared draft overlay를 사실상 한 벌 더 겹쳐 그리는 것을 확인했다.

- flow node count: `194`
- handle count: `9,536`
- DOM node count: 약 `44,889`

즉 baseline의 심각한 멈춤은 단순 WebSocket 지연이 아니라,
대형 persisted canvas와 full shared draft overlay가 겹친 상태에서 handle/measurement 비용이 폭증한 결과였다.

## 분석 내용

### startup phase 다이어그램

이번 이슈는 아래 파이프라인 기준으로 봐야 한다.

```text
Diagram route enter
  -> bootstrap/data fetch
  -> preview hydration
  -> collaboration runtime start
  -> request ws-ticket
  -> websocket handshake
  -> ws-connected
```

현재 관찰상 병목은 `request ws-ticket` 자체가 아니라,
그 이전 단계들 중 하나가 메인 스레드를 너무 오래 점유해
`request ws-ticket -> handshake` 시점을 뒤로 밀어내는 쪽이다.

### 핵심 관찰

브라우저 메인 스레드에서 매우 긴 long task가 발생했다.

- long task count: `15`
- total long task time: `54,968ms`
- 최대 long task:
  - 시작: 약 `1.4s`
  - 지속 시간: 약 `41,199ms`

이 long task가 발생하는 동안 첫 협업 초기화도 같이 밀린다.

### 첫 연결 실패가 생기는 구조

관찰상 흐름은 아래와 같다.

1. 다이어그램 진입
2. 메인 스레드 장기 점유 발생
3. 첫 `ws-ticket` 처리와 preview fallback timer가 함께 지연
4. ticket이 실제로 사용되기 전에 유효 시간이 지나감
5. 첫 WebSocket handshake 실패
6. 두 번째 ticket 발급은 즉시 완료
7. 두 번째 WebSocket은 빠르게 연결

즉 "WebSocket이 느리다"가 아니라,
"첫 협업 초기화가 메인 스레드 장기 점유에 막혀 ticket 사용 시점을 놓친다"가 더 정확한 설명이다.

## 원인

현재까지 확보한 증거 기준의 루트코즈는 아래다.

- 다이어그램 초기 진입 직후 클라이언트 메인 스레드에서 약 `41초` 규모의 장기 동기 작업이 발생한다.
- 이 장기 작업 때문에 첫 `ws-ticket` 요청/소비와 preview fallback unlock이 함께 밀린다.
- [WsTicketService.java](/Users/j.jaeyo/Project/ETC/smart-erd/src/main/java/com/smarterd/domain/diagram/websocket/ticket/WsTicketService.java) 의 ticket TTL은 `30초`다.
- 따라서 첫 ticket은 실제 handshake 시점에는 stale/expired 상태가 되고, 첫 WebSocket handshake는 `200` 응답으로 실패한다.
- 이후 두 번째 ticket은 즉시 발급되고, 두 번째 WebSocket은 정상 연결된다.

한 줄로 요약하면:

**첫 협업 연결 실패의 직접 원인은 WebSocket transport가 아니라, 초기 다이어그램 runtime이 만든 메인 스레드 장기 점유다.**

그리고 그 장기 점유의 중심에는 아래 두 축이 있었다.

1. large sync canvas 위의 full shared draft overlay 중복 렌더
2. 대량 `Handle` DOM과 `TableNode.updateNodeInternals()` 초기 측정 비용

## 배제된 가설

아래 항목들은 별도 실험으로 병목이 아님을 확인했다.

1. backend `/api/ws-ticket` 자체가 느리다
- teams 페이지에서 수동 `requestWsTicket()` 호출: 약 `61ms`
- diagram 페이지 수동 호출: 약 `41ms`
- 별도 probe 페이지 호출: 약 `38ms`

2. Vite proxy 자체가 느리다
- proxy 경유 수동 WS open: 약 `6ms`

3. backend WS transport 자체가 느리다
- backend 직결 수동 WS open: 약 `2ms`

4. 일반 fetch/XHR이 막힌다
- 동일 페이지 수동 `fetch('/api/ws-ticket')`: 약 `433ms`
- 동일 페이지 수동 `XMLHttpRequest`: 약 `417ms`
- document start 초기 XHR: 약 `94ms`

5. API 동시성 자체가 병목이다
- `me`, `bootstrap`, `diagram`, dictionary API를 섞어 호출한 뒤 `ws-ticket`을 호출해도
  `ws-ticket`은 약 `98ms` 수준이었다.

즉 서버, 프록시, transport, 일반 API 호출이 아니라
**실제 diagram runtime 진입 직후의 클라이언트 메인 스레드 장기 점유**가 문제다.

## 관련 코드

이번 조사에서 직접 봐야 하는 지점은 아래다.

- [use-diagram-collaboration-runtime.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/use-diagram-collaboration-runtime.ts)
- [diagram-collaboration-provider-connection.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-collaboration-provider-connection.ts)
- [YjsProvider.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/YjsProvider.ts)
- [diagram-preview-hydration-controller.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-preview-hydration-controller.ts)
- [WsTicketService.java](/Users/j.jaeyo/Project/ETC/smart-erd/src/main/java/com/smarterd/domain/diagram/websocket/ticket/WsTicketService.java)

## 1차 수정 내용

- large diagram에서 persisted canvas 위 full shared draft overlay를 숨기고, 안내 배너만 남겼다.
- 컬럼 row의 `Handle` DOM은 연결된 컬럼이거나 FK 모드일 때만 렌더하도록 줄였다.
- `TableNode.updateNodeInternals()` 는 프레임 단위 배치 실행으로 바꿨고, 핸들이 없는 테이블은 초기 update를 건너뛰게 정리했다.

관련 코드:

- [use-diagram-page-runtime-state.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/pages/diagram/use-diagram-page-runtime-state.ts)
- [DiagramPage.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/pages/diagram/DiagramPage.tsx)
- [StaticColumnRow.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/StaticColumnRow.tsx)
- [EditableColumnRow.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/EditableColumnRow.tsx)
- [TableNode.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/TableNode.tsx)

## 1차 수정 후 측정

### 핵심 지표

- `first ws-ticket`: 약 `1.3s`
- `final ws-connected`: 약 `4.4s`
- `max long task`: 약 `2.6 ~ 2.9s`
- flow node count: `97`
- handle count: `624`
- DOM node count: 약 `17.5k ~ 21.0k`

### baseline 대비 변화

- `first ws-ticket`: 약 `44.1s` -> 약 `1.3s`
- `final ws-connected`: 약 `47.7s` -> 약 `4.4s`
- `max long task`: 약 `41.2s` -> 약 `2.6 ~ 2.9s`
- flow node count: `194` -> `97`
- handle count: `9,536` -> `624`

### 회귀 확인

- `npm run build` 통과
- `diagram-auto-edge-obstacle-route.spec.ts` 통과
- `diagram-collaboration.spec.ts` 통과

즉 1차 수정은 startup unusable 상태를 깨는 데는 성공했고,
관계선/협업 경로도 현재 기준으로 회귀 없이 유지됐다.

## 2차 수정 내용

- `QuickTermDialog` 는 실제로 열릴 때만 렌더하게 바꿔 테이블별 숨은 dialog 트리를 제거했다.
- `TableNode` 와 `PreviewTableNodes` 는 컬럼 메타와 connected column 계산을 부모 단위로 재사용하게 정리했다.
- static row는 행별 `Tooltip`, `useTranslation`, `useErdFkMode` 의존을 걷어내고, 경고는 native `title` 기반으로 바꿨다.
- preview unlock은 `ws-connected` 이후 Y.Doc이 비어 있으면 1초 grace 뒤 fallback 하게 바꿔 `preview-unlocked` 를 줄였다.
- dev 계열 프런트는 WS를 Vite proxy 대신 백엔드로 직접 붙게 바꿨고, `YjsProvider` 중복 socket 생성 가드로 startup 경로를 안정화했다.

관련 코드:

- [TableNode.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/TableNode.tsx)
- [PreviewTableNodes.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/PreviewTableNodes.tsx)
- [StaticColumnRow.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/StaticColumnRow.tsx)
- [useColumnValidation.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/hooks/useColumnValidation.ts)
- [diagram-preview-hydration-controller.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/channel/diagram/diagram-preview-hydration-controller.ts)
- [YjsProvider.ts](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/collaboration/YjsProvider.ts)

## 2차 수정 후 측정

### 핵심 지표

- `ticket-issued`: 약 `0.58 ~ 0.61s`
- `ws-connected`: 약 `1.88 ~ 1.89s`
- `preview-unlocked`: 약 `2.96 ~ 3.12s`
- startup `max long task`: 약 `1.03 ~ 1.10s`
- interaction `max long task`: 약 `167 ~ 203ms`
- interaction `max frame gap`: 약 `168 ~ 333ms`

### baseline 대비 변화

- `ticket-issued`: 약 `44.1s` -> 약 `0.6s`
- `ws-connected`: 약 `47.7s` -> 약 `1.9s`
- `preview-unlocked`: 약 `45.4s` -> 약 `3.0s`
- startup `max long task`: 약 `41.2s` -> 약 `1.0 ~ 1.1s`
- flow node count: `194` -> `97`
- handle count: `9,536` -> `624`

### 회귀 확인

- `npm run build` 통과
- `diagram-auto-edge-obstacle-route.spec.ts` 통과
- `diagram-collaboration.spec.ts` 통과

즉 2차 수정은 startup ready 시간과 interaction 체감을 한 단계 더 낮추는 데 성공했다.
다만 `max long task <= 500ms` 는 아직 남아 있고, 큰 다이어그램의 정적 렌더 비용은 추가로 줄일 여지가 있다.

## 3차 수정 내용

- connected column 계산을 노드별 `useStore` 스캔이 아니라 캔버스 단위 공유 context로 올렸다.
- large diagram overview 모드에서는 비연결 컬럼을 전체 렌더하지 않고, 연결된 컬럼 또는 앞쪽 1개만 렌더한 뒤 요약 행으로 접는다.
- compact overview 임계치는 minimap 표시 임계치와 분리해서 관리한다.

관련 코드:

- [ConnectedColumnIdsContext.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/ConnectedColumnIdsContext.tsx)
- [CompactTableRenderingContext.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/CompactTableRenderingContext.tsx)
- [ERDCanvas.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/ERDCanvas.tsx)
- [PreviewCanvas.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/PreviewCanvas.tsx)
- [TableNode.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/TableNode.tsx)
- [PreviewTableNodes.tsx](/Users/j.jaeyo/Project/ETC/smart-erd/client/src/components/erd/PreviewTableNodes.tsx)

## 3차 수정 후 측정

### 핵심 지표

- sync `ticket-issued`: 약 `358 ~ 398ms`
- sync `ws-connected`: 약 `1.16 ~ 1.35s`
- sync `preview-unlocked`: 약 `2.28 ~ 2.39s`
- startup `max long task`: 약 `596 ~ 616ms`
- interaction `max long task`: 약 `117 ~ 138ms`
- interaction `max frame gap`: 약 `116 ~ 141ms`
- sync DOM node count: 약 `7.3k`

### 2차 수정 대비 변화

- `ticket-issued`: 약 `0.6s` -> 약 `0.36 ~ 0.40s`
- `ws-connected`: 약 `1.9s` -> 약 `1.16 ~ 1.35s`
- `preview-unlocked`: 약 `3.0s` -> 약 `2.28 ~ 2.39s`
- startup `max long task`: 약 `1.0 ~ 1.1s` -> 약 `596 ~ 616ms`
- interaction `max long task`: 약 `167 ~ 203ms` -> 약 `117 ~ 138ms`
- interaction `max frame gap`: 약 `168 ~ 333ms` -> 약 `116 ~ 141ms`
- sync DOM node count: 약 `17.5k ~ 21.0k` -> 약 `7.3k`

### 회귀 확인

- `npm run build` 통과
- `diagram-auto-edge-obstacle-route.spec.ts` 통과
- `diagram-collaboration.spec.ts --trace off` 통과

즉 3차 수정은 남아 있던 startup long task와 interaction frame gap을 더 줄이는 데 성공했고,
`max long task <= 500ms` 바로 아래까지 접근한 상태다.

## 남은 문제

- `max long task <= 500ms` 는 아직 못 맞췄다.
- large diagram 렌더 후 pan/zoom 상호작용에도 frame gap이 남아 있다.
- overlay를 숨기는 방식은 성능에는 맞지만, large diagram에서 sync 화면의 draft awareness를 제한하는 절충안이다.
- 최근 reconnect 조사 결과, 반복 reconnect 자체는 앱 로직 회귀가 아니라 연결 수 초과(`4408`/`4409`) 상황과 구분해야 한다.

## 후속 조치 방법

다음 수정은 다시 WebSocket retry를 먼저 건드리기보다, 아래 순서로 가는 것이 맞다.

### 1. `TableNode` / `PreviewTableNodes` 잔여 row DOM 추가 축소

- large diagram overview에서 viewport 밖 테이블의 컬럼 row를 더 공격적으로 줄일지 검토한다.
- summary row 이후 상세 컬럼을 클릭/확장할 때만 지연 렌더하는 패턴을 검토한다.
- compact overview threshold를 node count 외에 viewport/zoom 조건과 합칠지 본다.

### 2. 렌더 후 interaction frame gap 축소

- pan/zoom/selection 시 남는 long task를 줄인다.
- 필요하면 정적 테이블 row 메모화나 계산 캐시를 검토한다.

### 3. large diagram 회귀 측정 유지

- 같은 다이어그램을 대상으로 3회 평균 측정을 자동화하거나 반자동화한다.
- 추적 지표:
  - first ticket 완료
  - final ws-connected
  - long task 최대값
  - interaction frame gap

## 성공 기준

후속 마감은 아래 네 항목 중 최소 세 항목을 만족해야 한다.

1. `first ws-ticket` 평균 `<= 3s`
2. `final ws-connected` 평균 `<= 5s`
3. `max long task` `<= 500ms`
4. large diagram 노드 수와 preview 표시가 기존과 동일하게 유지된다

## 회귀/성능 검증 계획

### 자동/반자동 검증 대상

- large diagram 첫 진입
- rendered node count
- first ticket 시간
- final ws-connected 시간
- 최대 long task
- interaction frame gap

### 회귀 검증 항목

- 노드/엣지가 누락되지 않는다
- preview가 영구 lock 상태가 되지 않는다
- `connected=true`까지 최종 도달한다
- 첫 연결 실패가 사라지거나, 최소한 최종 connected 시간이 budget 안으로 들어온다

### 권장 harness

- Playwright 기반 large diagram 측정 스크립트 1개
- 필요 시 브라우저 내 `performance.mark()` + `PerformanceObserver(longtask)` 사용
- 결과는 3회 반복 평균으로 기록

## Failure modes

### A. phase 계측이 부정확한 경우

- 실패 형태: 잘못된 구간을 hotspot으로 판단할 수 있다
- 방어: 문서의 phase 이름과 코드 mark 이름을 1:1로 맞춘다

### B. hotspot defer 후 일부 렌더가 늦거나 누락되는 경우

- 실패 형태: 노드/엣지/preview 일부가 사라지거나 지연될 수 있다
- 방어: node count, preview 표시, connected 도달을 회귀 검증에 포함한다

### C. overlay suppression이 draft awareness를 과하게 낮추는 경우

- 실패 형태: large diagram sync 화면에서 초안 awareness가 너무 약해질 수 있다
- 방어: large diagram 안내 배너를 유지하고, 실제 persisted 결과는 계속 동일하게 보장한다

### D. ticket TTL 문제를 증상 완화로만 가리는 경우

- 실패 형태: 근본 원인은 남고 환경에 따라 다시 터진다
- 방어: runtime long task 축소를 우선하고, TTL 조정은 여전히 fallback 옵션으로만 둔다

## NOT in scope

- WebSocket transport 전면 교체
- ticket TTL 즉시 상향을 1차 해법으로 채택하는 것
- generic perf infra 신설
- 대규모 virtualization / canvas 아키텍처 재작성
- unrelated page/runtime cleanup 추가 확장

## 다음 작업 정의

이 항목의 다음 구현 목표는 아래다.

1. `TableNode` 정적 렌더와 column warning/dictionary 계산 중 남은 hotspot을 다시 식별한다.
2. 렌더 후 interaction frame gap을 줄이는 2차 수정 1개만 수행한다.
3. 같은 `GH 도급` 다이어그램으로 3회 재측정해서 `max long task <= 500ms`에 얼마나 가까워졌는지 확인한다.
4. 결과를 이 문서에 다시 누적한다.

## 판단

- 현재 상태는 baseline unusable 상태는 벗어났지만, 아직 large diagram 성능 최적화가 끝난 것은 아니다.
- 따라서 이 항목은 종료가 아니라 **2차 최적화 진행 중**으로 보는 것이 맞다.
