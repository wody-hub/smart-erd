# 04-01-PLAN: BE 기반 — Project 엔티티 확장 + Flyway + business-overview GET/PATCH API

## 1. 파일 목록

### 신규 생성

| 파일 경로 | 역할 |
|---|---|
| `src/main/resources/db/migration/V20260414_01__project_business_overview_columns.sql` | 6개 컬럼 DDL 마이그레이션 |
| `src/main/java/com/smarterd/api/project/dto/BusinessOverviewResponse.java` | GET 응답 DTO (record) |
| `src/main/java/com/smarterd/api/project/dto/UpdateBusinessOverviewRequest.java` | PATCH 요청 DTO (record) |

### 수정

| 파일 경로 | 변경 내용 |
|---|---|
| `Project.java` | 6개 필드 추가, `updateBusinessOverview()` 메서드 추가 |
| `MessageCode.java` | `ERROR_BUSINESS_INVALID_PROJECT_PERIOD` enum 값 추가 |
| `messages.properties` / `messages_ko.properties` | 에러 + validation 메시지 추가 |
| `ProjectService.java` | `getBusinessOverview()`, `updateBusinessOverview()`, `BusinessOverviewResult` inner record |
| `ProjectController.java` | `GET/PATCH /{projectId}/business-overview` 엔드포인트 추가 |
| `TeamService.java` | `countMembers(Team team)` 메서드 추가 |
| `DiagramRepository.java` | `countByProjectAndDeletedAtIsNull()` 메서드 추가 |
| `TeamMemberRepository.java` | `countByTeam()` 메서드 추가 (TeamService에서 호출) |

---

## 2. 구현 순서

```
Step 1: Flyway SQL 마이그레이션
Step 2: MessageCode enum 추가
Step 3: i18n 메시지 파일 (messages.properties + messages_ko.properties)
Step 4: Project.java 엔티티 확장
Step 5: Repository 메서드 추가 (TeamMember + Diagram)
Step 6: ProjectService.java 확장
Step 7: DTO 생성 (BusinessOverviewResponse, UpdateBusinessOverviewRequest)
Step 8: ProjectController.java 확장
```

---

## 3. 상세 명세

### Step 1: Flyway SQL

**파일:** `V20260414_01__project_business_overview_columns.sql`

```sql
-- Phase 4: Project 사업 개요 컬럼 추가
ALTER TABLE projects ADD COLUMN IF NOT EXISTS client_company VARCHAR(200);
ALTER TABLE projects ADD COLUMN IF NOT EXISTS contractor_company VARCHAR(200);
ALTER TABLE projects ADD COLUMN IF NOT EXISTS contract_amount BIGINT;
ALTER TABLE projects ADD COLUMN IF NOT EXISTS project_start_date DATE;
ALTER TABLE projects ADD COLUMN IF NOT EXISTS project_end_date DATE;
ALTER TABLE projects ADD COLUMN IF NOT EXISTS project_scope TEXT;
```

- 모두 nullable (NOT NULL 제약 없음)
- `contract_amount`: BIGINT (Java Long 매핑)
- `project_scope`: TEXT (길이 제한 없는 서술형)
- `IF NOT EXISTS` 패턴 (기존 마이그레이션 관례)

### Step 2: MessageCode

`ERROR_BUSINESS_INVALID_PROJECT_PERIOD("error.business.invalid-project-period")` 추가.
알파벳 정렬 기준으로 `ERROR_BUSINESS_PROJECT_TEAM_MISMATCH` 앞에 삽입.

### Step 3: i18n 메시지

**messages.properties:**
```properties
error.business.invalid-project-period=Project start date must not be after end date
validation.size.client-company=Client company must be {max} characters or less
validation.size.contractor-company=Contractor company must be {max} characters or less
validation.min.contract-amount=Contract amount must be 0 or greater
validation.project-period.pair-required=Project start date and end date must both be provided or both be empty
```

**messages_ko.properties:**
```properties
error.business.invalid-project-period=프로젝트 시작일은 종료일보다 이후일 수 없습니다
validation.size.client-company=발주사는 {max}자 이하로 입력해 주세요
validation.size.contractor-company=수주사는 {max}자 이하로 입력해 주세요
validation.min.contract-amount=계약 금액은 0 이상이어야 합니다
validation.project-period.pair-required=프로젝트 시작일과 종료일은 함께 입력하거나 함께 비워야 합니다
```

### Step 4: Project 엔티티 확장

**추가 필드** (기존 `description` 뒤에 삽입):

```java
@Nullable @Column(name = "client_company", length = 200)
private String clientCompany;

@Nullable @Column(name = "contractor_company", length = 200)
private String contractorCompany;

@Nullable @Column(name = "contract_amount")
private Long contractAmount;

/** DATE 타입 사용 근거: 시간(시/분/초)이 아닌 날짜만 저장하는 필드이므로 Instant/timestamptz가 아닌 LocalDate/DATE가 적합 */
@Nullable @Column(name = "project_start_date")
private LocalDate projectStartDate;

@Nullable @Column(name = "project_end_date")
private LocalDate projectEndDate;

@Nullable @Column(name = "project_scope", columnDefinition = "TEXT")
private String projectScope;
```

**추가 메서드:**

```java
public void updateBusinessOverview(
    @Nullable String clientCompany,
    @Nullable String contractorCompany,
    @Nullable Long contractAmount,
    @Nullable LocalDate startDate,
    @Nullable LocalDate endDate,
    @Nullable String projectScope
) {
    if (startDate != null && endDate != null && startDate.isAfter(endDate)) {
        throw new BusinessException(MessageCode.ERROR_BUSINESS_INVALID_PROJECT_PERIOD.code());
    }
    this.clientCompany = clientCompany;
    this.contractorCompany = contractorCompany;
    this.contractAmount = contractAmount;
    this.projectStartDate = startDate;
    this.projectEndDate = endDate;
    this.projectScope = projectScope;
}
```

**설계 의도:**
- 날짜 쌍 검증은 도메인 불변식 → 엔티티 캡슐화
- 한쪽만 null인 케이스는 DTO `@AssertTrue`로 처리

### Step 5: Repository 메서드 추가

**DiagramRepository:** `long countByProjectAndDeletedAtIsNull(Project project);`

JPA 파생 쿼리 — QueryDSL 불필요.

> **참고:** `TeamMember` count는 기존 의존 패턴 일관성을 위해 `TeamService`에 `long countMembers(Team team)` 메서드를 추가하여 위임한다 (ProjectService가 TeamMemberRepository를 직접 주입하지 않음).

### Step 6: ProjectService 확장

- `getBusinessOverview()`: verifyMembership → `teamService.countMembers(team)` + `diagramRepository.countByProject...` → `BusinessOverviewResult` 반환
- `updateBusinessOverview()`: `@Transactional`, verifyEditable → `project.updateBusinessOverview()` → count 쿼리 → 결과 반환
- `BusinessOverviewResult` inner record (11개 필드, `progressRate`는 Phase 4에서 항상 null)

> **SRP 기술 부채 마커:** 현재 `ProjectService`에 사업 개요 메서드 2개 추가는 허용 범위이나, Phase 5(WBS) 착수 시점에 `BusinessOverviewService` 분리를 검토한다. `ProjectService`가 3개 이상의 관심사(CRUD + 사업 개요 + WBS)를 담게 되면 반드시 분리 필요.

### Step 7: DTO

**BusinessOverviewResponse.java** (record):
- 11개 필드 (projectId, projectName, clientCompany, contractorCompany, contractAmount, projectStartDate, projectEndDate, projectScope, memberCount, documentCount, progressRate)
- `static BusinessOverviewResponse from(BusinessOverviewResult result)` 정적 팩토리
- `@Schema` 어노테이션 완비 — `progressRate`에 `@Schema(description = "Phase 5(WBS) 예정, 현재 항상 null")` 명시
- 각 필드에 `/** 설명 */` Javadoc 포함

**UpdateBusinessOverviewRequest.java** (record):
- 6개 필드 (clientCompany, contractorCompany, contractAmount, projectStartDate, projectEndDate, projectScope)
- `@Size(max=200)` for company names, `@Min(0)` for amount
- `@AssertTrue isProjectPeriodPairValid()`: 날짜 쌍 정합성 (둘 다 null 또는 둘 다 non-null)

### Step 8: ProjectController 확장

```
GET  /{projectId}/business-overview  → 200 BusinessOverviewResponse
PATCH /{projectId}/business-overview → 200 BusinessOverviewResponse
```

- PATCH 선택 이유: 부분 업데이트 의미론, 6개 필드만 변경 (projectId/name은 기존 PUT에서 관리)
- `@Valid @RequestBody` + `@PatchMapping` import 추가

---

## 4. 데이터 흐름

### GET

```
JWT → loginId → verifyMembership → findProject → verifyBelongsToTeam
→ countByTeam + countByProjectAndDeletedAtIsNull
→ BusinessOverviewResult(progressRate: null)
→ BusinessOverviewResponse.from()
→ 200 OK
```

### PATCH

```
JWT + @Valid RequestBody → isProjectPeriodPairValid (400)
→ loginId → verifyEditable → findProject → verifyBelongsToTeam
→ project.updateBusinessOverview() (400 if startDate > endDate)
→ JPA Dirty Checking
→ count 쿼리 → BusinessOverviewResult → 200 OK
```

---

## 5. 체크리스트

- [ ] Flyway 버전이 `V20260402_01` 이후인지 확인
- [ ] 6개 컬럼 모두 `IF NOT EXISTS` 멱등성
- [ ] `@Nullable` = `org.springframework.lang.Nullable` (`@NonNullApi` 정책)
- [ ] `updateBusinessOverview()` 같은 날 허용 (isAfter만 검증)
- [ ] `@AssertTrue` 메서드명 `is` prefix (Bean Validation 규약)
- [ ] PATCH HTTP 메서드 + `@PatchMapping` import
- [ ] `./gradlew compileJava` 통과
- [ ] Flyway 마이그레이션 성공 로그
- [ ] Swagger UI 두 엔드포인트 노출 확인
