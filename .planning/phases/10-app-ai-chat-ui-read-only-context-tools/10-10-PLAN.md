---
phase: 10-app-ai-chat-ui-read-only-context-tools
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
  - src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
  - src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java
  - client/src/types/ai-chat.ts
  - client/src/hooks/useAiChatExecution.ts
  - client/test/unit/ai-chat-execution.test.ts
  - client/test/unit/ai-chat-api.test.ts
autonomous: true
gap_closure: true
requirements:
  - AI-CHAT-02
  - AI-READ-04
must_haves:
  truths:
    - "Team-context sends from `/teams/:teamId/projects` use the backend multi-project contract and can resolve current-team fanout questions."
    - "Phase 10 chat DTOs do not expose item-level `selectedResource` because chat read context is project/team-scoped."
    - "Frontend and backend request-contract tests agree on team fanout as `scopeMode: MULTI_PROJECT`."
  artifacts:
    - path: "client/src/hooks/useAiChatExecution.ts"
      provides: "Frontend team context to backend MULTI_PROJECT request mapping"
      contains: "MULTI_PROJECT"
    - path: "src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java"
      provides: "Chat request DTO without selectedResource false contract"
      contains: "isMultiProjectMode"
    - path: "src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java"
      provides: "Backend DTO contract tests for multi-project team scope and no selectedResource chat contract"
      contains: "MULTI_PROJECT"
  key_links:
    - from: "client/src/hooks/useAiChatExecution.ts"
      to: "src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java"
      via: "scopeMode"
      pattern: "MULTI_PROJECT"
    - from: "src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java"
      to: "src/main/java/com/smarterd/application/ai/chat/AiChatContextResolver.java"
      via: "ChatCommand.currentTeamMode and multiProjectQuestion"
      pattern: "isMultiProjectMode"
---

<objective>
Close chat request-contract gaps for current-team fanout and unsupported selected-resource scope.

Purpose: The client currently enables team-context sends but sends a weak server contract, and the chat API advertises selected resources that it neither validates nor applies. Phase 10 should preserve current-team multi-project questions while keeping the chat DTO honest and project/team-scoped.
Output: Frontend team requests encode `MULTI_PROJECT`, backend DTO tests prove current-team fanout flags, and selectedResource is removed from the Phase 10 chat contract.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-CONTEXT.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VERIFICATION.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-REVIEW.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-05-SUMMARY.md
@src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
@src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
@client/src/hooks/useAiChatExecution.ts
@client/src/types/ai-chat.ts
@src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java
@client/test/unit/ai-chat-execution.test.ts

<interfaces>
Existing contracts to preserve:
- `AiChatRequest.toCommand()` maps frontend context hints to `AiChatExecutionService.ChatCommand`.
- Backend team fanout works when `ChatCommand.currentTeamMode()` and `ChatCommand.multiProjectQuestion()` are both true.
- `buildAiChatRequest` is the single frontend request builder for `/api/ai/chat`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unsupported selectedResource from the Phase 10 chat contract</name>
  <files>src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java, src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java, src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java, client/src/types/ai-chat.ts, client/src/hooks/useAiChatExecution.ts, client/test/unit/ai-chat-api.test.ts, client/test/unit/ai-chat-execution.test.ts</files>
  <read_first>
    - src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
    - src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
    - src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java
    - client/src/types/ai-chat.ts
    - client/src/hooks/useAiChatExecution.ts
    - client/test/unit/ai-chat-api.test.ts
    - client/test/unit/ai-chat-execution.test.ts
    - .planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VERIFICATION.md
  </read_first>
  <behavior>
    - `AiChatRequest` no longer has a `selectedResource` record component.
    - `AiChatExecutionService.ChatCommand` no longer has an `AiSelectedResource` component or import.
    - Frontend `AiChatRequest` type and `buildAiChatRequest` no longer expose or emit `selectedResource`.
    - Phase 9 provider execute DTOs and `SelectedResourceValidator` remain untouched.
  </behavior>
  <action>
    Follow the Phase 10 pattern map while narrowing the chat contract: use `src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteRequest.java` as the backend record-to-command DTO analog, use `client/src/types/ai-provider.ts` as the frontend typed DTO separation analog, and preserve the hook/API boundary style shown in `client/src/hooks/useAiProviderStatus.ts`.

    Remove the `selectedResource` component from `AiChatRequest` and from its `toCommand()` mapping. Remove the `AiSelectedResource` import and field from `AiChatExecutionService.ChatCommand`, updating convenience constructors and call sites as needed.

    Remove `selectedResource` from the frontend `AiChatRequest` type and keep `buildAiChatRequest` focused on message, team/project IDs, project name, scope mode, locale, context, and selectedContext. Do not remove Phase 9 `AiProviderExecuteRequest.selectedResource` or `AiSelectedResourceRequest`; this task only closes the Phase 10 chat false contract.

    Update `AiChatDtoContractTest` so it no longer constructs or asserts `selectedResource` for chat. Add a regression assertion that serialized chat requests/responses do not include selectedResource. Update frontend unit tests so the request builder and API request fixture compile without selectedResource.
  </action>
  <acceptance_criteria>
    - The Phase 10 chat API no longer advertises item-level selected-resource reads.
    - Project/team read-tool scope and all existing chat request fields continue to work.
    - Phase 9 provider selected-resource validation remains available outside the chat DTO.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.smarterd.api.ai.dto.AiChatDtoContractTest" --tests "com.smarterd.api.ai.AiChatControllerMvcTest" --tests "com.smarterd.application.ai.chat.AiChatExecutionServiceTest"</automated>
    <automated>cd client && npm run test:unit</automated>
    <automated>! rg "selectedResource" src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java client/src/types/ai-chat.ts client/src/hooks/useAiChatExecution.ts</automated>
  </verify>
  <done>Chat DTOs and frontend request builders contain no `selectedResource` field, while targeted backend and frontend tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map team context sends to backend MULTI_PROJECT contract</name>
  <files>client/src/hooks/useAiChatExecution.ts, client/test/unit/ai-chat-execution.test.ts, src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java</files>
  <read_first>
    - client/src/hooks/useAiChatExecution.ts
    - client/test/unit/ai-chat-execution.test.ts
    - src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
    - src/main/java/com/smarterd/application/ai/chat/AiChatContextResolver.java
    - src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java
    - .planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-UI-SPEC.md
  </read_first>
  <behavior>
    - A send built from a team route context such as `/teams/:teamId/projects` remains sendable when provider is available and message is non-empty.
    - `buildAiChatRequest` maps `context.kind === "team"` or `"multi-project"` to `scopeMode: "MULTI_PROJECT"`, keeps `teamId`, and sends `projectId: null`.
    - Backend `AiChatRequest.toCommand()` treats that request as `currentTeamMode=true` and `multiProjectQuestion=true`.
  </behavior>
  <action>
    Follow the existing provider/chat boundary analogs while changing the scope mapping: use `src/main/java/com/smarterd/application/ai/AiExecutionGateway.java` and `src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteRequest.java` as backend command-mapping references, and keep the frontend hook behavior aligned with `client/src/hooks/useAiProviderStatus.ts` without importing axios, provider runtime, cancellation, or read-tool calls into the chat hook.

    Add a small helper in `useAiChatExecution.ts` that resolves the outgoing scope mode. It must return `MULTI_PROJECT` for team and multi-project contexts, and preserve project scope behavior for project contexts. Use this helper in `buildAiChatRequest`.

    Add frontend unit coverage with a team context fixture that represents `/teams/:teamId/projects`: `kind: "team"`, `teamId` present, `projectId` null, `scopeRequired: true`, and source/route metadata. Assert `resolveAiChatCanSend` returns `canSend: true` for provider AVAILABLE, and assert `buildAiChatRequest` returns `scopeMode: "MULTI_PROJECT"`, `teamId` present, and `projectId` null.

    Add backend DTO coverage in `AiChatDtoContractTest` constructing an `AiChatRequest` with `scopeMode: "MULTI_PROJECT"` and context kind `team`; assert the resulting `ChatCommand` has `currentTeamMode()` and `multiProjectQuestion()` both true. This is the cross-layer contract that lets `AiChatContextResolver` use the existing current-team fanout branch for D-06 and D-10.
  </action>
  <acceptance_criteria>
    - Current-team multi-project questions preserve D-06 and D-10 by using the backend multi-project contract instead of falling to weak scope.
    - Weak contexts remain blocked by `resolveAiChatCanSend`.
    - No frontend component calls read tools, provider runtime, Codex, Electron IPC, or axios directly.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit</automated>
    <automated>./gradlew test --tests "com.smarterd.api.ai.dto.AiChatDtoContractTest" --tests "com.smarterd.application.ai.chat.AiChatContextResolverTest"</automated>
    <automated>rg "MULTI_PROJECT" client/src/hooks/useAiChatExecution.ts client/test/unit/ai-chat-execution.test.ts src/test/java/com/smarterd/api/ai/dto/AiChatDtoContractTest.java</automated>
    <automated>! rg "axiosInstance|codex|electron|ipc|fetchWbs|fetchIssues|fetchTodos|fetchMilestones|readContext|providerContext" client/src/hooks/useAiChatExecution.ts client/test/unit/ai-chat-execution.test.ts</automated>
  </verify>
  <done>Frontend team sends produce `scopeMode: MULTI_PROJECT`, backend DTO maps them to current-team multi-project command flags, and unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route-derived team context -> `/api/ai/chat` | Client route hints cross into backend scope resolution and must use the explicit multi-project contract. |
| chat DTO -> backend read context | Chat request fields define which scopes the backend will authorize and read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Information Disclosure | `buildAiChatRequest` team scope mapping | mitigate | Encode team fanout as `MULTI_PROJECT` so backend resolver applies current-team project authorization instead of weak fallback. |
| T-10-10-02 | Spoofing | `AiChatRequest` selected resource contract | mitigate | Remove unsupported `selectedResource` from Phase 10 chat DTO so clients cannot imply item-level resource scope that chat does not validate or use. |
| T-10-10-03 | Tampering | frontend request builder | mitigate | Keep `/api/ai/chat` construction centralized in `useAiChatExecution`/`aiChatApi` tests and verify no direct read-tool/provider runtime access. |
| T-10-10-SC | Tampering | package-manager installs | accept | No npm, pip, cargo, or Gradle dependency install is planned. |
</threat_model>

<verification>
Run cross-layer contract tests:

- `./gradlew test --tests "com.smarterd.api.ai.dto.AiChatDtoContractTest" --tests "com.smarterd.api.ai.AiChatControllerMvcTest" --tests "com.smarterd.application.ai.chat.AiChatContextResolverTest" --tests "com.smarterd.application.ai.chat.AiChatExecutionServiceTest"`
- `cd client && npm run test:unit`
</verification>

<success_criteria>
Team-context chat sends route through the backend multi-project contract, and Phase 10 chat no longer accepts a selected-resource field that is not applied to read context.
</success_criteria>

<output>
Create `.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-10-SUMMARY.md` when done.
</output>
