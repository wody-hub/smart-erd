---
phase: 10-app-ai-chat-ui-read-only-context-tools
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/main/java/com/smarterd/application/ai/chat/AiChatContextResolver.java
  - src/main/java/com/smarterd/application/ai/chat/AiReadContextService.java
  - src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
  - src/main/java/com/smarterd/api/ai/AiChatController.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatSourceChipResponse.java
  - client/src/types/ai-chat.ts
  - client/src/stores/useAiChatStore.ts
  - client/src/hooks/useAiRouteContext.ts
  - client/src/components/ai/AiAnswerCard.tsx
  - client/src/components/ai/AiSourceChips.tsx
  - src/test/java/com/smarterd/application/ai/chat/AiChatContextResolverTest.java
  - src/test/java/com/smarterd/application/ai/chat/AiReadContextServiceTest.java
  - src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java
  - src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java
  - client/test/unit/ai-chat-store.test.ts
  - client/test/unit/ai-chat-context.test.ts
  - client/test/unit/ai-chat-response-cards.test.ts
  - client/e2e/smoke/ai-chat-drawer.spec.ts
autonomous: true
requirements:
  - AI-CHAT-01
  - AI-CHAT-02
  - AI-READ-01
  - AI-READ-02
  - AI-READ-03
  - AI-READ-04
user_setup: []
must_haves:
  truths:
    - "Missing Phase 10 test files and compile-safe production skeletons exist before backend/frontend implementation proceeds."
    - "Backend tests describe auth-first scope resolution, summary-first reads, chat assembler mapping, action rejection, and read-only chat HTTP behavior."
    - "Frontend tests describe route-independent persistence, scope derivation, answer cards, and global drawer smoke."
    - "Wave 0 validation runs fail only on behavioral RED assertions or pass; they must not fail because exact production class/module names are missing."
  artifacts:
    - path: "src/test/java/com/smarterd/application/ai/chat/AiChatContextResolverTest.java"
      provides: "Weak, ambiguous, conflicting, and unauthorized scope tests"
    - path: "src/test/java/com/smarterd/application/ai/chat/AiReadContextServiceTest.java"
      provides: "Summary-first authorized read-tool test contract"
    - path: "src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java"
      provides: "Chat assembler and provider-action rejection tests"
    - path: "src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java"
      provides: "Authenticated read-only chat HTTP boundary tests"
    - path: "client/test/unit/ai-chat-store.test.ts"
      provides: "Local persistence and no-secret storage tests"
    - path: "client/test/unit/ai-chat-context.test.ts"
      provides: "Route/manual/weak context resolver tests"
    - path: "client/test/unit/ai-chat-response-cards.test.ts"
      provides: "Sectioned answer card/source-chip tests"
    - path: "client/e2e/smoke/ai-chat-drawer.spec.ts"
      provides: "Authenticated drawer route-persistence smoke"
  key_links:
    - from: "10-VALIDATION.md"
      to: "Wave 0 test files"
      via: "Per-task verification map"
      pattern: "10-W0-0[1-7]"
---

<objective>
Create the Phase 10 automated test contract before implementation starts.

Purpose: 10-VALIDATION.md requires Wave 0 coverage for high-risk AI chat, scope, privacy, read-tool, and UI behavior.
Output: Backend, frontend unit, and Playwright smoke test files that later plans make green.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-CONTEXT.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VALIDATION.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-01-SUMMARY.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-03-SUMMARY.md
</context>

## Source Audit

Plan-set coverage: GOAL Phase 10 is covered by plans 10-01 through 10-07. REQ AI-CHAT-01, AI-CHAT-02, AI-READ-01, AI-READ-02, AI-READ-03, and AI-READ-04 are all represented in this plan's Wave 0 tests and implemented by later plans. RESEARCH requirements for no new packages, server-mediated read tools, typed HTTP APIs, Zustand/local storage, and Radix/shadcn-compatible UI are covered. CONTEXT decisions D-01 through D-30 are covered; deferred Phase 11/12 ideas are excluded.

## Must Haves

- Create minimal production skeletons for exact backend/frontend targets before tests import them. Skeletons may return safe empty/confirmation/failure values, but must not implement provider/read business behavior ahead of plans 10-02 through 10-07.
- Every missing test named in `10-VALIDATION.md` exists.
- The tests encode the locked decisions by ID in comments or test names where useful, especially D-01 through D-05, D-07 through D-15, D-19 through D-24, and D-27 through D-30.
- Initial targeted runs are allowed to be RED because behavior is not implemented yet; failures must be assertion failures against safe skeleton behavior, not missing-class, missing-module, syntax, import, or test-runner setup failures.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backend Wave 0 chat/read tests</name>
  <files>src/main/java/com/smarterd/application/ai/chat/AiChatContextResolver.java, src/main/java/com/smarterd/application/ai/chat/AiReadContextService.java, src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java, src/main/java/com/smarterd/api/ai/AiChatController.java, src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java, src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java, src/main/java/com/smarterd/api/ai/dto/AiChatSourceChipResponse.java, src/test/java/com/smarterd/application/ai/chat/AiChatContextResolverTest.java, src/test/java/com/smarterd/application/ai/chat/AiReadContextServiceTest.java, src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java, src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</files>
  <read_first>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VALIDATION.md</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md</file>
    <file>src/test/java/com/smarterd/api/ai/AiProviderControllerMvcTest.java</file>
    <file>src/test/java/com/smarterd/application/ai/AiExecutionGatewayTest.java</file>
    <file>src/main/java/com/smarterd/application/ai/validation/ProviderOutputValidator.java</file>
    <file>src/main/java/com/smarterd/application/ai/provider/AiProviderResult.java</file>
    <file>src/test/java/com/smarterd/domain/pm/issue/service/ProjectIssueServiceTest.java</file>
    <file>src/main/java/com/smarterd/domain/pm/common/ProjectContextLoader.java</file>
    <file>src/main/java/com/smarterd/application/ai/SelectedResourceValidator.java</file>
  </read_first>
  <behavior>
    - `AiChatContextResolverTest` covers weak context, ambiguous project names, misspelled/conflicting names, current-route scope, current-team multi-project expansion, and cross-team exclusion per D-07 through D-12 and AI-CHAT-02.
    - `AiReadContextServiceTest` covers overview, WBS, milestone, issue, current-user TODO, authorized member TODO summary by member/status/count only, and recent history/comment summaries per D-15 through D-22 and AI-READ-01 through AI-READ-04.
    - `AiChatExecutionServiceTest` covers the resolved chat assembler contract: confirmed facts, source chips, context, and confirmation candidates come from resolver/read services; provider `answer` maps only to `interpretation`; conclusion comes from the first confirmed fact/read summary or localized safe fallback; needs-confirmation comes only from resolver/read-service gaps; non-empty provider actions are rejected or omitted from chat responses.
    - `AiChatControllerMvcTest` covers authenticated `POST /api/ai/chat`, no anonymous execution, scope denial before provider invocation, confirmation response, provider failure mapping, and no action/write controls per D-27 through D-30.
  </behavior>
  <action>Create minimal backend production skeletons for `AiChatContextResolver`, `AiReadContextService`, `AiChatExecutionService`, `AiChatController`, `AiChatRequest`, `AiChatResponse`, and `AiChatSourceChipResponse` before writing tests that import them. Skeletons must compile, expose only the method/record signatures asserted by Wave 0 tests, return safe confirmation/empty/failure responses, and perform no provider/read-tool business logic. Then create the four backend test files using local JUnit 5, Mockito, MockMvc, AssertJ, and `TestJwtArgumentResolver` patterns. Name the production targets exactly as above. The tests must assert provider execution is never called when scope is weak, ambiguous, unauthorized, or cross-team. The tests must assert read context exposes summary DTOs/source-chip metadata only and does not expose raw prompt/context/provider output fields. The execution-service tests must import `com.smarterd.application.ai.validation.ProviderOutputValidator` and use the existing `AiProviderResult` contract without expecting a global provider schema replacement.</action>
  <acceptance_criteria>
    - The backend skeletons and four backend test files compile immediately after this task.
    - Test method names map to the task IDs in 10-VALIDATION.md: 10-W0-01, 10-W0-02, 10-W0-03, and 10-W0-08.
    - The targeted backend command reaches behavioral RED assertion failures or passes; it does not fail because `AiChat*` classes, DTO records, or `ProviderOutputValidator` imports are missing.
    - No test expects server chat history, action proposals, preview/diff, approval flow, write execution, delete/destructive actions, hosted provider support, or provider credential management.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiChatContext*" --tests "*AiReadContext*" --tests "*AiChatExecutionService*" --tests "*AiChatController*"</automated>
  </verify>
  <done>Backend Wave 0 tests exist; the targeted command reaches RED implementation-missing failures or passes if later code already exists, with no syntax/setup failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create frontend Wave 0 unit tests</name>
  <files>client/src/types/ai-chat.ts, client/src/stores/useAiChatStore.ts, client/src/hooks/useAiRouteContext.ts, client/src/components/ai/AiAnswerCard.tsx, client/src/components/ai/AiSourceChips.tsx, client/test/unit/ai-chat-store.test.ts, client/test/unit/ai-chat-context.test.ts, client/test/unit/ai-chat-response-cards.test.ts</files>
  <read_first>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VALIDATION.md</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-UI-SPEC.md</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md</file>
    <file>client/test/unit/ai-provider-status.test.ts</file>
    <file>client/test/unit/recent-project-context.test.ts</file>
    <file>client/src/constants/storage.ts</file>
    <file>client/src/constants/query-keys.ts</file>
    <file>client/src/hooks/useRecentProjectContext.ts</file>
  </read_first>
  <behavior>
    - `ai-chat-store.test.ts` covers drawer open state, message persistence by authenticated login/user storage key, explicit new conversation reset, logout clearing behavior, most-recent-50 rendered message retention cap, and exclusion of token/cookie/password/env/raw context-shaped fields per D-03 through D-05 and D-29.
    - `ai-chat-context.test.ts` covers route-derived context, manual override, weak-context requirement, current-team multi-project mode, and send-time context snapshot per D-06 through D-13.
    - `ai-chat-response-cards.test.ts` covers conclusion, source chips, confirmed facts, interpretation, needs confirmation, error card state, and absence of action proposal/approval/delete controls per D-23 through D-30.
  </behavior>
  <action>Create minimal frontend production skeletons for `client/src/types/ai-chat.ts`, `client/src/stores/useAiChatStore.ts`, `client/src/hooks/useAiRouteContext.ts`, `client/src/components/ai/AiAnswerCard.tsx`, and `client/src/components/ai/AiSourceChips.tsx` before writing tests that import them. Skeletons must compile, export only the types/functions/components asserted by Wave 0 tests, return safe empty/disabled/noop values, and perform no API/provider/read-tool business logic. Then create Node.js test-runner unit tests using `node:test` and `node:assert/strict`. Target exact modules in the existing compiled `.js` unit-test style. Do not add DOM test libraries or new packages.</action>
  <acceptance_criteria>
    - The three files are included by `cd client && npm run test:unit`.
    - The targeted frontend command reaches behavioral RED assertion failures or passes; it does not fail because target Phase 10 modules are missing.
    - Test assertions explicitly reject persisted keys containing `accessToken`, `refreshToken`, `cookie`, `password`, `rawPrompt`, `rawContext`, `rawProviderOutput`, `env`, or `toolPayload`.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit</automated>
  </verify>
  <done>Frontend Wave 0 unit tests exist and are discoverable by the existing Node test pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create drawer Playwright smoke</name>
  <files>client/e2e/smoke/ai-chat-drawer.spec.ts</files>
  <read_first>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VALIDATION.md</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-UI-SPEC.md</file>
    <file>client/e2e/smoke/diagram-loading.spec.ts</file>
    <file>client/e2e/shared/diagram-e2e.ts</file>
    <file>client/src/components/ai/AiProviderStatusBadge.tsx</file>
  </read_first>
  <action>Create `client/e2e/smoke/ai-chat-drawer.spec.ts` that logs in through existing helpers, opens the global AI drawer by accessible label `AI에게 질문`, verifies drawer title `AI 업무 질문`, changes route while the drawer remains open, submits one localized question against deterministic network setup, and asserts a structured answer or localized error card is visible. Default deterministic setup is Playwright route interception for `/api/ai/provider/status` and `/api/ai/chat` returning a fixed available status plus source-backed chat response; a real/noop provider path may be a separate optional test branch but must not be required for the smoke to pass. The smoke must not depend on server chat history or write/action UI. Use existing Playwright config and helper conventions.</action>
  <acceptance_criteria>
    - The spec contains checks for authenticated-only visibility, route-change persistence, context bar visibility, send lifecycle, and answer/error card visibility.
    - The spec defines deterministic provider/chat setup through route mocks or an explicit noop expected-error mode before submitting the question.
    - The spec uses semantic locators first and adds passive `data-testid` expectations only if the implementation later needs them.
    - The spec does not inspect browser localStorage values that could expose secrets in traces.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx playwright test client/e2e/smoke/ai-chat-drawer.spec.ts</automated>
  </verify>
  <done>Drawer smoke spec exists and is ready for the final Wave 4 implementation to make it pass when the local app is running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures to production contracts | Test data must encode security requirements without normalizing unsafe behavior. |
| Browser local test state to persistent storage | Store tests must prove no secret/raw context persistence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-10-01-01 | Information Disclosure | High | Wave 0 tests | mitigate | Tests must fail if local storage or response contracts include token/cookie/password/env/raw provider fields. | `cd client && npm run test:unit` |
| T-10-01-02 | Tampering | High | Backend test contracts | mitigate | Tests must assert provider/read tools do not run until context is authorized and unambiguous. | `./gradlew test --tests "*AiChat*" --tests "*AiReadContext*"` |
| T-10-01-03 | Elevation of Privilege | High | Chat response tests | mitigate | Tests must assert action proposal, approval, write execution, and destructive controls are absent in Phase 10. | `cd client && npm run test:unit` |
| T-10-01-SC | Tampering | Low | Package installs | accept | Phase 10 installs no packages; package legitimacy audit records none new. | `rg "npm install|gradle .*implementation" .planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-01-PLAN.md` |
</threat_model>

## Verification

- Targeted backend RED/GREEN contract: `./gradlew test --tests "*AiChatContext*" --tests "*AiReadContext*" --tests "*AiChatController*"`.
- Targeted frontend RED/GREEN contract: `cd client && npm run test:unit`.
- E2E smoke contract: `cd client && npx playwright test client/e2e/smoke/ai-chat-drawer.spec.ts`.

<success_criteria>
Wave 0 is complete when all seven validation files exist, encode the Phase 10 locked decisions, and fail only for missing implementation or pass after implementation.
</success_criteria>

<output>
Create `.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-01-SUMMARY.md` when done.
</output>
