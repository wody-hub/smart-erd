---
phase: 10-app-ai-chat-ui-read-only-context-tools
plan: 07
type: execute
wave: 4
depends_on:
  - 10-03
  - 10-06
files_modified:
  - client/src/components/ai/AiChatDrawer.tsx
  - client/src/components/ai/AiChatTrigger.tsx
  - client/src/App.tsx
  - client/src/components/layout/Header.tsx
autonomous: true
requirements:
  - AI-CHAT-01
  - AI-CHAT-02
  - AI-READ-01
  - AI-READ-02
  - AI-READ-03
  - AI-READ-04
user_setup: []
must_haves:
  truths:
    - "Logged-in users can open a global right-side AI drawer from every authenticated app screen."
    - "Drawer open state and messages survive route changes and browser refresh."
    - "Users can send a project-management question and receive a structured source-backed answer or confirmation/error card."
    - "Frontend never calls Codex, Electron IPC, provider runtime, or AI read tools directly."
  artifacts:
    - path: "client/src/components/ai/AiChatDrawer.tsx"
      provides: "Global right-side drawer host"
    - path: "client/src/components/ai/AiChatTrigger.tsx"
      provides: "Header utility trigger"
    - path: "client/src/App.tsx"
      provides: "Authenticated global drawer mounting"
    - path: "client/src/components/layout/Header.tsx"
      provides: "Shared header trigger placement"
  key_links:
    - from: "Header"
      to: "AiChatTrigger"
      via: "authenticated utility rail"
      pattern: "AiChatTrigger"
    - from: "AiChatDrawer"
      to: "useAiChatStore/useAiRouteContext/useAiChatExecution"
      via: "global drawer composition"
      pattern: "(useAiChatStore|useAiRouteContext|useAiChatExecution)"
    - from: "AiChatDrawer"
      to: "/api/ai/chat"
      via: "useAiChatExecution -> aiChatApi"
      pattern: "executeAiChat"
---

<objective>
Mount the final global AI chat drawer and verify the end-to-end read-only chat experience.

Purpose: This closes Phase 10 by making the authenticated drawer available across routes and wired to the typed backend chat API.
Output: Drawer shell, header trigger, app mounting, and final automated verification.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-CONTEXT.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-UI-SPEC.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md
@client/src/App.tsx
@client/src/components/layout/Header.tsx
@client/src/components/ui/dialog.tsx
@client/src/components/ai/AiProviderStatusBadge.tsx
@client/src/components/ai/AiChatContextBar.tsx
@client/src/components/ai/AiChatComposer.tsx
@client/src/components/ai/AiAnswerCard.tsx
@client/src/hooks/useAiChatExecution.ts
@client/src/hooks/useAiRouteContext.ts
@client/src/stores/useAiChatStore.ts
</context>

## Source Audit

This plan covers the full Phase 10 GOAL and all requirement IDs through the final integrated UI/API path. It implements CONTEXT decisions D-01 through D-06, D-13, D-14, D-23 through D-30, and UI-SPEC global drawer, persistence, lifecycle, accessibility, and local storage constraints. Deferred Phase 11/12 features remain absent.

## Must Haves

- Drawer renders only after authentication and does not render on `/login`, `/signup`, unauthenticated guide/setup states, or public routes.
- Trigger is in the shared header utility rail and provider status badge remains status-only.
- Drawer close hides without clearing messages; `새 대화` is the explicit reset.
- Transcript and context bar are fixed/scrollable as specified, with no overlapping text on mobile/desktop.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build global drawer shell and trigger</name>
  <files>client/src/components/ai/AiChatDrawer.tsx, client/src/components/ai/AiChatTrigger.tsx</files>
  <read_first>
    <file>client/e2e/smoke/ai-chat-drawer.spec.ts</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-UI-SPEC.md</file>
    <file>client/src/components/ui/dialog.tsx</file>
    <file>client/src/components/layout/Header.tsx</file>
    <file>client/src/components/ai/AiProviderStatusBadge.tsx</file>
    <file>client/src/components/ai/AiChatContextBar.tsx</file>
    <file>client/src/components/ai/AiChatComposer.tsx</file>
    <file>client/src/components/ai/AiAnswerCard.tsx</file>
  </read_first>
  <behavior>
    - `AiChatTrigger` opens the drawer and uses localized visible/ARIA copy.
    - `AiChatDrawer` uses Radix Dialog-compatible primitives as a right-side sheet with accessible title, close, new conversation, context bar, transcript, and composer.
    - Drawer close does not reset messages or selected context.
    - New conversation asks local-only confirmation and clears only browser-local conversation state.
  </behavior>
  <action>Create `AiChatDrawer` and `AiChatTrigger` in `client/src/components/ai`. Build the drawer from existing `Dialog`/Radix patterns or a local sheet-style composition inside the component; do not add a registry dependency. Compose `AiProviderStatusBadge`, `AiChatContextBar`, `AiAnswerCard`, and `AiChatComposer`. Apply UI-SPEC dimensions: desktop width 520px max 100vw, mobile 100vw, fixed header/context/composer, independently scrolling transcript. Use lucide icons and semantic token classes only.</action>
  <acceptance_criteria>
    - Drawer has accessible title `AI 업무 질문`, trigger label `AI에게 질문`, and keyboard close behavior.
    - `Esc` closes the drawer when no nested context picker/dialog is active.
    - `rg "bg-gray|text-blue|bg-emerald|#[0-9A-Fa-f]" client/src/components/ai/AiChatDrawer.tsx client/src/components/ai/AiChatTrigger.tsx` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit</automated>
  </verify>
  <done>Global drawer shell and trigger exist as reusable authenticated UI components.</done>
</task>

<task type="auto">
  <name>Task 2: Mount drawer in authenticated app shell</name>
  <files>client/src/App.tsx, client/src/components/layout/Header.tsx</files>
  <read_first>
    <file>client/src/App.tsx</file>
    <file>client/src/components/layout/Header.tsx</file>
    <file>client/src/components/auth/ProtectedRoute.tsx</file>
    <file>client/src/stores/useAuthStore.ts</file>
    <file>client/src/components/ai/AiChatDrawer.tsx</file>
    <file>client/src/components/ai/AiChatTrigger.tsx</file>
  </read_first>
  <action>Mount the drawer host high enough inside the router/app shell that it survives route changes. Render it only when the user is authenticated and normal app routes are active. Add `AiChatTrigger` to the shared header utility rail for authenticated screens without replacing or overloading `AiProviderStatusBadge`. Ensure existing `rightSlot` behavior remains intact and page-specific header utilities still render. Do not render drawer/trigger on login, signup, unauthenticated guide-only setup, or settings setup routes before authentication.</action>
  <acceptance_criteria>
    - Header still shows theme/language/settings/logout behavior.
    - Trigger appears on authenticated pages with shared `Header`.
    - Route changes do not unmount/reset `useAiChatStore`.
    - `rg "AiChat" client/src/pages` is not required for global availability; page-local chat panels are not introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <done>The AI drawer is globally available after login and remains route-independent.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 10 verification</name>
  <files>client/src/components/ai/AiChatDrawer.tsx, client/src/App.tsx</files>
  <read_first>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-VALIDATION.md</file>
    <file>client/e2e/smoke/ai-chat-drawer.spec.ts</file>
    <file>README.md</file>
    <file>CLAUDE.md</file>
  </read_first>
  <action>Run the phase gate commands from 10-VALIDATION.md after implementation. If the local app is not already running for Playwright, start the appropriate local/test backend and frontend using existing scripts before running the drawer smoke, then stop only processes started by this task. Inspect frontend AI chat files for forbidden direct provider/runtime access and forbidden secret/raw-context persistence. Do not weaken tests to pass.</action>
  <acceptance_criteria>
    - Backend targeted and full frontend unit/build checks pass.
    - Drawer smoke passes when local E2E environment credentials are available; if credentials are unavailable, record the exact blocker in the summary and still run build/unit/backend checks.
    - Forbidden grep checks show no Codex/Electron/runtime direct calls and no token/cookie/password/raw context storage in frontend chat files.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test && cd client && npm run build && npm run test:unit</automated>
    <automated>cd client && npx playwright test client/e2e/smoke/ai-chat-drawer.spec.ts</automated>
  </verify>
  <done>Phase 10 integrated chat UI/API passes automated gates or records only environment-specific E2E credential blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated app shell to global drawer | Drawer must be available to logged-in users only. |
| Drawer to chat API | UI sends user messages and context hints through typed API hooks only. |
| Local persistence to browser profile | Persisted conversation survives refresh and must stay user-scoped and sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-10-07-01 | Spoofing | High | App shell mount | mitigate | Render drawer/trigger only when authenticated store has an access token. | `cd client && npm run build` and Playwright smoke |
| T-10-07-02 | Information Disclosure | High | Drawer transcript persistence | mitigate | Use `useAiChatStore` sanitized login-scoped state; do not inspect/store raw provider payloads. | `cd client && npm run test:unit` |
| T-10-07-03 | Elevation of Privilege | High | Global drawer UI | mitigate | No write/action/approval/destructive UI and no direct Codex/Electron/provider calls. | `rg "codex|electron|ipc|proposal|approval|diff|delete" client/src/components/ai client/src/App.tsx client/src/components/layout/Header.tsx` |
| T-10-07-04 | Denial of Service | Medium | Send lifecycle | mitigate | Disable concurrent sends and expose cancel-running without clearing transcript. | `cd client && npm run test:unit` |
| T-10-07-SC | Tampering | Low | Package installs | accept | No package-manager install is planned. | `git diff -- client/package.json package-lock.json` |
</threat_model>

## Verification

- `./gradlew test && cd client && npm run build && npm run test:unit`.
- `cd client && npx playwright test client/e2e/smoke/ai-chat-drawer.spec.ts`.
- `rg "codex|electron|ipc" client/src/api client/src/hooks client/src/components/ai`.
- `rg "accessToken|refreshToken|cookie|password|rawPrompt|rawContext|rawProviderOutput|SMART_ERD|SPRING_" client/src/types/ai-chat.ts client/src/stores/useAiChatStore.ts client/src/components/ai`.

<success_criteria>
Plan 10-07 is complete when the authenticated global drawer is mounted, survives route/refresh, sends through Spring HTTP only, and renders read-only structured answers with source chips.
</success_criteria>

<output>
Create `.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-07-SUMMARY.md` when done.
</output>
