---
phase: 10-app-ai-chat-ui-read-only-context-tools
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - src/main/java/com/smarterd/api/ai/AiChatController.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatSourceChipResponse.java
  - src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
autonomous: true
requirements:
  - AI-CHAT-01
  - AI-CHAT-02
  - AI-READ-01
  - AI-READ-02
  - AI-READ-03
  - AI-READ-04
user_setup: []
must_haves:
  truths:
    - "Authenticated clients can call a chat-specific Spring HTTP endpoint."
    - "The endpoint returns either confirmation-needed data or a read-only structured answer with source chips."
    - "Provider invocation happens only after resolved scope and read context assembly."
    - "Action proposals and write execution are rejected or omitted from Phase 10 chat responses."
  artifacts:
    - path: "src/main/java/com/smarterd/api/ai/AiChatController.java"
      provides: "Thin authenticated `/api/ai/chat` HTTP boundary"
      exports: ["POST /api/ai/chat"]
    - path: "src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java"
      provides: "Chat orchestration through resolver, read tools, provider gateway, and response assembler"
    - path: "src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java"
      provides: "Read-only structured answer/confirmation contract"
  key_links:
    - from: "AiChatController"
      to: "AiChatExecutionService"
      via: "jwt subject + validated DTO"
      pattern: "aiChatExecutionService\\.execute\\(jwt\\.getSubject"
    - from: "AiChatExecutionService"
      to: "AiReadContextService"
      via: "read context before provider request"
      pattern: "aiReadContextService"
    - from: "AiChatExecutionService"
      to: "AiExecutionGateway or AiProvider"
      via: "Phase 9 provider boundary only"
      pattern: "(aiExecutionGateway|AiProvider)"
---

<objective>
Create the backend chat HTTP/API orchestration layer that turns server-read facts into a Phase 10 answer contract.

Purpose: Frontend chat must call a typed Spring API while backend owns scope, tool context, provider invocation, source chips, and read-only response shaping.
Output: `/api/ai/chat` controller, request/response DTOs, and `AiChatExecutionService`.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-CONTEXT.md
@.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-PATTERNS.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-01-SUMMARY.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-02-SUMMARY.md
@src/main/java/com/smarterd/api/ai/AiProviderController.java
@src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteRequest.java
@src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteResponse.java
@src/main/java/com/smarterd/application/ai/AiExecutionGateway.java
@src/main/java/com/smarterd/application/ai/provider/AiProviderResult.java
</context>

## Source Audit

This plan covers GOAL chat ask/answer grounding, REQ AI-CHAT-01 and all AI-READ requirements through the chat endpoint, RESEARCH guidance to avoid direct provider/frontend coupling, and CONTEXT decisions D-15, D-23 through D-30. It explicitly excludes server chat history, action proposal UX, approval, and write execution per D-30.

## Must Haves

- `POST /api/ai/chat` accepts authenticated requests only.
- Confirmation-needed responses do not invoke the provider.
- Successful responses include conclusion, confirmed facts, interpretation text, needs-confirmation items, source chips, execution metadata, and status.
- Non-empty provider action drafts are not returned to the chat UI and cannot create approval/write UI in Phase 10.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define chat HTTP DTO contract</name>
  <files>src/main/java/com/smarterd/api/ai/dto/AiChatRequest.java, src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java, src/main/java/com/smarterd/api/ai/dto/AiChatSourceChipResponse.java</files>
  <read_first>
    <file>src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</file>
    <file>.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-CONTEXT.md</file>
    <file>src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteRequest.java</file>
    <file>src/main/java/com/smarterd/api/ai/dto/AiProviderExecuteResponse.java</file>
    <file>src/main/java/com/smarterd/api/ai/dto/AiSelectedResourceRequest.java</file>
  </read_first>
  <behavior>
    - Request carries `message`, optional `teamId`, optional `projectId`, optional named project text, `scopeMode`, `locale`, and optional selected resource.
    - Response carries `status`, `executionId`, `requiresConfirmation`, `confirmationReason`, `context`, `sourceChips`, `conclusion`, `confirmedFacts`, `interpretation`, `needsConfirmation`, and `error`.
    - Response contains no `actions`, `proposal`, `diff`, `approval`, or write execution fields.
  </behavior>
  <action>Create Java record DTOs under `com.smarterd.api.ai.dto` with Bean Validation message keys. Keep `message` bounded to the existing provider message maximum or stricter. Add nested records or separate records for context scope, confirmation candidates, answer sections, source chips, and errors as needed. DTOs map to/from application service records; the API DTO contract must be read-only and not expose raw provider output.</action>
  <acceptance_criteria>
    - DTO fields match frontend contract names planned for `client/src/types/ai-chat.ts`.
    - Bean Validation rejects blank messages and invalid oversized input.
    - `rg "action|proposal|approval|diff|executeWrite|delete" src/main/java/com/smarterd/api/ai/dto/AiChat*.java` shows no write-control fields except comments/test-prohibited strings if any.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiChatController*"</automated>
  </verify>
  <done>Chat DTOs provide a stable read-only request/response contract for frontend Phase 10 integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chat execution orchestration</name>
  <files>src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java</files>
  <read_first>
    <file>src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</file>
    <file>src/main/java/com/smarterd/application/ai/chat/AiChatContextResolver.java</file>
    <file>src/main/java/com/smarterd/application/ai/chat/AiReadContextService.java</file>
    <file>src/main/java/com/smarterd/application/ai/AiExecutionGateway.java</file>
    <file>src/main/java/com/smarterd/application/ai/provider/AiProviderResult.java</file>
  </read_first>
  <behavior>
    - Resolve scope first; if confirmation is required, return a confirmation response and do not call provider.
    - Build read context second; if no usable facts are available, return a needs-confirmation/read-empty response rather than fabricating facts.
    - Invoke the Phase 9 provider boundary with sanitized summary context only.
    - Assemble answer sections so `confirmedFacts` are server-read facts and `interpretation` is provider answer text.
    - If provider result contains action drafts, return a safe read-only error or ignore them with a validation marker; do not expose them to the API response.
  </behavior>
  <action>Create `AiChatExecutionService` in `com.smarterd.application.ai.chat`. Use the existing `AiExecutionGateway` if its public contract can accept sanitized read context without weakening Phase 9; otherwise inject `AiProvider`, `AiExecutionRegistry`, and `ProviderOutputValidator` using the same authorization/validation/audit patterns as `AiExecutionGateway`. Preserve Phase 9 timeout, cancel, registry, and audit behavior. Do not bypass output validation. Do not store prompt, raw context, raw model output, credentials, or tool payloads.</action>
  <acceptance_criteria>
    - `AiChatControllerMvcTest` denial and confirmation cases prove provider is not invoked.
    - Successful case proves read context source chips are returned from server context, not provider text.
    - Provider failure returns HTTP 200 structured failed response when the provider failed safely, while auth/validation failures use existing HTTP error handling.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiChatController*" --tests "*AiReadContext*" --tests "com.smarterd.application.ai.*"</automated>
  </verify>
  <done>Chat orchestration enforces scope/read/provider ordering and returns read-only structured answers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add authenticated chat controller</name>
  <files>src/main/java/com/smarterd/api/ai/AiChatController.java</files>
  <read_first>
    <file>src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</file>
    <file>src/main/java/com/smarterd/api/ai/AiProviderController.java</file>
    <file>src/main/java/com/smarterd/api/common/GlobalExceptionHandler.java</file>
  </read_first>
  <action>Create `AiChatController` with `@RestController`, `@RequestMapping("/api/ai/chat")`, `@RequiredArgsConstructor`, and `POST /` or `POST ""` handling. Accept `@AuthenticationPrincipal Jwt`, `@Valid @RequestBody AiChatRequest`, call `aiChatExecutionService.execute(jwt.getSubject(), request.toCommand())`, and return `AiChatResponse.from(...)`. Keep the controller thin; no read-tool selection, no provider calls, no Electron/Codex logic, and no persistence logic in the controller.</action>
  <acceptance_criteria>
    - `POST /api/ai/chat` requires authentication in the MockMvc test.
    - Controller delegates once to `AiChatExecutionService`.
    - Controller does not reference PM domain services, repositories, `AiProvider`, Codex process classes, Electron, or local storage concepts.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiChatController*"</automated>
  </verify>
  <done>`/api/ai/chat` is available as a typed authenticated Spring HTTP endpoint for the frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend to `/api/ai/chat` | User-controlled prompt, context IDs, and locale enter backend. |
| Chat service to provider boundary | Sanitized summaries cross from Smart-ERD into AI provider execution. |
| Provider result to frontend response | Model output becomes rendered interpretation only after validation and action rejection. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-10-03-01 | Spoofing | High | `AiChatController` | mitigate | Use `@AuthenticationPrincipal Jwt` and no anonymous path. | `./gradlew test --tests "*AiChatController*"` |
| T-10-03-02 | Information Disclosure | High | `AiChatExecutionService` | mitigate | Provider receives sanitized read summaries only after resolver and read service succeed. | `./gradlew test --tests "*AiChatController*" --tests "*AiReadContext*"` |
| T-10-03-03 | Tampering | High | Provider result handling | mitigate | Keep `ProviderOutputValidator` authoritative and reject/omit non-empty actions from chat response. | `./gradlew test --tests "com.smarterd.application.ai.*" --tests "*AiChatController*"` |
| T-10-03-04 | Elevation of Privilege | High | Chat API contract | mitigate | No action proposal, preview/diff, approval, delete, or write execution fields in DTOs. | `rg "proposal|approval|diff|delete|executeWrite" src/main/java/com/smarterd/api/ai/dto/AiChat*.java` |
| T-10-03-SC | Tampering | Low | Package installs | accept | No package-manager install is planned. | `rg "npm install|implementation " .planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-03-PLAN.md` |
</threat_model>

## Verification

- `./gradlew test --tests "*AiChatController*" --tests "*AiReadContext*"`.
- `./gradlew test --tests "com.smarterd.application.ai.*" --tests "com.smarterd.api.ai.*"`.
- `rg "codex|ProcessBuilder|electron|ipc" src/main/java/com/smarterd/api/ai src/main/java/com/smarterd/application/ai/chat`.

<success_criteria>
Plan 10-03 is complete when authenticated chat requests return confirmation or structured read-only answers, and provider execution never happens before server scope/read authorization.
</success_criteria>

<output>
Create `.planning/phases/10-app-ai-chat-ui-read-only-context-tools/10-03-SUMMARY.md` when done.
</output>
