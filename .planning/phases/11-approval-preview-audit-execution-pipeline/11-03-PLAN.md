---
phase: 11-approval-preview-audit-execution-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/main/java/com/smarterd/application/ai/history/AiProjectHistoryService.java
  - src/main/java/com/smarterd/api/ai/AiProjectHistoryController.java
  - src/main/java/com/smarterd/api/ai/dto/AiProjectHistoryResponse.java
  - src/main/java/com/smarterd/domain/ai/AiActionProposalRepository.java
  - src/main/java/com/smarterd/domain/ai/AiExecutionAuditRepository.java
  - src/test/java/com/smarterd/application/ai/history/AiProjectHistoryServiceTest.java
  - src/test/java/com/smarterd/api/ai/AiProjectHistoryControllerMvcTest.java
autonomous: true
requirements: [AI-AUD-01, AI-AUD-02, AI-AUD-03]
user_setup: []
must_haves:
  truths:
    - "D-20, D-21, D-22, D-23, D-24, and D-25: project AI history lists sanitized execution/proposal/decision metadata only and follows project authorization plus personal TODO privacy."
    - "D-13 and D-14: history rows link persisted proposal ids to execution ids and the originating project context."
    - "D-04: normal users never see raw provider JSON, raw payload, raw prompt, raw context, or provider stdout/stderr through history."
  artifacts:
    - path: "src/main/java/com/smarterd/application/ai/history/AiProjectHistoryService.java"
      provides: "Project-scoped sanitized AI history query"
    - path: "src/main/java/com/smarterd/api/ai/AiProjectHistoryController.java"
      provides: "Read-only project AI history endpoint"
  key_links:
    - from: "AiProjectHistoryService"
      to: "ProjectContextLoader"
      via: "project authorization before history read"
      pattern: "load"
---

<objective>
Add backend project AI history for redacted executions, proposals, decisions, and outcomes.

Purpose: Project users need a read-only trace of AI proposal activity without exposing prompts, raw provider output, or private TODO details.
Output: project-scoped history service, API, DTOs, repository queries, and privacy tests.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-CONTEXT.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-RESEARCH.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-VALIDATION.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-PATTERNS.md
@src/main/java/com/smarterd/domain/pm/common/ProjectContextLoader.java
@src/main/java/com/smarterd/domain/ai/AiExecutionAudit.java
@src/main/java/com/smarterd/domain/ai/AiActionProposal.java
</context>

## Source Audit

This plan covers AI-AUD-01, AI-AUD-02, and AI-AUD-03 for backend history. It depends on Plan 11-01 proposal persistence. Frontend history rendering is Plan 11-05.

## Decision Traceability

- D-20 through D-22 are covered by redacted history DTOs and audit/proposal query shaping.
- D-23 is covered by `GET /api/teams/{teamId}/projects/{projectId}/ai-history`.
- D-24 is covered by personal TODO proposal detail filtering.
- D-25 is covered by status-specific history row types for cancel, reject, execute, and fail.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sanitized project AI history service</name>
  <files>src/main/java/com/smarterd/application/ai/history/AiProjectHistoryService.java, src/main/java/com/smarterd/domain/ai/AiActionProposalRepository.java, src/main/java/com/smarterd/domain/ai/AiExecutionAuditRepository.java, src/test/java/com/smarterd/application/ai/history/AiProjectHistoryServiceTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/domain/pm/common/ProjectContextLoader.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiActionProposalRepository.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiExecutionAuditRepository.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiActionProposal.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiExecutionAudit.java</file>
  </read_first>
  <action>Create `AiProjectHistoryService` with `getProjectHistory(loginId, teamId, projectId, limit)` that first calls `ProjectContextLoader.load(loginId, teamId, projectId, false)`. Query recent `AiActionProposal` and `AiExecutionAudit` rows for the project, sort by newest created/decided timestamp, cap default limit to 50 and max to 100, and map to row views with `kind`, `executionId`, `proposalId`, `provider`, `promptVersion`, `actionType`, `riskLevel`, `status`, `targetType`, `targetId`, `targetLabel`, `summary`, `requestedBy`, `decisionBy`, `createdAt`, `decidedAt`, and redacted error fields. For action types or target types that include `todo` and represent personal/private TODO detail, expose only owner/requester-safe summary unless `loginId` equals requester/owner or the target is intentionally linked to project-visible WBS context.</action>
  <acceptance_criteria>
    - Service tests prove unauthorized project access fails before repository history queries.
    - History rows contain proposal/execution metadata and never contain sanitized payload JSON, raw prompt, raw context, provider output, stdout, stderr, tokens, cookies, env, or local paths.
    - Personal TODO proposal detail is hidden from a different project member unless the row has a project-visible WBS link marker.
    - `limit` over 100 is reduced to 100.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiProjectHistoryService*"</automated>
  </verify>
  <done>Project history service returns only authorized, redacted AI rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose read-only project AI history API</name>
  <files>src/main/java/com/smarterd/api/ai/AiProjectHistoryController.java, src/main/java/com/smarterd/api/ai/dto/AiProjectHistoryResponse.java, src/test/java/com/smarterd/api/ai/AiProjectHistoryControllerMvcTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/api/project/ProjectIssueController.java</file>
    <file>src/main/java/com/smarterd/api/project/ProjectTodoController.java</file>
    <file>src/main/java/com/smarterd/api/ai/AiActionProposalController.java</file>
    <file>src/main/java/com/smarterd/application/ai/history/AiProjectHistoryService.java</file>
    <file>src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</file>
  </read_first>
  <action>Create `AiProjectHistoryController` with `GET /api/teams/{teamId}/projects/{projectId}/ai-history?limit=50`. Require `@AuthenticationPrincipal Jwt`; delegate to `AiProjectHistoryService`; return `AiProjectHistoryResponse` with `items`, `limit`, and `hasMore`. DTO item fields must mirror the sanitized service view. Add MVC tests for anonymous denial, authorized list, limit parameter, project authorization failure mapping, and raw-key absence in serialized JSON.</action>
  <acceptance_criteria>
    - Endpoint path is exactly `/api/teams/{teamId}/projects/{projectId}/ai-history`.
    - Controller has no write methods and no approve/cancel mutation logic.
    - MVC JSON contains `items[0].executionId`, `items[0].proposalId`, `items[0].status`, and `items[0].summary`.
    - MVC JSON does not contain `payload`, `prompt`, `context`, `stdout`, `stderr`, `token`, `cookie`, or `env`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiProjectHistoryController*"</automated>
  </verify>
  <done>Project AI history is available as a read-only authorized API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project route to history query | Path IDs are user-controlled and must be authorized before reading AI rows. |
| Proposal/audit storage to history DTO | Internal persisted JSON and errors must be reduced to sanitized metadata. |
| Personal TODO proposal to project history | Private owner-scoped TODO details need extra visibility filtering. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-11-03-01 | Information Disclosure | High | History service | mitigate | Load project authorization before repository reads and whitelist history fields. | `./gradlew test --tests "*AiProjectHistoryService*"` |
| T-11-03-02 | Information Disclosure | High | Personal TODO rows | mitigate | Hide TODO proposal detail from non-owner/non-requester unless linked to project-visible WBS context. | `./gradlew test --tests "*AiProjectHistoryService*"` |
| T-11-03-03 | Repudiation | Medium | History rows | mitigate | Include requester, decision actor, status, execution id, proposal id, and timestamps. | `./gradlew test --tests "*AiProjectHistoryController*"` |
| T-11-03-SC | Tampering | Low | Package installs | accept | No dependency changes are planned. | `git diff -- build.gradle client/package.json` shows no package changes |
</threat_model>

<verification>
- `./gradlew test --tests "*AiProjectHistory*" --tests "*Ai*Audit*"`
- `rg "payload|rawPrompt|rawContext|rawProviderOutput|stdout|stderr|accessToken|refreshToken|cookie|password|SMART_ERD_|SPRING_|env" src/main/java/com/smarterd/api/ai src/main/java/com/smarterd/application/ai/history`
</verification>

<success_criteria>
Plan 11-03 is complete when project-scoped AI history is authorized, read-only, redacted, capped, and privacy-safe for personal TODO proposal detail.
</success_criteria>

<output>
Create `.planning/phases/11-approval-preview-audit-execution-pipeline/11-03-SUMMARY.md` when done.
</output>
