---
phase: 11-approval-preview-audit-execution-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - client/src/types/ai-chat.ts
  - client/src/api/aiChatApi.ts
  - client/src/constants/query-keys.ts
  - client/src/hooks/useAiChatExecution.ts
  - client/src/stores/useAiChatStore.ts
  - client/src/components/ai/AiAnswerCard.tsx
  - client/src/components/ai/AiProposalPanel.tsx
  - client/src/components/ai/AiProposalPreview.tsx
  - client/src/components/ai/AiProposalStatusBadge.tsx
  - client/src/locales/ko/translation.json
  - client/src/locales/en/translation.json
  - client/test/unit/ai-chat-response-cards.test.ts
  - client/test/unit/ai-chat-store.test.ts
  - client/test/unit/ai-chat-api.test.ts
  - client/test/unit/ai-chat-execution.test.ts
autonomous: true
requirements: [AI-ACT-01, AI-APP-01, AI-APP-02, AI-APP-03, AI-AUD-02]
user_setup: []
must_haves:
  truths:
    - "D-01, D-02, D-03, D-04, D-05, and D-06: proposal cards render inside the originating answer, one card per proposal, while preserving Phase 10 answer sections."
    - "D-07 through D-12: frontend renders server-provided typed previews only and never computes authoritative diffs from provider payload."
    - "D-14 through D-19: approve/cancel mutations use proposal id, update the original card in place, and preserve terminal status across route changes."
    - "D-20 and D-25: browser storage contains sanitized proposal card state only."
  artifacts:
    - path: "client/src/components/ai/AiProposalPanel.tsx"
      provides: "Approval card container inside answer"
    - path: "client/src/stores/useAiChatStore.ts"
      provides: "Sanitized local proposal state and in-place terminal updates"
    - path: "client/src/api/aiChatApi.ts"
      provides: "Approve/cancel/refresh proposal API calls"
  key_links:
    - from: "AiAnswerCard"
      to: "AiProposalPanel"
      via: "render proposals below existing answer sections"
      pattern: "proposals"
---

<objective>
Implement frontend proposal cards, approve/cancel mutations, and sanitized drawer persistence.

Purpose: Users must see, approve, cancel, and retain each proposal card in the originating AI answer.
Output: typed proposal models, proposal UI components, API helpers, store updates, and unit tests.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-CONTEXT.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-RESEARCH.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-VALIDATION.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-PATTERNS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-UI-SPEC.md
@client/src/types/ai-chat.ts
@client/src/components/ai/AiAnswerCard.tsx
@client/src/stores/useAiChatStore.ts
@client/src/hooks/useAiChatExecution.ts
@client/src/api/aiChatApi.ts
</context>

## Source Audit

This plan covers frontend AI-ACT-01, AI-APP-01, AI-APP-02, AI-APP-03, and AI-AUD-02. It depends on Plan 11-02 backend proposal response and decision APIs. It does not add project history tab UI; that is Plan 11-05.

## Decision Traceability

- D-01 through D-06 are covered by proposal rendering in `AiAnswerCard`.
- D-07 through D-12 are covered by typed preview components that render response data only.
- D-14 through D-19 are covered by approve/cancel API helpers, mutation normalization, and in-place store updates.
- D-20 and D-25 are covered by local-storage sanitizer tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend proposal types and API helpers</name>
  <files>client/src/types/ai-chat.ts, client/src/api/aiChatApi.ts, client/src/constants/query-keys.ts, client/test/unit/ai-chat-api.test.ts</files>
  <read_first>
    <file>client/src/types/ai-chat.ts</file>
    <file>client/src/api/aiChatApi.ts</file>
    <file>client/src/api/aiProviderApi.ts</file>
    <file>client/src/constants/query-keys.ts</file>
    <file>client/test/unit/ai-chat-api.test.ts</file>
  </read_first>
  <action>Add proposal types in `client/src/types/ai-chat.ts`: `AiProposalStatus` with `PENDING`, `CANCELLED`, `EXPIRED`, `REJECTED`, `EXECUTED`, `FAILED`; `AiProposalRiskLevel`; `AiProposalTarget`; `AiProposalPreviewField`; `AiActionProposalCard`; and `AiActionProposalDecisionResponse`. Extend `AiChatResponse` with `proposals: AiActionProposalCard[]`. In `aiChatApi.ts`, add `fetchAiProposal(proposalId)`, `approveAiProposal(proposalId)`, and `cancelAiProposal(proposalId)` using base paths `/ai/proposals/{proposalId}`, `/ai/proposals/{proposalId}/approve`, and `/ai/proposals/{proposalId}/cancel`. Add query keys `aiChat.proposal(proposalId)`, `aiChat.proposalApprove(proposalId)`, and `aiChat.proposalCancel(proposalId)`.</action>
  <acceptance_criteria>
    - Unit tests prove API helpers call the exact paths listed in the action.
    - Type definitions do not include `payload`, `rawPrompt`, `rawContext`, `rawProviderOutput`, `stdout`, `stderr`, `token`, `cookie`, or `env` fields.
    - `AiChatResponse` requires `proposals` in normalized responses and defaults to an empty array.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit -- ai-chat-api</automated>
    <automated>rg "payload|rawPrompt|rawContext|rawProviderOutput|stdout|stderr|accessToken|refreshToken|cookie|password|SMART_ERD_|SPRING_|env" client/src/types/ai-chat.ts client/src/api/aiChatApi.ts</automated>
  </verify>
  <done>Frontend has sanitized proposal types and decision API helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render proposal panels inside AI answers</name>
  <files>client/src/components/ai/AiAnswerCard.tsx, client/src/components/ai/AiProposalPanel.tsx, client/src/components/ai/AiProposalPreview.tsx, client/src/components/ai/AiProposalStatusBadge.tsx, client/src/locales/ko/translation.json, client/src/locales/en/translation.json, client/test/unit/ai-chat-response-cards.test.ts</files>
  <read_first>
    <file>.planning/phases/11-approval-preview-audit-execution-pipeline/11-UI-SPEC.md</file>
    <file>client/src/components/ai/AiAnswerCard.tsx</file>
    <file>client/src/components/ai/AiSourceChips.tsx</file>
    <file>client/src/components/ui/button.tsx</file>
    <file>client/src/components/ui/badge.tsx</file>
    <file>client/test/unit/ai-chat-response-cards.test.ts</file>
  </read_first>
  <action>Create `AiProposalStatusBadge`, `AiProposalPreview`, and `AiProposalPanel`. Render `AiProposalPanel` in `AiAnswerCard` after existing conclusion/source/facts/interpretation/needs-confirmation sections. Follow `11-UI-SPEC.md`: no nested `Card` inside `AiAnswerCard`, use a bordered `<section>`, lucide icons for approve/cancel/status, show summary, target, changed fields, content, warnings, risk, expiry, and status; hide approve/cancel buttons when status is not `PENDING` or `executable` is false. Add localized strings under `aiChat.proposals.*` for status labels, approve, cancel, unsupported, expired, rejected, executed, failed, and no-preview warning.</action>
  <acceptance_criteria>
    - Response card tests prove multiple proposals render as independent panels with distinct proposal ids.
    - Tests prove summary, target label, risk, field before/after labels, content, warnings, and terminal status are visible.
    - Tests prove raw JSON keys such as `payload`, `sanitizedPayloadJson`, `rawPrompt`, and `stdout` are not rendered.
    - Tests prove existing Phase 10 answer sections still render when proposals are present.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit -- ai-chat-response-cards</automated>
  </verify>
  <done>Proposal cards are visible inside the originating AI answer without exposing raw payload data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize decisions and persist sanitized card state</name>
  <files>client/src/hooks/useAiChatExecution.ts, client/src/stores/useAiChatStore.ts, client/test/unit/ai-chat-store.test.ts, client/test/unit/ai-chat-execution.test.ts</files>
  <read_first>
    <file>client/src/hooks/useAiChatExecution.ts</file>
    <file>client/src/stores/useAiChatStore.ts</file>
    <file>client/src/constants/storage.ts</file>
    <file>client/test/unit/ai-chat-store.test.ts</file>
    <file>client/test/unit/ai-chat-execution.test.ts</file>
  </read_first>
  <action>Extend `normalizeAiChatResponse` to normalize `proposals` and all nested preview fields. Add store action `updateProposalInMessage(messageId, proposal)` or equivalent that replaces a proposal by `proposalId` inside the original assistant message response and persists the sanitized conversation. Add exported pure sanitizer helpers for proposal cards so tests can assert raw-key stripping. Add proposal decision handling in the chat execution hook or a small hook used by `AiProposalPanel`: approve/cancel calls API helper, receives `AiActionProposalDecisionResponse`, and updates the original card in place. Do not persist browser-supplied payload, request body, prompt, context, token, cookie, or env values.</action>
  <acceptance_criteria>
    - Store tests prove route-independent drawer persistence keeps proposal terminal state attached to the original assistant message.
    - Store tests prove local storage strips `payload`, `rawPrompt`, `rawContext`, `rawProviderOutput`, `stdout`, `stderr`, `accessToken`, `refreshToken`, `cookie`, `password`, `env`, and `toolPayload`.
    - Execution tests prove approve/cancel results update a single proposal without appending a new assistant message.
    - Normalization defaults missing `proposals` to `[]`.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npm run test:unit -- ai-chat-store ai-chat-execution</automated>
  </verify>
  <done>Proposal decision state is sanitized, persisted, and updated in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend proposal DTO to UI | UI receives sanitized proposal card fields and must not infer hidden payload data. |
| User click to backend decision | Frontend calls approve/cancel by proposal id only. |
| Proposal card to local storage | Rendered state survives route changes without leaking raw payload or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-11-04-01 | Information Disclosure | High | Proposal card rendering | mitigate | Render typed fields only and test raw-key absence. | `cd client && npm run test:unit -- ai-chat-response-cards` |
| T-11-04-02 | Tampering | High | Decision API helpers | mitigate | Send only proposal id to approve/cancel endpoints; no client payload is accepted. | `cd client && npm run test:unit -- ai-chat-api` |
| T-11-04-03 | Information Disclosure | High | Zustand persistence | mitigate | Sanitize proposal cards before local storage and strip raw/secret keys. | `cd client && npm run test:unit -- ai-chat-store` |
| T-11-04-SC | Tampering | Low | Package installs | accept | Use existing React, React Query, Zustand, shadcn-compatible primitives, and lucide icons only. | `git diff -- client/package.json` shows no package changes |
</threat_model>

<verification>
- `cd client && npm run test:unit -- ai-chat-response-cards ai-chat-store ai-chat-api ai-chat-execution`
- `rg "payload|rawPrompt|rawContext|rawProviderOutput|stdout|stderr|accessToken|refreshToken|cookie|password|SMART_ERD_|SPRING_|env" client/src/types/ai-chat.ts client/src/components/ai client/src/stores/useAiChatStore.ts client/src/hooks/useAiChatExecution.ts client/src/api/aiChatApi.ts`
</verification>

<success_criteria>
Plan 11-04 is complete when proposal cards render inside AI answers, approve/cancel mutations update cards in place, and route-persistent browser state remains sanitized.
</success_criteria>

<output>
Create `.planning/phases/11-approval-preview-audit-execution-pipeline/11-04-SUMMARY.md` when done.
</output>
