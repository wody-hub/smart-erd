---
phase: 11-approval-preview-audit-execution-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
  - src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java
  - src/main/java/com/smarterd/api/ai/dto/AiActionProposalResponse.java
  - src/main/java/com/smarterd/api/ai/dto/AiActionProposalDecisionResponse.java
  - src/main/java/com/smarterd/api/ai/AiActionProposalController.java
  - src/main/java/com/smarterd/domain/common/message/MessageCode.java
  - src/main/resources/i18n/messages.properties
  - src/main/resources/i18n/messages_ko.properties
  - src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java
  - src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java
  - src/test/java/com/smarterd/api/ai/AiActionProposalControllerMvcTest.java
autonomous: true
requirements: [AI-ACT-01, AI-APP-01, AI-APP-02, AI-APP-03, AI-AUD-01, AI-AUD-02]
user_setup: []
must_haves:
  truths:
    - "D-01, D-02, D-03, D-04, D-05, and D-06: chat answers keep existing Phase 10 sections and append sanitized proposal card data under the originating answer."
    - "D-07 through D-12: chat and proposal endpoints return server-generated preview data and block missing, unauthorized, invalid, stale, or unsupported proposals."
    - "D-13 through D-19: approve/cancel operate by persisted proposal id, revalidate server state, and return immutable terminal status without duplicate mutation."
    - "D-20, D-21, D-22, and D-25: all decision, rejection, failure, and cancellation metadata is audit-safe and redacted."
  artifacts:
    - path: "src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java"
      provides: "Provider action drafts converted to persisted proposal views"
    - path: "src/main/java/com/smarterd/api/ai/AiActionProposalController.java"
      provides: "Approve, cancel, and refresh endpoints"
    - path: "src/main/java/com/smarterd/api/ai/dto/AiActionProposalResponse.java"
      provides: "Sanitized proposal DTO for chat cards and refresh"
  key_links:
    - from: "AiChatExecutionService"
      to: "AiActionProposalService"
      via: "replace read-only action rejection branch"
      pattern: "readOnlyActionRejected"
---

<objective>
Wire persisted proposals into backend chat responses and expose approve/cancel proposal APIs.

Purpose: Provider action drafts should become visible, reviewable proposals instead of read-only chat errors.
Output: chat response proposal DTOs, proposal decision endpoints, localized errors, and MVC/service tests.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-CONTEXT.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-RESEARCH.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-VALIDATION.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-PATTERNS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-UI-SPEC.md
@src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java
@src/main/java/com/smarterd/api/ai/AiChatController.java
@src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java
@src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java
</context>

## Source Audit

This plan covers the application/API integration for AI-ACT-01, AI-APP-01, AI-APP-02, AI-APP-03, AI-AUD-01, and AI-AUD-02. It depends on Plan 11-01 proposal persistence and lifecycle services. It does not implement frontend rendering; that is Plan 11-04.

## Decision Traceability

- D-01 through D-06 are covered by extending `AiChatView` and `AiChatResponse` with proposal views while preserving Phase 10 answer sections.
- D-07 through D-12 are covered by only mapping `AiActionProposalView` preview data from the backend.
- D-13 through D-19 are covered by `AiActionProposalController` approve/cancel/get endpoints and service revalidation.
- D-20 through D-22 and D-25 are covered by redacted decision response DTOs and tests that reject raw fields.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert provider actions into chat proposals</name>
  <files>src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java, src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java, src/main/java/com/smarterd/api/ai/dto/AiActionProposalResponse.java, src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java, src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java</file>
    <file>src/main/java/com/smarterd/api/ai/dto/AiChatResponse.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java</file>
    <file>src/main/java/com/smarterd/application/ai/provider/AiProviderResult.java</file>
    <file>src/test/java/com/smarterd/application/ai/chat/AiChatExecutionServiceTest.java</file>
    <file>src/test/java/com/smarterd/api/ai/AiChatControllerMvcTest.java</file>
  </read_first>
  <action>Inject `AiActionProposalService` into `AiChatExecutionService`. Replace the `readOnlyActionRejected(...)` branch so non-empty `execution.actions()` calls `createProposals(...)` with `executionId`, provider metadata, context team/project ids, authenticated login id, and actions. Return status `ANSWER` with existing `conclusion`, `confirmedFacts`, `interpretation`, `needsConfirmation`, `sourceChips`, and a new `proposals` list. Add `List<AiActionProposalView> proposals` to `AiChatView`; add `List<AiActionProposalResponse> proposals` to `AiChatResponse`; default it to an empty list for `ERROR` and `NEEDS_CONFIRMATION`. `AiActionProposalResponse` must expose only proposal id, status, executable, action type, risk level, target view, title, summary, preview fields/content/warnings, `expiresAt`, and redacted error summary. Update tests so provider actions no longer produce `READ_ONLY_PROVIDER_ACTION_REJECTED`; they produce sanitized proposal response data with no raw payload.</action>
  <acceptance_criteria>
    - Existing Phase 10 fields still map in `AiChatResponse.from(...)`.
    - `AiChatExecutionServiceTest` proves a provider action creates one persisted proposal and leaves answer facts/source chips intact.
    - `AiChatControllerMvcTest` response JSON contains `proposals[0].proposalId`, `proposals[0].summary`, `proposals[0].target.label`, and `proposals[0].fields`, and does not contain `payload`, `rawPrompt`, `rawContext`, `stdout`, or `stderr`.
    - `READ_ONLY_PROVIDER_ACTION_REJECTED` is not used for valid approval-required drafts after this task.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiChatExecutionService*" --tests "*AiChatController*"</automated>
    <automated>rg "READ_ONLY_PROVIDER_ACTION_REJECTED" src/test/java src/main/java/com/smarterd/application/ai/chat/AiChatExecutionService.java</automated>
  </verify>
  <done>Chat answers can carry sanitized proposal cards from persisted proposal records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add approve, cancel, and refresh endpoints</name>
  <files>src/main/java/com/smarterd/api/ai/AiActionProposalController.java, src/main/java/com/smarterd/api/ai/dto/AiActionProposalDecisionResponse.java, src/main/java/com/smarterd/api/ai/dto/AiActionProposalResponse.java, src/main/java/com/smarterd/domain/common/message/MessageCode.java, src/main/resources/i18n/messages.properties, src/main/resources/i18n/messages_ko.properties, src/test/java/com/smarterd/api/ai/AiActionProposalControllerMvcTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/api/ai/AiChatController.java</file>
    <file>src/main/java/com/smarterd/api/ai/AiProviderController.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java</file>
    <file>src/main/java/com/smarterd/domain/common/message/MessageCode.java</file>
    <file>src/test/java/com/smarterd/api/ai/AiProviderControllerMvcTest.java</file>
  </read_first>
  <action>Create `AiActionProposalController` under `/api/ai/proposals`. Add `GET /api/ai/proposals/{proposalId}` to refresh sanitized proposal view, `POST /api/ai/proposals/{proposalId}/approve` to call `AiActionProposalService.approve(jwt.getSubject(), proposalId)`, and `POST /api/ai/proposals/{proposalId}/cancel` to call `cancel`. Add `AiActionProposalDecisionResponse` with `proposal`, `decision`, `terminal`, and `message` fields where `decision` is one of `APPROVE`, `CANCEL`, `REFRESH`, `IDEMPOTENT`. Anonymous requests throw `ERROR_ACCESS_DENIED_AI_EXECUTION`. Add localized message codes for proposal not found, expired, rejected unsupported action, invalid/stale proposal, and terminal state returned. Tests must cover anonymous denial, approve of pending unsupported action returning `REJECTED`, cancel returning `CANCELLED`, repeated approve/cancel returning current terminal status, and no raw fields in JSON.</action>
  <acceptance_criteria>
    - MVC tests prove all three endpoints require JWT authentication.
    - Approve and cancel endpoints accept only `proposalId` in the path; no browser-supplied payload or preview data is accepted for execution.
    - Repeated approve/cancel of terminal proposals returns HTTP 200 with the existing terminal status and does not call a write executor.
    - JSON responses do not contain `sanitizedPayloadJson`, `payload`, `rawPrompt`, `rawContext`, `rawProviderOutput`, `stdout`, `stderr`, `token`, `cookie`, or `env`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiActionProposalController*"</automated>
  </verify>
  <done>Proposal decisions are server-owned, idempotent, authenticated endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record proposal decision audit metadata</name>
  <files>src/main/java/com/smarterd/application/ai/AiExecutionAuditService.java, src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java, src/test/java/com/smarterd/application/ai/AiExecutionAuditServiceTest.java, src/test/java/com/smarterd/application/ai/proposal/AiActionProposalServiceTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/application/ai/AiExecutionAuditService.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiExecutionAudit.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java</file>
    <file>.planning/phases/11-approval-preview-audit-execution-pipeline/11-CONTEXT.md</file>
  </read_first>
  <action>Extend `AiExecutionAuditService` with proposal-safe methods such as `recordProposalCreated`, `recordProposalDecision`, and `recordProposalFailure` or add equivalent proposal audit writing inside `AiActionProposalService` if a separate event table is not introduced. The audit data must include execution id, proposal id, provider, prompt version, action type, risk level, team id, project id, target type/id/label, requester, decision actor, terminal status, timestamps, and redacted error type/title/detail. Do not store full prompt text, read context, provider output, stdout/stderr, or payload. Add tests that cancel decisions are audit events, validation rejections are audit events, and redacted error detail length is capped consistently with `AiExecutionAudit` patterns.</action>
  <acceptance_criteria>
    - Proposal creation, cancel, reject, execute, and failure paths have audit coverage in tests.
    - Audit methods never accept a raw provider result or raw prompt string as a parameter.
    - `AiExecutionAuditServiceTest` proves error title/detail are redacted or capped before persistence.
    - Cancel decisions are not represented as failures.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiExecutionAuditService*" --tests "*AiActionProposalService*"</automated>
  </verify>
  <done>Proposal decisions and safe failures are captured as redacted audit metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat provider result to response DTO | Model action drafts become user-visible cards only through persisted sanitized proposal views. |
| Browser decision to service | Approve/cancel carries only proposal id and authenticated actor. |
| Decision event to audit | Audit stores traceable metadata without raw prompt, raw context, or payload leakage. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-11-02-01 | Information Disclosure | High | Chat DTO mapping | mitigate | Return `AiActionProposalResponse` from server views only and test for absent raw keys. | `./gradlew test --tests "*AiChatController*"` |
| T-11-02-02 | Tampering | High | Approve/cancel API | mitigate | Accept proposal id only; reload persisted record and revalidate state on server. | `./gradlew test --tests "*AiActionProposalController*"` |
| T-11-02-03 | Elevation of Privilege | High | Approval service | mitigate | Revalidate auth/status/action/executor support before execution; unsupported stays non-mutating. | `./gradlew test --tests "*AiActionProposalService*"` |
| T-11-02-04 | Repudiation | Medium | Proposal decisions | mitigate | Record cancel/reject/failure as audit events with actor and terminal status. | `./gradlew test --tests "*AiExecutionAuditService*"` |
| T-11-02-SC | Tampering | Low | Package installs | accept | No package-manager install is planned. | `git diff -- build.gradle client/package.json` shows no package changes |
</threat_model>

<verification>
- `./gradlew test --tests "*AiChatExecutionService*" --tests "*AiChatController*" --tests "*AiActionProposalController*" --tests "*AiExecutionAuditService*"`
- `rg "payload|rawPrompt|rawContext|rawProviderOutput|stdout|stderr|accessToken|refreshToken|cookie|password|SMART_ERD_|SPRING_|env" src/main/java/com/smarterd/api/ai src/main/java/com/smarterd/application/ai/chat src/main/java/com/smarterd/application/ai/proposal`
</verification>

<success_criteria>
Plan 11-02 is complete when provider actions in chat create sanitized proposal cards, proposal decisions use authenticated idempotent endpoints, approval revalidates server state, and unsupported actions cannot mutate data.
</success_criteria>

<output>
Create `.planning/phases/11-approval-preview-audit-execution-pipeline/11-02-SUMMARY.md` when done.
</output>
