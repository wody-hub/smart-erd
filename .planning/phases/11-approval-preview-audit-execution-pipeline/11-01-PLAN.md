---
phase: 11-approval-preview-audit-execution-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V20260604_01__phase11_ai_action_proposals.sql
  - src/main/java/com/smarterd/domain/ai/AiActionProposal.java
  - src/main/java/com/smarterd/domain/ai/AiActionProposalRepository.java
  - src/main/java/com/smarterd/domain/ai/AiActionProposalStatus.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizer.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionProposalValidator.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionPreviewService.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionProposalView.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionExecutor.java
  - src/main/java/com/smarterd/application/ai/proposal/AiActionExecutorRegistry.java
  - src/test/java/com/smarterd/application/ai/proposal/AiActionProposalServiceTest.java
  - src/test/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizerTest.java
  - src/test/java/com/smarterd/application/ai/ActionDraftValidatorTest.java
autonomous: true
requirements: [AI-ACT-01, AI-APP-01, AI-APP-03, AI-AUD-01, AI-AUD-02]
user_setup: []
must_haves:
  truths:
    - "D-07, D-08, D-09, D-10, D-11, and D-12: proposal preview is generated on the server as typed, user-readable data before any mutation path is reachable."
    - "D-13, D-14, D-15, D-16, D-17, D-18, and D-19: pending proposals are persisted server-side, expire after 15 minutes, transition immutably, and execute only through a registry boundary."
    - "D-20, D-21, D-22, and D-25: proposal storage and audit metadata are redaction-first and never store raw prompts, raw provider context/output, stdout/stderr, credentials, tokens, cookies, env, or local paths."
    - "D-26 and D-27: Phase 11 creates extension points for issue/TODO/WBS actions but leaves production executors unregistered so unsupported actions cannot mutate data."
  artifacts:
    - path: "src/main/java/com/smarterd/domain/ai/AiActionProposal.java"
      provides: "Persisted proposal lifecycle record"
    - path: "src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java"
      provides: "Proposal creation, preview, expiry, terminal transition, and empty executor boundary"
    - path: "src/main/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizer.java"
      provides: "Whitelisted payload/preview/result/error redaction"
  key_links:
    - from: "AiActionProposal"
      to: "AiExecutionAudit"
      via: "metadata-only AI persistence pattern"
      pattern: "executionId"
---

<objective>
Create the backend proposal persistence and lifecycle core for Phase 11.

Purpose: AI write intent must become a persisted, sanitized, typed proposal before it can be shown or approved.
Output: proposal schema/entity/repository, sanitizer/validator/preview services, empty executor registry, and lifecycle tests.
</objective>

<execution_context>
@/Users/j.jaeyo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/j.jaeyo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-CONTEXT.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-RESEARCH.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-VALIDATION.md
@.planning/phases/11-approval-preview-audit-execution-pipeline/11-PATTERNS.md
@src/main/java/com/smarterd/application/ai/provider/AiActionDraft.java
@src/main/java/com/smarterd/application/ai/validation/ActionDraftValidator.java
@src/main/java/com/smarterd/application/ai/validation/ProviderOutputValidator.java
@src/main/java/com/smarterd/domain/ai/AiExecutionAudit.java
</context>

## Source Audit

This plan covers the backend substrate for AI-ACT-01, AI-APP-01, AI-APP-03, AI-AUD-01, and AI-AUD-02. It intentionally does not wire provider actions into chat responses or expose HTTP endpoints; those are Plan 11-02. It intentionally does not register concrete issue/TODO/WBS write executors; those are Phase 12.

## Decision Traceability

- D-07 through D-12 are covered by `AiActionPreviewService` and `AiActionProposalValidator`.
- D-13 through D-19 are covered by `AiActionProposal`, `AiActionProposalStatus`, `AiActionProposalService`, and `AiActionExecutorRegistry`.
- D-20 through D-22 and D-25 are covered by `AiActionProposalSanitizer` and redacted proposal fields.
- D-26 and D-27 are covered by an executor interface plus an empty production registry and unsupported-action rejection tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add proposal persistence schema and domain model</name>
  <files>src/main/resources/db/migration/V20260604_01__phase11_ai_action_proposals.sql, src/main/java/com/smarterd/domain/ai/AiActionProposal.java, src/main/java/com/smarterd/domain/ai/AiActionProposalRepository.java, src/main/java/com/smarterd/domain/ai/AiActionProposalStatus.java</files>
  <read_first>
    <file>src/main/resources/db/migration/V20260601_01__phase9_ai_execution_audit.sql</file>
    <file>src/main/java/com/smarterd/domain/ai/AiExecutionAudit.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiExecutionAuditRepository.java</file>
    <file>src/main/java/com/smarterd/domain/common/entity/BaseAuditEntity.java</file>
  </read_first>
  <action>Create Flyway migration `V20260604_01__phase11_ai_action_proposals.sql` for table `ai_action_proposals` with stable external `proposal_id VARCHAR(80) UNIQUE NOT NULL`, `execution_id`, `provider`, `prompt_version`, `action_type`, `risk_level`, `status`, `team_id`, `project_id`, `target_type`, `target_id`, `target_label`, `title`, `summary`, `requested_by`, `decision_by`, `decided_at`, `expires_at`, `sanitized_payload_json`, `preview_json`, `result_json`, `redacted_error_type`, `redacted_error_title`, `redacted_error_detail`, audit columns, and indexes on `proposal_id`, `execution_id`, `(team_id, project_id, created_at DESC, id DESC)`, `(requested_by, created_at DESC, id DESC)`, and `(status, expires_at)`. Create `AiActionProposalStatus` enum with exactly `PENDING`, `CANCELLED`, `EXPIRED`, `REJECTED`, `EXECUTED`, and `FAILED`. Create `AiActionProposal` as a JPA entity extending `BaseAuditEntity`; add domain methods for `cancel`, `expire`, `reject`, `markExecuted`, and `markFailed` that only transition from `PENDING` and otherwise leave terminal state unchanged. Create `AiActionProposalRepository` with lookup by `proposalId` and project history query support.</action>
  <acceptance_criteria>
    - Migration contains table `ai_action_proposals` and indexes named `idx_ai_action_proposals_proposal_id`, `idx_ai_action_proposals_execution_id`, `idx_ai_action_proposals_project`, `idx_ai_action_proposals_requested_by`, and `idx_ai_action_proposals_status_expiry`.
    - `AiActionProposalStatus` contains only the six statuses listed in this task.
    - `AiActionProposal` has no fields or accessors named `rawPrompt`, `rawContext`, `rawProviderOutput`, `stdout`, `stderr`, `token`, `cookie`, `env`, or `credential`.
    - Repository exposes `Optional<AiActionProposal> findByProposalId(String proposalId)`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiActionProposal*"</automated>
    <automated>rg "rawPrompt|rawContext|rawProviderOutput|stdout|stderr|token|cookie|env|credential" src/main/java/com/smarterd/domain/ai/AiActionProposal.java src/main/resources/db/migration/V20260604_01__phase11_ai_action_proposals.sql</automated>
  </verify>
  <done>Proposal persistence exists with immutable terminal state support and no raw AI payload fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sanitizer, validator, preview view, and executor boundary</name>
  <files>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizer.java, src/main/java/com/smarterd/application/ai/proposal/AiActionProposalValidator.java, src/main/java/com/smarterd/application/ai/proposal/AiActionPreviewService.java, src/main/java/com/smarterd/application/ai/proposal/AiActionProposalView.java, src/main/java/com/smarterd/application/ai/proposal/AiActionExecutor.java, src/main/java/com/smarterd/application/ai/proposal/AiActionExecutorRegistry.java, src/test/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizerTest.java, src/test/java/com/smarterd/application/ai/ActionDraftValidatorTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/application/ai/provider/AiActionDraft.java</file>
    <file>src/main/java/com/smarterd/application/ai/provider/AiActionRiskLevel.java</file>
    <file>src/main/java/com/smarterd/application/ai/validation/ActionDraftValidator.java</file>
    <file>src/test/java/com/smarterd/application/ai/ActionDraftValidatorTest.java</file>
    <file>.planning/phases/11-approval-preview-audit-execution-pipeline/11-RESEARCH.md</file>
  </read_first>
  <action>Create application package `com.smarterd.application.ai.proposal`. Add `AiActionProposalSanitizer` that converts draft payload maps into a whitelisted JSON-safe map with only `targetType`, `targetId`, `targetLabel`, `projectId`, `teamId`, `fields`, `content`, `assumptions`, and `labels`, dropping unknown nested raw keys and redacting any key matching case-insensitive `token`, `cookie`, `password`, `secret`, `credential`, `env`, `stdout`, `stderr`, `prompt`, `context`, `providerOutput`, `shell`, `command`, `sql`, or `path`. Add `AiActionProposalValidator` that rejects missing approval, destructive/bulk/shell/sql action type fragments, missing action type, missing title/summary, missing target metadata, expired proposals, and non-pending proposals. Add `AiActionPreviewService` that returns typed field/content/warning preview data from sanitized values only. Add `AiActionExecutor` interface and `AiActionExecutorRegistry` Spring component that returns no production executors in Phase 11. Extend `ActionDraftValidatorTest` for `bulk`, `sql`, `command`, and approval-required cases.</action>
  <acceptance_criteria>
    - Sanitizer tests prove raw keys are removed from nested maps and lists.
    - Validator tests prove destructive-looking action types are rejected before proposal persistence.
    - `AiActionExecutorRegistry` has no built-in executor for `issue.create`, `issue.update`, `todo.create`, `todo.update`, `wbs.comment.add`, or `wbs.memo.add`.
    - `AiActionProposalView` exposes `proposalId`, `status`, `executable`, `actionType`, `riskLevel`, `target`, `summary`, `fields`, `content`, `warnings`, and `expiresAt`, and does not expose `payload`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*ActionDraftValidator*" --tests "*AiActionProposalSanitizer*"</automated>
    <automated>rg "payload\\(|rawPrompt|rawContext|rawProviderOutput|stdout|stderr|token|cookie|env" src/main/java/com/smarterd/application/ai/proposal</automated>
  </verify>
  <done>Provider drafts can be reduced to safe proposal previews and the only execution boundary is a currently-empty registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement proposal lifecycle service</name>
  <files>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalService.java, src/test/java/com/smarterd/application/ai/proposal/AiActionProposalServiceTest.java</files>
  <read_first>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalSanitizer.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionProposalValidator.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionPreviewService.java</file>
    <file>src/main/java/com/smarterd/application/ai/proposal/AiActionExecutorRegistry.java</file>
    <file>src/main/java/com/smarterd/domain/ai/AiActionProposal.java</file>
    <file>src/main/java/com/smarterd/application/ai/AiExecutionAuditService.java</file>
  </read_first>
  <action>Create `AiActionProposalService` with transactional methods `createProposals(CreateCommand)`, `getProposal(loginId, proposalId)`, `cancel(loginId, proposalId)`, `approve(loginId, proposalId)`, and `expirePending(Instant now)`. `CreateCommand` must include `executionId`, `provider`, `promptVersion`, `teamId`, `projectId`, `requestedBy`, and `List<AiActionDraft>`. Creation validates drafts, sanitizes payload, generates preview, sets status `PENDING`, sets `expiresAt` to `createdAt + 15 minutes`, and initializes audit actor as `requestedBy`. `cancel` changes only pending proposals to `CANCELLED`; terminal proposals return current view. `approve` revalidates pending status, expiry, auth placeholder rules, action type, sanitized payload, target fields, and executor availability; with no executor it transitions to `REJECTED` with redacted error metadata and no mutation. Add tests for creation, sanitization, 15 minute expiry, cancel idempotency, approve idempotency, unsupported rejection, and no executor invocation.</action>
  <acceptance_criteria>
    - `AiActionProposalServiceTest` covers task IDs `11-01-01` and `11-02-01` from `11-VALIDATION.md`.
    - Creating two drafts under one execution creates two independent proposal ids.
    - Repeated cancel or approve against `CANCELLED`, `EXPIRED`, `REJECTED`, `EXECUTED`, or `FAILED` returns the same terminal status without creating another mutation/result.
    - Approving `issue.create` before Phase 12 executor registration returns `REJECTED` and leaves no project issue, TODO, WBS comment, or work memo service invoked.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "*AiActionProposalService*"</automated>
  </verify>
  <done>Proposal lifecycle core is persisted, sanitized, expiring, idempotent, and executor-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider action to proposal service | Model-controlled action data becomes server-owned proposal data. |
| Proposal record to browser DTO | Persisted data is reduced to sanitized, user-readable preview fields. |
| Approval request to executor registry | User click may request mutation but must pass server transition and executor gates first. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification |
|-----------|----------|----------|-----------|-------------|-----------------|--------------|
| T-11-01-01 | Information Disclosure | High | Sanitizer and entity | mitigate | Whitelist proposal fields and reject/redact raw prompt/context/provider/stdout/stderr/token/cookie/env keys. | `./gradlew test --tests "*AiActionProposalSanitizer*"` plus `rg` raw-key scan |
| T-11-01-02 | Tampering | High | Proposal lifecycle | mitigate | Only `PENDING` transitions; terminal approve/cancel is idempotent and cannot duplicate execution. | `./gradlew test --tests "*AiActionProposalService*"` |
| T-11-01-03 | Elevation of Privilege | High | Executor registry | mitigate | Empty production registry in Phase 11; unsupported approve transitions to `REJECTED` without mutation. | `./gradlew test --tests "*AiActionProposalService*"` |
| T-11-01-SC | Tampering | Low | Package installs | accept | No Java or npm dependency is introduced by this plan. | `git diff -- build.gradle client/package.json` shows no package changes |
</threat_model>

<verification>
- `./gradlew test --tests "*ActionDraftValidator*" --tests "*AiActionProposal*"`
- `rg "rawPrompt|rawContext|rawProviderOutput|stdout|stderr|accessToken|refreshToken|cookie|password|SMART_ERD_|SPRING_|env" src/main/java/com/smarterd/domain/ai src/main/java/com/smarterd/application/ai/proposal`
</verification>

<success_criteria>
Plan 11-01 is complete when provider drafts can be persisted as sanitized pending proposals, terminal state transitions are immutable/idempotent, preview data is server generated, and unsupported actions cannot reach any production write service.
</success_criteria>

<output>
Create `.planning/phases/11-approval-preview-audit-execution-pipeline/11-01-SUMMARY.md` when done.
</output>
