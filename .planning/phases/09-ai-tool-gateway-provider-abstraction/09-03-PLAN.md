---
phase: 09-ai-tool-gateway-provider-abstraction
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - client/src/types/ai-provider.ts
  - client/src/api/aiProviderApi.ts
  - client/src/constants/query-keys.ts
  - client/src/hooks/useAiProviderStatus.ts
  - client/src/components/ai/AiProviderStatusBadge.tsx
  - client/src/pages/project/ProjectsPage.tsx
  - client/src/i18n/locales/ko/translation.json
  - client/src/i18n/locales/en/translation.json
  - client/test/unit/ai-provider-status.test.ts
  - .planning/phases/09-ai-tool-gateway-provider-abstraction/VERIFICATION.md
  - .planning/phases/09-ai-tool-gateway-provider-abstraction/SUMMARY.md
autonomous: true
requirements:
  - AI-RUN-01
  - AI-RUN-02
  - AI-RUN-03
  - AI-RUN-04
  - AI-SEC-01
requirements_addressed: [AI-RUN-01, AI-RUN-02, AI-RUN-03, AI-RUN-04, AI-SEC-01]

must_haves:
  truths:
    - "Decision coverage: D-03, D-21, D-23, D-24, D-49, D-52, D-53, D-58, D-64"
    - "React talks to Spring HTTP through shared `aiProviderApi`; Electron IPC is not required for Phase 9."
    - "The Phase 9 frontend adds a minimal provider status surface or reusable hook only. It does not add app chat, rich read tools, approval preview, write execution, or provider configuration UI."
    - "Provider status UI must not display executable paths, auth paths, raw stderr, raw stdout, prompt text, context JSON, or credential-related details."
    - "Frontend status typing and query keys are covered by TypeScript build and at least one focused unit test/helper test."
  artifacts:
    - path: "client/src/api/aiProviderApi.ts"
      provides: "typed HTTP client for Phase 9 provider status, execute, execution status, and cancel endpoints"
    - path: "client/src/hooks/useAiProviderStatus.ts"
      provides: "React Query hook for minimal AI runtime status surface"
    - path: "client/src/components/ai/AiProviderStatusBadge.tsx"
      provides: "compact user-visible status badge for AI-RUN-01"
    - path: "client/src/pages/project/ProjectsPage.tsx"
      provides: "existing project workspace entry point where the compact status badge is visible without building chat UI"
  key_links:
    - from: "client/src/components/ai/AiProviderStatusBadge.tsx"
      to: "client/src/hooks/useAiProviderStatus.ts"
      via: "status query"
      pattern: "useAiProviderStatus"
    - from: "client/src/hooks/useAiProviderStatus.ts"
      to: "client/src/api/aiProviderApi.ts"
      via: "React Query fetcher"
      pattern: "fetchAiProviderStatus"
    - from: "client/src/api/aiProviderApi.ts"
      to: "src/main/java/com/smarterd/api/ai/AiProviderController.java"
      via: "HTTP path contract"
      pattern: "/ai/provider/status"
---

<objective>
Add the minimal frontend contract and user-visible runtime status surface needed to prove Phase 9 works for Web and Electron through the same Spring HTTP API. Then verify the backend/frontend contract end to end and write closeout artifacts.

This plan deliberately avoids the Phase 10 app AI chatbot. It only exposes provider status and typed API primitives that later phases can reuse.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-CONTEXT.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-AI-SPEC.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-RESEARCH.md
@.planning/phases/09-ai-tool-gateway-provider-abstraction/09-PATTERNS.md
@src/main/java/com/smarterd/api/ai/AiProviderController.java
@client/src/api/issuesApi.ts
@client/src/api/projectTodoApi.ts
@client/src/constants/query-keys.ts
@client/src/pages/project/ProjectsPage.tsx
@client/src/i18n/locales/ko/translation.json
@client/src/i18n/locales/en/translation.json
</context>

<goal_backward_verification>

| Requirement | Plan coverage | Completion proof |
|-------------|---------------|------------------|
| AI-RUN-01 | Adds provider status API client, React Query hook, i18n labels, and compact badge | User can see whether AI runtime is available, not configured, missing, not logged in, or unsupported |
| AI-RUN-02 | Frontend calls only Spring HTTP API and imports no Codex/Electron IPC runtime | TypeScript/rg checks prove no UI-to-Codex or Electron IPC coupling |
| AI-RUN-03 | Frontend DTOs include the structured execute/status contract from backend | API typing matches backend DTO shape and build passes |
| AI-RUN-04 | Frontend API primitives include execute/status/cancel with typed statuses/errors | TypeScript build and focused tests cover status enum and query key behavior |
| AI-SEC-01 | UI displays safe enum/summary only and never raw provider process details | Component/test inspection proves no path/stderr/stdout/prompt/context fields are rendered |

</goal_backward_verification>

<threat_model>

| Threat | Mitigation | Verification |
|--------|------------|--------------|
| Frontend bypasses backend provider boundary | `aiProviderApi` uses existing `axiosInstance` and `/api/ai/provider/**` paths only | `rg "codex|electron|ipc" client/src/api client/src/components/ai client/src/hooks` shows no direct runtime coupling |
| User sees local secrets or machine paths | Backend DTO exposes safe status enum; badge maps enum to localized label only | Component/unit tests and code inspection assert no raw detail/path/stderr/stdout fields are rendered |
| Phase 10 chat scope leaks into Phase 9 | Only status badge/hook/API primitives are added; no chat message composer/history/context tools | File diff contains no chat UI, read-tool UI, approval preview, or write-execution UI |
| DTO drift between backend and frontend | Mirror exact status/state enums and endpoint paths from Plan 01/02 | `npm run build` and focused frontend test pass |

</threat_model>

<tasks>

<task id="09-03-01" type="auto">
  <name>Add typed frontend AI provider API and query keys</name>
  <files>
    client/src/types/ai-provider.ts,
    client/src/api/aiProviderApi.ts,
    client/src/constants/query-keys.ts,
    client/test/unit/ai-provider-status.test.ts
  </files>
  <read_first>
    - client/src/api/issuesApi.ts
    - client/src/api/projectTodoApi.ts
    - client/src/constants/query-keys.ts
  </read_first>
  <action>
    Add TypeScript types for:
    - provider availability enum: `AVAILABLE`, `NOT_CONFIGURED`, `CODEX_NOT_FOUND`, `CODEX_NOT_LOGGED_IN`, `UNSUPPORTED_ENVIRONMENT`
    - execution state enum: `QUEUED`, `RUNNING`, `SUCCEEDED`, `FAILED`, `TIMED_OUT`, `CANCELLED`
    - execute request/response
    - execution status response
    - action draft response
    - provider error response

    Add `aiProviderApi` functions:
    - `fetchAiProviderStatus()`
    - `executeAiProvider(request)`
    - `fetchAiExecutionStatus(executionId)`
    - `cancelAiExecution(executionId)`

    Extend query keys with `queryKeys.aiProvider.status()` and `queryKeys.aiProvider.execution(executionId)`.
  </action>
  <acceptance_criteria>
    - Paths match `/ai/provider/**` client convention behind `axiosInstance`.
    - Types do not include raw stdout/stderr, prompt, context, executable path, auth path, or credential fields.
    - Focused unit test covers status label/query-key helper behavior if a pure helper is added.
  </acceptance_criteria>
</task>

<task id="09-03-02" type="auto">
  <name>Add reusable status hook and compact badge</name>
  <files>
    client/src/hooks/useAiProviderStatus.ts,
    client/src/components/ai/AiProviderStatusBadge.tsx,
    client/src/pages/project/ProjectsPage.tsx,
    client/src/i18n/locales/ko/translation.json,
    client/src/i18n/locales/en/translation.json
  </files>
  <read_first>
    - client/src/pages/project/ProjectsPage.tsx
    - client/src/components/common or existing small badge/button patterns if present
    - client/src/i18n/locales/ko/translation.json
  </read_first>
  <action>
    Add `useAiProviderStatus` with React Query using the new query key and API function.

    Add `AiProviderStatusBadge` as a compact, non-blocking status surface. Place it in an existing project workspace entry/utility area, such as the Projects page hero action area, so users can see AI runtime availability without a chat UI.

    Map statuses to localized labels. Use restrained UI styling consistent with existing app controls and avoid displaying process details. Unknown/loading/error states should be safe and concise.
  </action>
  <acceptance_criteria>
    - Badge is visible on the chosen project workspace entry surface.
    - Badge renders safe status label only.
    - No Electron IPC, direct Codex invocation, or local path handling appears in client code.
    - Loading/error states do not imply AI is usable when provider status is unknown.
  </acceptance_criteria>
</task>

<task id="09-03-03" type="auto">
  <name>Run contract verification and write closeout artifacts</name>
  <files>
    .planning/phases/09-ai-tool-gateway-provider-abstraction/VERIFICATION.md,
    .planning/phases/09-ai-tool-gateway-provider-abstraction/SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/09-ai-tool-gateway-provider-abstraction/09-VALIDATION.md
    - .planning/phases/09-ai-tool-gateway-provider-abstraction/09-01-PLAN.md
    - .planning/phases/09-ai-tool-gateway-provider-abstraction/09-02-PLAN.md
  </read_first>
  <action>
    After implementation, run the backend and frontend verification matrix. Record command outputs, skipped local Codex smoke conditions, browser/manual observations if performed, and residual risks in `VERIFICATION.md`.

    Write `SUMMARY.md` with delivered scope, exact out-of-scope items, important test coverage, and follow-up items for Phase 10+.
  </action>
  <acceptance_criteria>
    - `VERIFICATION.md` includes backend tests, frontend build, security rg checks, and local smoke status.
    - `SUMMARY.md` explicitly says Phase 9 did not implement chat UI, rich read tools, approval UI, write tools, hosted providers, or one-shot correction retry.
    - Follow-up notes include the deferred one-shot correction retry phase noted in 09-CONTEXT.md.
  </acceptance_criteria>
</task>

</tasks>

<verification>

Frontend verification:

```bash
(cd client && npm run build)
(cd client && npm run test:unit)
```

Full Phase 9 verification:

```bash
./gradlew test
cd client && npm run build
```

Contract/security checks:

```bash
rg "codex|electron|ipc" client/src/api client/src/hooks client/src/components/ai
rg "stdout|stderr|prompt|context|token|cookie|password|executablePath|authPath" client/src/types client/src/api client/src/components/ai
```

</verification>

<success_criteria>

- A user can see Local Codex runtime availability through a minimal status surface.
- Web and Electron both use the same Spring HTTP API path; no Electron IPC is introduced for Phase 9.
- Frontend exposes reusable typed API primitives for later chat/read/action phases.
- Backend tests, frontend build, and security checks pass or have documented opt-in smoke skip reasons.

</success_criteria>
