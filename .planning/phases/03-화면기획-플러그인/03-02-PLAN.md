---
phase: 03-화면기획-플러그인
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts
  - client/e2e/shared/screen-spec-e2e.ts
  - client/src/pages/screendesign/ScreenDesignInteractivePage.tsx
  - client/src/pages/screendesign/ScreenDesignEditorShell.tsx
  - client/src/pages/screendesign/ScreenDesignInspector.tsx
  - client/src/i18n/locales/ko/translation.json
  - client/src/i18n/locales/en/translation.json
  - src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java
autonomous: true
requirements:
  - SPEC-02
  - SPEC-03

must_haves:
  truths:
    - "D-04: collaboration closeout uses three browser contexts/accounts, not a two-account happy path"
    - "D-05: collaboration scenario covers screen creation/rename, master create/update/delete, instance placement/move/resize, save/re-entry, and scope lock behavior"
    - "D-06: collaboration pass/fail is UX-strict; propagation delay, missing lock indicator, or remote-state display problems are failures"
    - "D-10: `ScreenSpecCollaborationPlugin.validationHook()` intentionally remains no-op for v1 closeout"
    - "D-11: no code TODO, backlog entry, or backend structure-validation implementation is required in this phase"
    - "D-12: validation rationale relies on frontend document model, mutation policy/applier tests, scope resolver tests, and E2E/browser evidence"
    - "협업 closeout E2E는 owner/member-one/member-two 3개 계정과 3개 isolated browser context를 사용한다"
    - "세 계정은 같은 screen-spec 문서를 열고 screen/master/instance 변경 전파를 상호 확인한다"
    - "테스트는 최종 수렴만으로 통과하지 않고 remote change 표시와 scope lock/conflict UX를 확인한다"
    - "scope lock/conflict 표시가 화면기획 UI에 없으면 PLAN 실행 중 최소 표시를 추가해야 하며, 없음을 정상으로 문서화해서는 안 된다"
    - "save/re-entry 후 세 계정 변경의 persisted content가 유지된다"
    - "backend `ScreenSpecCollaborationPlugin.validationHook()`은 no-op으로 유지한다"
  artifacts:
    - path: "client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts"
      provides: "three-account SPEC-03 evidence"
    - path: "client/src/pages/screendesign/ScreenDesignEditorShell.tsx"
      provides: "compact collaboration/lock status affordance if missing"
    - path: "src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java"
      provides: "scope resolver lock baseline coverage"
---

<objective>
Create UX-strict three-account collaboration evidence for screen-spec. This plan adapts the existing markdown three-account pattern and treats missing remote/lock UX as a blocking closeout issue, not a documentation caveat.

Purpose: prove SPEC-03 on the actual Yjs/ScopeLock/Presence path and strengthen SPEC-02 propagation evidence.
Output:
- `screen-spec-three-account-collaboration.spec.ts`
- minimal screen-spec collaboration status UI only if absent
- targeted backend scope resolver coverage if gaps are found
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-화면기획-플러그인/03-CONTEXT.md
@.planning/phases/03-화면기획-플러그인/03-RESEARCH.md
@.planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
@.planning/phases/03-화면기획-플러그인/03-PATTERNS.md
@client/e2e/smoke/markdown-three-account-collaboration.spec.ts
@client/e2e/shared/diagram-e2e.ts
@client/e2e/shared/screen-spec-e2e.ts
@client/src/pages/screendesign/ScreenDesignInteractivePage.tsx
@client/src/pages/screendesign/ScreenDesignEditorShell.tsx
@client/src/pages/screendesign/ScreenDesignInspector.tsx
@client/src/collaboration/plugins/screen-spec/screen-spec-scope-resolver.ts
@src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolver.java
@src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java
</context>

<threat_model>
T-03-04: Same-scope edits silently overwrite remote user work while final Y.Doc convergence makes the test appear green.
Mitigation: E2E must assert visible lock/conflict/rejected-edit status and fail when no user-visible signal exists.

T-03-05: Member users access documents outside team authorization during fixture setup.
Mitigation: adapt markdown access checks and assert diagram detail/bootstrap are accessible only after invite.

T-03-06: Browser console/page errors hide collaboration runtime failures.
Mitigation: attach diagnostics to all three pages and fail on warning/error/pageerror unless explicitly filtered with a concrete reason.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: adapt three-account fixture and diagnostics for screen-spec</name>
  <files>
    client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts,
    client/e2e/shared/screen-spec-e2e.ts
  </files>
  <read_first>
    - client/e2e/smoke/markdown-three-account-collaboration.spec.ts
    - client/e2e/shared/diagram-e2e.ts
    - client/e2e/shared/screen-spec-e2e.ts
  </read_first>
  <action>
Create `client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts` by adapting the markdown three-account spec:

1. Implement local helpers equivalent to:
   - `signupUser`
   - `inviteMemberToTeam`
   - `assertDocumentAccess`
   - `attachDiagnostics`
   - `assertNoBrowserIssues`
2. Provision owner/team/project/document where document creation payload uses `pluginId: 'screen-spec'`.
3. Sign up `member-one` and `member-two`, invite both to the owner team, and assert detail/bootstrap access for both.
4. Open owner/member-one/member-two pages in isolated browser contexts through `openScreenSpecDocumentSession`.
5. Use `PROPAGATION_TIMEOUT_MS = 20_000` or stricter.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run test:e2e -- e2e/smoke/screen-spec-three-account-collaboration.spec.ts --browser=chromium --workers=1 --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "member-one\\|member-two" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "pluginId: 'screen-spec'" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "assertDocumentAccess" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "attachDiagnostics" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: three-account propagation, lock/conflict, save/re-entry scenario</name>
  <files>
    client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts,
    client/e2e/shared/screen-spec-e2e.ts
  </files>
  <read_first>
    - client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts
    - client/e2e/shared/screen-spec-e2e.ts
    - client/src/pages/screendesign/use-screen-design-document.ts
    - client/src/collaboration/plugins/screen-spec/screen-spec-scope-resolver.ts
  </read_first>
  <action>
Implement one end-to-end scenario:

1. Owner adds a second screen and renames it to `Owner screen ${suffix}`.
2. Member one waits until `Owner screen ${suffix}` is visible.
3. Owner drags a master to canvas and changes the selected instance label to `Owner instance ${suffix}`.
4. Member two waits until `Owner instance ${suffix}` is visible or otherwise observable through the inspector/canvas helper.
5. Member one performs a different-scope edit, such as renaming another screen to `Member one screen ${suffix}`.
6. Owner and member two both wait for `Member one screen ${suffix}`.
7. Member two attempts a same-scope edit while owner/member one is editing the selected screen or instance scope.
8. Assert one of these exact outcomes:
   - visible lock/conflict/rejected-edit indicator appears, or
   - the edit is prevented and an accessible status text explains the lock/conflict.
9. Save from owner page if save button is visible.
10. Reload owner and member pages and assert both owner and member-one changes remain.
11. Fail if browser diagnostics contain warnings/errors/pageerrors.

Do not accept "final data converged after refresh" as sufficient for the same-scope conflict step.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run test:e2e -- e2e/smoke/screen-spec-three-account-collaboration.spec.ts --browser=chromium --workers=1 --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Owner screen" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "Member one screen" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -ni "lock\\|conflict\\|rejected\\|잠금\\|충돌" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `grep -n "reload" client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts`
    - `cd client && npm run test:e2e -- e2e/smoke/screen-spec-three-account-collaboration.spec.ts --browser=chromium --workers=1 --retries=0`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: minimal lock/remote state UI if shared shell is insufficient</name>
  <files>
    client/src/pages/screendesign/ScreenDesignInteractivePage.tsx,
    client/src/pages/screendesign/ScreenDesignEditorShell.tsx,
    client/src/pages/screendesign/ScreenDesignInspector.tsx,
    client/src/i18n/locales/ko/translation.json,
    client/src/i18n/locales/en/translation.json
  </files>
  <read_first>
    - client/src/pages/screendesign/ScreenDesignInteractivePage.tsx
    - client/src/pages/screendesign/ScreenDesignEditorShell.tsx
    - client/src/pages/screendesign/ScreenDesignInspector.tsx
    - client/src/pages/screendesign/use-screen-design-interactive-page-data.ts
    - client/src/collaboration/channel/document/use-screen-design-document-runtime.ts
    - client/src/i18n/locales/ko/translation.json
    - client/src/i18n/locales/en/translation.json
    - .planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
  </read_first>
  <action>
First inspect whether existing shared collaboration state already exposes a user-visible lock/conflict indicator in the screen-spec route. If it does, do not add new UI; instead wire the E2E to assert the existing indicator.

If no visible state exists, add the smallest screen-spec affordance:

1. Extend `ScreenDesignEditorShell` props with `collaborationStatusLabel: string` and optional `collaborationStatusTone: 'ready' | 'connecting' | 'warning' | 'error'`.
2. Replace the current ready/connecting pill text with `collaborationStatusLabel`.
3. Use existing semantic token classes only:
   - ready: `text-muted-foreground`
   - connecting: `text-muted-foreground`
   - warning: `border-composition-warning-border bg-composition-warning-bg text-composition-warning-foreground`
   - error: destructive token classes already used in the app
4. Add Korean/English translation keys under `screenSpec.status`:
   - `remoteChanged`
   - `locked`
   - `editRejected`
5. Feed `remoteChanged` when `lastDocumentChangeSummary?.origin === 'remote'`.
6. Feed `locked` or `editRejected` only if the runtime exposes rejected/locked mutation state. If that runtime state is not currently exposed, do not fake it; instead make Task 2 fail and record the missing state as a blocker for SPEC-03 implementation.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - If UI added: `grep -n "collaborationStatusLabel" client/src/pages/screendesign/ScreenDesignEditorShell.tsx`
    - If UI added: `grep -n "remoteChanged\\|locked\\|editRejected" client/src/i18n/locales/ko/translation.json client/src/i18n/locales/en/translation.json`
    - If UI not added: `screen-spec-three-account-collaboration.spec.ts` contains assertions for an existing lock/conflict indicator
    - `git diff -- client/src/pages/screendesign client/src/i18n/locales` contains no hardcoded color hex values
    - `cd client && npm run build`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: backend scope resolver regression coverage</name>
  <files>
    src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java
  </files>
  <read_first>
    - src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolver.java
    - src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java
    - src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecCollaborationPlugin.java
  </read_first>
  <action>
Review `ScreenSpecScopeResolverTest` and add missing cases for:

- screen rename/update command resolves screen scope exclusive
- master update/delete command resolves master scope exclusive
- instance move/resize/update command resolves instance + owning screen scope
- layer move command resolves layer/screen scope
- malformed payload falls back to document root scope

Do not change `ScreenSpecCollaborationPlugin.validationHook()`; it must remain no-op for v1 closeout.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd && ./gradlew test --tests '*ScreenSpecScopeResolverTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "malformed\\|fallback\\|root" src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java`
    - `grep -n "master\\|instance\\|layer" src/test/java/com/smarterd/domain/diagram/collaboration/ScreenSpecScopeResolverTest.java`
    - `grep -n "no-op validation hook\\|return (snapshot)" src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecCollaborationPlugin.java`
    - `./gradlew test --tests '*ScreenSpecScopeResolverTest'`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `./gradlew test --tests '*ScreenSpecScopeResolverTest'`
2. `cd client && npm run test:e2e -- e2e/smoke/screen-spec-three-account-collaboration.spec.ts --browser=chromium --workers=1 --retries=0`
3. `cd client && npm run build`
</verification>

<success_criteria>
- SPEC-03 has three-account browser evidence.
- Lock/conflict UX is visible and asserted, or the phase fails with a concrete blocker instead of being marked complete.
- SPEC-02 has cross-client propagation evidence for master/instance state.
</success_criteria>

<output>
After completion, update `.planning/phases/03-화면기획-플러그인/03-VALIDATION.md` and preserve command/evidence details for `03-VERIFICATION.md`.
</output>
