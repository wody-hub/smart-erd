---
phase: 03-화면기획-플러그인
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/e2e/shared/screen-spec-e2e.ts
  - client/e2e/smoke/screen-spec-authoring-export.spec.ts
  - client/src/pages/screendesign/ScreenDesignEditorShell.tsx
  - client/src/pages/screendesign/ScreenDesignLibrary.tsx
  - client/src/pages/screendesign/ScreenDesignCanvas.tsx
  - client/src/pages/screendesign/ScreenDesignInspector.tsx
  - client/test/unit/screen-design-export.test.ts
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-04

must_haves:
  truths:
    - "D-01: automated test-profile E2E evidence and dev-profile manual QA evidence are both required before closeout"
    - "D-03: SPEC-01, SPEC-02, SPEC-03, and SPEC-04 each need at least one concrete evidence item before Phase 3 can be marked complete"
    - "D-07: PNG and PDF export are both mandatory for SPEC-04; if either fails, SPEC-04 remains incomplete"
    - "D-08: export verification uses Playwright download handling with filename, non-empty size, PNG signature, and PDF structural sanity checks"
    - "D-09: manual dev QA may supplement export evidence, but automated download checks are the primary acceptance signal"
    - "screen-spec 문서는 기존 diagram E2E provisioning helper의 `pluginId: 'screen-spec'` 경로로 생성된다"
    - "자동 E2E는 실제 화면기획 에디터를 열고, 화면 추가/이름 변경/마스터 배치/인스턴스 이동 또는 리사이즈/저장/재진입을 검증한다"
    - "SPEC-04 자동 증거는 Playwright download 이벤트로 PNG와 PDF를 모두 검사한다"
    - "PNG 검증은 `.png` 파일명, 8-byte PNG signature, 0보다 큰 파일 크기를 확인한다"
    - "PDF 검증은 `.pdf` 파일명, `%PDF` signature, 0보다 큰 파일 크기, page marker 또는 동등한 구조 sanity를 확인한다"
    - "E2E 안정성을 위한 test hook은 화면기획 wrapper 요소에만 최소 추가하고 시각 스타일을 바꾸지 않는다"
  artifacts:
    - path: "client/e2e/shared/screen-spec-e2e.ts"
      provides: "screen-spec E2E locators and reusable authoring/export actions"
    - path: "client/e2e/smoke/screen-spec-authoring-export.spec.ts"
      provides: "SPEC-01/SPEC-02/SPEC-04 browser evidence"
    - path: "client/src/pages/screendesign/*"
      provides: "minimal stable hooks if semantic locators are insufficient"
  key_links:
    - from: "screen-spec-authoring-export.spec.ts"
      to: "diagram-e2e.ts"
      via: "provisionCollaborationFixture({ pluginId: 'screen-spec' }) + loginViaUi + diagramUrl"
      pattern: "getE2EProvisioningConfig|provisionCollaborationFixture|diagramUrl"
    - from: "screen-spec-authoring-export.spec.ts"
      to: "ScreenDesignEditorShell.tsx"
      via: "toolbar export dropdown"
      pattern: "screenSpec.toolbar.export|PNG|PDF"
---

<objective>
Create deterministic browser evidence for single-user screen-spec authoring and export. This plan closes the E2E harness gap for SPEC-01, exercises the existing master/instance cascade enough to support SPEC-02, and proves SPEC-04 with real PNG/PDF downloads.

Purpose: turn already-implemented screen-spec UI/export runtime into automated closeout evidence.
Output:
- `client/e2e/shared/screen-spec-e2e.ts`
- `client/e2e/smoke/screen-spec-authoring-export.spec.ts`
- minimal screen-spec UI test hooks only if required
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-화면기획-플러그인/03-CONTEXT.md
@.planning/phases/03-화면기획-플러그인/03-RESEARCH.md
@.planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
@.planning/phases/03-화면기획-플러그인/03-VALIDATION.md
@.planning/phases/03-화면기획-플러그인/03-PATTERNS.md
@client/e2e/shared/diagram-e2e.ts
@client/e2e/smoke/diagram-collaboration.spec.ts
@client/e2e/smoke/dictionary-export-all.spec.ts
@client/src/pages/screendesign/ScreenDesignInteractivePage.tsx
@client/src/pages/screendesign/ScreenDesignEditorShell.tsx
@client/src/pages/screendesign/ScreenDesignLibrary.tsx
@client/src/pages/screendesign/ScreenDesignCanvas.tsx
@client/src/pages/screendesign/ScreenDesignInspector.tsx
@client/src/pages/screendesign/screen-design-export.ts
@client/src/pages/screendesign/use-screen-design-export.ts
</context>

<threat_model>
T-03-01: E2E fixture data leaks between runs or reuses real user documents.
Mitigation: create unique owner/team/project/document names with timestamp/random suffix and use test-profile provisioning helpers.

T-03-02: Export tests pass on a UI click without verifying generated file content.
Mitigation: assert Playwright download path, suggested filename extension, file size, PNG/PDF magic bytes, and PDF page marker or equivalent structural sanity.

T-03-03: Test-only hooks alter production behavior.
Mitigation: hooks must be passive attributes on wrapper elements only; no styling or branch logic may depend on E2E mode.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: screen-spec E2E helper layer</name>
  <files>
    client/e2e/shared/screen-spec-e2e.ts
  </files>
  <read_first>
    - client/e2e/shared/diagram-e2e.ts
    - client/e2e/smoke/diagram-collaboration.spec.ts
    - client/e2e/smoke/dictionary-export-all.spec.ts
    - client/src/pages/screendesign/ScreenDesignEditorShell.tsx
    - client/src/pages/screendesign/ScreenDesignLibrary.tsx
    - client/src/pages/screendesign/ScreenDesignCanvas.tsx
    - client/src/pages/screendesign/ScreenDesignInspector.tsx
    - .planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
  </read_first>
  <action>
Create `client/e2e/shared/screen-spec-e2e.ts` with reusable helpers:

1. Export `openScreenSpecDocumentSession(context, config, loginId, password, target)`:
   - login through `loginViaUi`
   - navigate to `diagramUrl(config, target)`
   - wait for `workspace.action.backToDocuments`
   - wait for screen-spec workspace title text from `screenSpec.canvas.workspaceTitle`
   - wait for ready status text matching `/캔버스 연결됨|Canvas connected/i`

2. Export `waitForScreenSpecEditorReady(page)`:
   - assert screen list title `/화면 목록|Screen list/i`
   - assert library title `/빌딩 블록|Building blocks/i`
   - assert inspector title `/인스펙터|Inspector/i`
   - assert export button `/내보내기|Export/i`

3. Export action helpers:
   - `addScreen(page)`
   - `renameSelectedScreen(page, name)`
   - `dragFirstLibraryItemToCanvas(page)`
   - `selectFirstInstance(page)`
   - `moveSelectedInstanceByKeyboard(page, dxSteps, dySteps)` using arrow keys where practical
   - `triggerExportDownload(page, format: 'png' | 'pdf')`

4. Export assertion helpers:
   - `expectScreenNameVisible(page, name)`
   - `expectInstanceCountVisible(page, count)`
   - `assertPngDownload(download)`
   - `assertPdfDownload(download)`

Use role/name locators first. If Konva or drag/drop cannot be targeted reliably with role/name locators, add minimal `data-testid` hooks in Task 2 before completing helper implementation.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run test:e2e -- e2e/smoke/screen-spec-authoring-export.spec.ts --browser=chromium --workers=1 --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/e2e/shared/screen-spec-e2e.ts`
    - `grep -n "openScreenSpecDocumentSession" client/e2e/shared/screen-spec-e2e.ts`
    - `grep -n "waitForScreenSpecEditorReady" client/e2e/shared/screen-spec-e2e.ts`
    - `grep -n "triggerExportDownload" client/e2e/shared/screen-spec-e2e.ts`
    - `grep -n "assertPngDownload" client/e2e/shared/screen-spec-e2e.ts`
    - `grep -n "assertPdfDownload" client/e2e/shared/screen-spec-e2e.ts`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: minimal screen-spec test hooks if required</name>
  <files>
    client/src/pages/screendesign/ScreenDesignEditorShell.tsx,
    client/src/pages/screendesign/ScreenDesignLibrary.tsx,
    client/src/pages/screendesign/ScreenDesignCanvas.tsx,
    client/src/pages/screendesign/ScreenDesignInspector.tsx
  </files>
  <read_first>
    - client/src/pages/screendesign/ScreenDesignEditorShell.tsx
    - client/src/pages/screendesign/ScreenDesignLibrary.tsx
    - client/src/pages/screendesign/ScreenDesignCanvas.tsx
    - client/src/pages/screendesign/ScreenDesignInspector.tsx
    - .planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
  </read_first>
  <action>
Only if Task 1 cannot use stable role/name locators, add passive `data-testid` attributes:

- `screen-spec-editor-shell` on the top editor root in `ScreenDesignEditorShell`
- `screen-spec-screen-card-${screen.id}` on each screen card wrapper in `ScreenDesignLibrary`
- `screen-spec-library-item-${item.id}` on each draggable library item button in `ScreenDesignLibrary`
- `screen-spec-canvas-dropzone` on the canvas wrapper div in `ScreenDesignCanvas`
- `screen-spec-instance-label-input` on the instance label override input in `ScreenDesignInspector`

Do not add conditionals, styles, environment checks, or test-only code paths. These attributes are allowed only as stable locators.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - If hooks are added: `grep -n "screen-spec-canvas-dropzone\\|screen-spec-library-item" client/src/pages/screendesign/*.tsx`
    - If hooks are not added: `client/e2e/shared/screen-spec-e2e.ts` uses role/name locators for screen list, library, inspector, export, and selected screen rename
    - `git diff -- client/src/pages/screendesign` contains no hardcoded color hex values
    - `cd client && npm run build`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: authoring, persistence, PNG/PDF export E2E</name>
  <files>
    client/e2e/smoke/screen-spec-authoring-export.spec.ts,
    client/e2e/shared/screen-spec-e2e.ts
  </files>
  <read_first>
    - client/e2e/shared/screen-spec-e2e.ts
    - client/e2e/shared/diagram-e2e.ts
    - client/e2e/smoke/dictionary-export-all.spec.ts
    - client/src/pages/screendesign/use-screen-design-export.ts
    - client/src/pages/screendesign/screen-design-export.ts
    - client/test/unit/screen-design-export.test.ts
  </read_first>
  <action>
Create `client/e2e/smoke/screen-spec-authoring-export.spec.ts` with one smoke test:

1. Use `getE2EProvisioningConfig()`.
2. Provision a unique screen-spec document via `provisionCollaborationFixture(config, { pluginId: 'screen-spec', diagramName: 'Screen Spec E2E ${suffix}' })`.
3. Open the document through `openScreenSpecDocumentSession`.
4. Assert the initial screen exists.
5. Add a second screen.
6. Rename the selected screen to `Checkout ${suffix}` through `screen-spec-screen-name`.
7. Drag the first library item onto the canvas and assert instance count changes to 1.
8. Select the instance and change label override to `Primary CTA ${suffix}`.
9. Move or resize the instance using keyboard/mouse and assert inspector position or size text changes.
10. Save through the existing save action if visible; otherwise rely on the document runtime persistence path and reload/re-enter the route.
11. Re-open the same URL and assert `Checkout ${suffix}` and `Primary CTA ${suffix}` are visible.
12. Trigger PNG export and call `assertPngDownload`.
13. Trigger PDF export and call `assertPdfDownload`.
14. Attach target team/project/document annotations with `test.info().annotations.push`.

The test must fail if either PNG or PDF download is missing or structurally empty.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run test:e2e -- e2e/smoke/screen-spec-authoring-export.spec.ts --browser=chromium --workers=1 --retries=0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/e2e/smoke/screen-spec-authoring-export.spec.ts`
    - `grep -n "pluginId: 'screen-spec'" client/e2e/smoke/screen-spec-authoring-export.spec.ts`
    - `grep -n "assertPngDownload" client/e2e/smoke/screen-spec-authoring-export.spec.ts`
    - `grep -n "assertPdfDownload" client/e2e/smoke/screen-spec-authoring-export.spec.ts`
    - `grep -n "Primary CTA" client/e2e/smoke/screen-spec-authoring-export.spec.ts`
    - `cd client && npm run test:e2e -- e2e/smoke/screen-spec-authoring-export.spec.ts --browser=chromium --workers=1 --retries=0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd client && npm run test:e2e -- e2e/smoke/screen-spec-authoring-export.spec.ts --browser=chromium --workers=1 --retries=0`
2. `cd client && npm run test:unit -- screen-design`
3. `cd client && npm run build`
</verification>

<success_criteria>
- SPEC-01 has browser evidence for screen creation/rename, master placement, instance edit, and re-entry.
- SPEC-02 has at least one browser-visible inherited/override instance assertion or an explicitly linked unit assertion.
- SPEC-04 has real PNG and PDF download evidence.
</success_criteria>

<output>
After completion, update `.planning/phases/03-화면기획-플러그인/03-VALIDATION.md` task statuses and preserve evidence details for `03-VERIFICATION.md`.
</output>
