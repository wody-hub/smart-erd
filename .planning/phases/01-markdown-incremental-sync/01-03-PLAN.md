---
phase: 01-markdown-incremental-sync
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/lib/incremental-text-update.ts
  - client/src/collaboration/plugins/markdown/markdown-section-projector.ts
  - client/src/collaboration/yjs/markdown-yjs-document-adapter.ts
  - client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts
  - client/src/pages/document/use-markdown-document-session.ts
  - client/test/unit/markdown-section-update.test.ts
  - client/src/components/collaboration/RemotePendingBanner.tsx
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "단일 section 내 변경 시 setEditorBuffer가 markdown:section-update 커맨드를 발행한다"
    - "heading 추가/삭제 변경 시 markdown:body-replace fallback이 발행된다"
    - "section 경계를 넘는 변경 시 markdown:body-replace fallback이 발행된다"
    - "applyIncrementalTextUpdate가 diff 결과를 Y.Text에 정확히 반영한다"
    - "MarkdownDocumentMutationApplier의 markdown:section-update 케이스가 증분 적용 경로를 사용한다"
    - "원격 markdown:section-update 수신 시 현재 편집 중인 section과 다른 section이면 배너 없이 자동 수락한다 (D-07)"
    - "원격 markdown:section-update 수신 시 현재 편집 중인 section과 같은 section이면 3버튼 배너를 표시한다 (D-08)"
  artifacts:
    - path: "client/src/lib/incremental-text-update.ts"
      provides: "applyIncrementalTextUpdate — diff-match-patch → Y.Text 증분 적용"
      exports:
        - applyIncrementalTextUpdate
    - path: "client/src/collaboration/plugins/markdown/markdown-section-projector.ts"
      provides: "buildSectionCommands — 이전/현재 body 비교 → 발행할 커맨드 결정"
      exports:
        - buildSectionCommands
        - SectionCommand
    - path: "client/src/collaboration/yjs/markdown-yjs-document-adapter.ts"
      provides: "applySectionUpdate 메서드 추가 (기존 replaceBuffer 유지)"
    - path: "client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts"
      provides: "markdown:section-update → applySectionUpdate 경로로 교체"
    - path: "client/src/pages/document/use-markdown-document-session.ts"
      provides: "setEditorBuffer — section-aware buildSectionCommands 기반 커맨드 발행"
    - path: "client/src/components/collaboration/RemotePendingBanner.tsx"
      provides: "section-aware 자동 수락 로직 — 다른 section은 자동 수락, 같은 section은 3버튼 UI (D-07, D-08)"
  key_links:
    - from: "use-markdown-document-session.ts setEditorBuffer"
      to: "markdown-section-projector.ts buildSectionCommands"
      via: "import buildSectionCommands"
      pattern: "buildSectionCommands.*prevBody.*nextBody"
    - from: "markdown-document-mutation-applier.ts"
      to: "markdown-yjs-document-adapter.ts applySectionUpdate"
      via: "case 'markdown:section-update'"
      pattern: "applySectionUpdate"
    - from: "incremental-text-update.ts applyIncrementalTextUpdate"
      to: "yjs Y.Text"
      via: "Y.Doc.transact 내부 호출"
      pattern: "doc\\.transact.*applyIncrementalTextUpdate"
    - from: "RemotePendingBanner.tsx"
      to: "onAutoAccept callback"
      via: "remoteMutation.key === 'markdown:section-update' && sectionId !== currentSectionId"
      pattern: "autoAccept\\|auto.*accept\\|onAutoAccept"
---

<objective>
FE 증분 동기화 핵심 경로를 구현한다:
1. `applyIncrementalTextUpdate` — diff-match-patch로 Y.Text에 범위 한정 업데이트 적용
2. `buildSectionCommands` — 이전/현재 body 비교 → 발행할 커맨드(section-update vs body-replace fallback) 결정
3. `MarkdownDocumentMutationApplier` — section-update 케이스를 증분 적용 경로로 교체
4. `use-markdown-document-session.setEditorBuffer` — section-aware 커맨드 발행
5. `RemotePendingBanner` — section-aware 자동 수락 로직 (D-07, D-08)

Purpose: 전체 Y.Text delete→insert를 section 범위 한정 증분 업데이트로 전환 (D-01, D-02, D-03, D-04 결정 이행). 원격 변경이 다른 section에 적용되면 배너 없이 자동 수락하여 방해를 최소화 (D-07).
Output: 단일 section 편집 시 해당 section 범위만 Y.Text에 적용되고, 원격 section-update가 현재 편집 section과 다르면 즉시 자동 수락됨
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-markdown-incremental-sync/01-CONTEXT.md
@.planning/phases/01-markdown-incremental-sync/01-RESEARCH.md
@.planning/phases/01-markdown-incremental-sync/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 계약 — 구현 전 반드시 확인 -->

```typescript
// client/src/lib/markdown-section-index.ts (Plan 01에서 생성)
export interface SectionBoundary {
  startOffset: number;
  endOffset: number;
  id: string;
  level: number;
  text: string;
}
export function computeSectionBoundaries(body: string): SectionBoundary[];
export function findAffectedSection(
  boundaries: SectionBoundary[],
  changeStart: number,
  changeEnd: number,
): string | null;
```

```typescript
// client/src/collaboration/yjs/markdown-yjs-document-adapter.ts (기존)
// replaceBuffer(doc, buffer, origin): void — 전체 교체 (유지)
// 신규 추가:
// applySectionUpdate(doc: Y.Doc, sectionText: string, startOffset: number, endOffset: number, origin: unknown): void
```

```typescript
// markdown:section-update 커맨드 payload 구조 (RESEARCH.md)
{
  sectionId: string;      // section scope ID
  sectionText: string;    // 변경된 section 전체 텍스트
  startOffset: number;    // body 내 section 시작 offset
  endOffset: number;      // body 내 section 종료 offset
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyIncrementalTextUpdate + buildSectionCommands 구현 + 테스트 완성</name>
  <files>
    client/src/lib/incremental-text-update.ts,
    client/src/collaboration/plugins/markdown/markdown-section-projector.ts,
    client/test/unit/markdown-section-update.test.ts
  </files>
  <read_first>
    - client/src/lib/markdown-section-index.ts (Plan 01에서 생성, computeSectionBoundaries 확인)
    - .planning/phases/01-markdown-incremental-sync/01-RESEARCH.md (Pattern 2, Pitfall 3 전체)
    - client/test/unit/markdown-section-update.test.ts (Wave 1 스캐폴드 — import 주석 해제 대상)
    - client/src/collaboration/plugins/markdown/markdown-document-plugin.ts (MarkdownScopeResolver FE 버전 참고)
  </read_first>
  <behavior>
    - applyIncrementalTextUpdate Test 1: prevText='hello' nextText='hello world' → Y.Text에 ' world' insert됨
    - applyIncrementalTextUpdate Test 2: prevText='hello world' nextText='hello' → Y.Text에 ' world' delete됨
    - applyIncrementalTextUpdate Test 3: prevText=nextText → Y.Text 연산 0회 (diff empty)
    - applyIncrementalTextUpdate Test 4: 호출 후 Y.Text.toString() === nextText
    - buildSectionCommands Test 5: 단일 section 내 변경 → [{key:'markdown:section-update', payload:{sectionId, sectionText, startOffset, endOffset}}]
    - buildSectionCommands Test 6: heading '## New' 추가 (section 구조 변경) → [{key:'markdown:body-replace', payload:{buffer}}]
    - buildSectionCommands Test 7: heading '## Old' 줄 삭제 → [{key:'markdown:body-replace', payload:{buffer}}]
    - buildSectionCommands Test 8: section 경계를 넘는 변경 → [{key:'markdown:body-replace', payload:{buffer}}]
  </behavior>
  <action>
**`client/src/lib/incremental-text-update.ts`**

```typescript
import { diff_match_patch, DIFF_EQUAL, DIFF_INSERT, DIFF_DELETE } from 'diff-match-patch';
import * as Y from 'yjs';

/**
 * 이전/현재 텍스트 diff를 Y.Text 증분 연산으로 적용한다.
 *
 * IMPORTANT: 반드시 Y.Doc.transact() 내부에서 호출해야 한다.
 * 외부에서 transact로 감싸지 않으면 중간 상태가 원격에 전파된다.
 *
 * @param yText 대상 Y.Text
 * @param prevText 이전 텍스트
 * @param nextText 변경된 텍스트
 */
export function applyIncrementalTextUpdate(
  yText: Y.Text,
  prevText: string,
  nextText: string,
): void {
  if (prevText === nextText) {
    return; // diff_main 호출 불필요
  }
  const dmp = new diff_match_patch();
  const diffs = dmp.diff_main(prevText, nextText);
  dmp.diff_cleanupSemantic(diffs);

  let cursor = 0;
  for (const [op, text] of diffs) {
    if (op === DIFF_EQUAL) {
      cursor += text.length;
    } else if (op === DIFF_DELETE) {
      yText.delete(cursor, text.length);
      // delete 후 cursor 이동 없음 (삭제된 만큼 offset이 당겨짐)
    } else if (op === DIFF_INSERT) {
      yText.insert(cursor, text);
      cursor += text.length;
    }
  }
}
```

**`client/src/collaboration/plugins/markdown/markdown-section-projector.ts`**

FE에서 에디터 변경 발생 시 발행할 커맨드를 결정하는 순수 함수:

```typescript
import { computeSectionBoundaries, findAffectedSection } from '@/lib/markdown-section-index';

/** 발행할 커맨드 타입 */
export type SectionCommand =
  | {
      key: 'markdown:section-update';
      payload: {
        sectionId: string;
        sectionText: string;
        startOffset: number;
        endOffset: number;
      };
    }
  | {
      key: 'markdown:body-replace';
      payload: { buffer: string };
    };

/**
 * 이전/현재 body를 비교하여 발행할 커맨드를 결정한다.
 *
 * section 구조 변경(heading 추가/삭제/이동) 또는 section 경계를 넘는 변경은
 * markdown:body-replace fallback을 반환한다.
 *
 * @param prevBody 이전 markdown body
 * @param nextBody 현재 markdown body (frontmatter 제외)
 * @param fullBuffer 전체 markdown buffer (frontmatter 포함) — body-replace fallback용
 * @returns 발행할 커맨드 배열 (항상 1개)
 */
export function buildSectionCommands(
  prevBody: string,
  nextBody: string,
  fullBuffer: string,
): SectionCommand[] {
  const prevBoundaries = computeSectionBoundaries(prevBody);
  const nextBoundaries = computeSectionBoundaries(nextBody);

  // section 구조 변경 감지: section 수 또는 section ID 순서 변경
  const prevIds = prevBoundaries.map((b) => b.id);
  const nextIds = nextBoundaries.map((b) => b.id);
  const structureChanged =
    prevIds.length !== nextIds.length ||
    prevIds.some((id, i) => id !== nextIds[i]);

  if (structureChanged) {
    return [bodyReplaceCommand(fullBuffer)];
  }

  // diff-match-patch로 변경 offset 계산
  // 간단한 방법: nextBody의 변경 구간을 prevBody와 비교하여 findAffectedSection 사용
  // 여기서는 전체 body 비교 대신 section별 내용 비교 전략 사용:
  // — 변경된 section이 1개이면 section-update, 2개 이상이면 body-replace fallback

  let changedSectionIndex = -1;
  let changedCount = 0;
  for (let i = 0; i < prevBoundaries.length; i++) {
    const prev = prevBoundaries[i];
    const next = nextBoundaries[i];
    const prevText = prevBody.slice(prev.startOffset, prev.endOffset);
    const nextText = nextBody.slice(next.startOffset, next.endOffset);
    if (prevText !== nextText) {
      changedCount++;
      changedSectionIndex = i;
    }
  }

  if (changedCount !== 1) {
    // 변경된 section이 없거나 여러 개 → body-replace
    return [bodyReplaceCommand(fullBuffer)];
  }

  const boundary = nextBoundaries[changedSectionIndex];
  return [
    {
      key: 'markdown:section-update',
      payload: {
        sectionId: boundary.id,
        sectionText: nextBody.slice(boundary.startOffset, boundary.endOffset),
        startOffset: boundary.startOffset,
        endOffset: boundary.endOffset,
      },
    },
  ];
}

function bodyReplaceCommand(fullBuffer: string): SectionCommand {
  return { key: 'markdown:body-replace', payload: { buffer: fullBuffer } };
}
```

**테스트 파일 완성:**
`client/test/unit/markdown-section-update.test.ts`의 import 주석을 해제하고 케이스 본문을 구현한다.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run test:unit 2>&1 | grep -E "markdown-section-update|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `test -f client/src/lib/incremental-text-update.ts && echo OK`
    - applyIncrementalTextUpdate export 확인: `grep -n "export function applyIncrementalTextUpdate" client/src/lib/incremental-text-update.ts`
    - transact 주석 확인: `grep -n "transact" client/src/lib/incremental-text-update.ts`
    - buildSectionCommands export 확인: `grep -n "export function buildSectionCommands" client/src/collaboration/plugins/markdown/markdown-section-projector.ts`
    - 테스트 PASS: `cd client && npm run test:unit 2>&1 | grep "markdown-section-update"` — PASS 출력
    - diff-match-patch import 확인: `grep "diff-match-patch" client/src/lib/incremental-text-update.ts`
  </acceptance_criteria>
  <done>
    - applyIncrementalTextUpdate: diff-match-patch → Y.Text 증분 적용, 8개 테스트 케이스 GREEN
    - buildSectionCommands: section 구조 변경 감지 + 단일 section 변경 식별 로직 구현
    - markdown-section-update.test.ts 모든 케이스 GREEN
  </done>
</task>

<task type="auto">
  <name>Task 2: MutationApplier + DocumentAdapter + setEditorBuffer section-aware 교체</name>
  <files>
    client/src/collaboration/yjs/markdown-yjs-document-adapter.ts,
    client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts,
    client/src/pages/document/use-markdown-document-session.ts
  </files>
  <read_first>
    - client/src/collaboration/yjs/markdown-yjs-document-adapter.ts (기존 replaceBuffer 구현 확인)
    - client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts (markdown:section-update 케이스 현재 상태 확인)
    - client/src/pages/document/use-markdown-document-session.ts (setEditorBuffer 현재 구현 확인)
    - client/src/lib/incremental-text-update.ts (Task 1에서 생성)
    - client/src/collaboration/plugins/markdown/markdown-section-projector.ts (Task 1에서 생성)
    - .planning/phases/01-markdown-incremental-sync/01-RESEARCH.md (Pitfall 3 — transact 필수, Code Examples 섹션)
  </read_first>
  <action>
**1. `markdown-yjs-document-adapter.ts` — `applySectionUpdate` 메서드 추가**

기존 `replaceBuffer()` 유지. 신규 `applySectionUpdate()` 메서드 추가:

```typescript
/**
 * markdown:section-update 커맨드를 증분 Y.Text 적용으로 처리한다.
 *
 * @param doc 대상 Y.Doc
 * @param sectionText 변경된 section 전체 텍스트
 * @param startOffset body 내 section 시작 offset
 * @param endOffset body 내 section 종료 offset
 * @param origin Yjs transaction origin
 */
applySectionUpdate(
  doc: Y.Doc,
  sectionText: string,
  startOffset: number,
  endOffset: number,
  origin: unknown,
): void {
  const bodyText = this.getBodyText(doc);
  const currentSection = bodyText.toString().slice(startOffset, endOffset);
  if (currentSection === sectionText) {
    return; // 변경 없음
  }
  // CRITICAL: Y.Doc.transact 내부에서 applyIncrementalTextUpdate 호출 (Pitfall 3)
  doc.transact(() => {
    // section 범위만 대상으로 증분 적용
    // Y.Text 전체 기준 offset에서 section 범위만 업데이트
    const prevSection = bodyText.toString().slice(startOffset, endOffset);
    // section 내부 변경을 Y.Text에 반영: startOffset을 기준으로 local offset 적용
    const sectionBodyText = {
      delete: (index: number, length: number) => bodyText.delete(startOffset + index, length),
      insert: (index: number, content: string) => bodyText.insert(startOffset + index, content),
      toString: () => prevSection,
    };
    // applyIncrementalTextUpdate를 직접 호출하되, offset을 section 시작점 기준으로 보정한다
    // 단순 구현: section 범위 전체 교체 (전체 replace보다 효율적이나 완전한 증분은 아님)
    // 완전한 증분: applyIncrementalTextUpdate 사용 — 단, Y.Text wrapper가 필요
    if (currentSection !== sectionText) {
      if (currentSection.length > 0) {
        bodyText.delete(startOffset, currentSection.length);
      }
      if (sectionText.length > 0) {
        bodyText.insert(startOffset, sectionText);
      }
    }
  }, origin);
}
```

**주의:** 실제 diff-match-patch 기반 완전 증분 적용을 위해서는 Y.Text의 slice 범위에 대해 cursor offset을 startOffset 기준으로 보정해야 한다. `applyIncrementalTextUpdate`는 Y.Text 전체를 대상으로 하므로, section 범위에 특화된 version을 작성하거나, 다음 방식을 사용한다:

실제 구현:
```typescript
applySectionUpdate(doc, sectionText, startOffset, endOffset, origin): void {
  const bodyText = this.getBodyText(doc);
  const currentBody = bodyText.toString();
  const currentSection = currentBody.slice(startOffset, endOffset);
  if (currentSection === sectionText) return;

  const dmp = new diff_match_patch();
  const diffs = dmp.diff_main(currentSection, sectionText);
  dmp.diff_cleanupSemantic(diffs);

  doc.transact(() => {
    let cursor = startOffset; // body 전체 기준 cursor
    for (const [op, text] of diffs) {
      if (op === DIFF_EQUAL) { cursor += text.length; }
      else if (op === DIFF_DELETE) { bodyText.delete(cursor, text.length); }
      else if (op === DIFF_INSERT) { bodyText.insert(cursor, text); cursor += text.length; }
    }
  }, origin);
}
```

`diff-match-patch` import 추가: `import { diff_match_patch, DIFF_EQUAL, DIFF_INSERT, DIFF_DELETE } from 'diff-match-patch';`

**2. `markdown-document-mutation-applier.ts` — section-update 케이스 교체**

현재 `case 'markdown:section-update': return this.toApplyResult(this.applyBufferReplace(mutation));` 를 `applySectionUpdate` 호출로 교체:

```typescript
case 'markdown:section-update':
  return this.toApplyResult(this.applySectionUpdate(mutation));
```

신규 private 메서드:
```typescript
private applySectionUpdate(mutation: DocumentMutation): boolean {
  const payload = mutation.payload;
  if (
    typeof payload?.sectionId !== 'string' ||
    typeof payload?.sectionText !== 'string' ||
    typeof payload?.startOffset !== 'number' ||
    typeof payload?.endOffset !== 'number'
  ) {
    return false;
  }
  this.documentAdapter.applySectionUpdate(
    this.engine.getDocument(),
    payload.sectionText as string,
    payload.startOffset as number,
    payload.endOffset as number,
    mutation.key,
  );
  return true;
}
```

**3. `use-markdown-document-session.ts` — `setEditorBuffer` section-aware 커맨드 발행**

현재: `markdown:body-replace` 고정 발행
변경 후: `buildSectionCommands(prevBody, nextBody, nextBuffer)` 결과에 따라 커맨드 선택

```typescript
// prevBody 추적을 위한 ref 추가 (useState 사용 X — render 유발 방지)
const prevBodyRef = useRef<string>('');

const setEditorBuffer = useCallback(
  (nextBuffer: string) => {
    if (!documentMutationSession?.enabled || !collaborationReady) {
      return;
    }
    const prevBody = prevBodyRef.current;
    // parseMarkdownBuffer는 body를 추출 (frontmatter 제외)
    const nextBody = parseMarkdownBuffer(nextBuffer).body.replace(/\r\n/g, '\n');
    const commands = buildSectionCommands(prevBody, nextBody, nextBuffer);
    prevBodyRef.current = nextBody;

    for (const command of commands) {
      documentMutationSession.emitCommand(command, { origin: { source: 'local' } });
    }
  },
  [collaborationReady, documentMutationSession],
);
```

`parseMarkdownBuffer` import는 이미 존재함. `buildSectionCommands` import 추가 필요.
`prevBodyRef` 초기값: 협업 ready 시 현재 body로 초기화해야 함 — `setCollaborationReady(true)` 직전 또는 `subscribeDocumentChanges` 콜백에서 `prevBodyRef.current = parseMarkdownBuffer(readSerializedBuffer()).body` 갱신.

**코어 수정 제로 원칙 준수:** DocumentStore, ChangeBus, CollaborationRuntime 등 core 모듈 수정 금지.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - applySectionUpdate 메서드 추가 확인: `grep -n "applySectionUpdate" client/src/collaboration/yjs/markdown-yjs-document-adapter.ts`
    - MutationApplier 케이스 교체 확인: `grep -n "applySectionUpdate" client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts`
    - setEditorBuffer 변경 확인: `grep -n "buildSectionCommands" client/src/pages/document/use-markdown-document-session.ts`
    - prevBodyRef 추가 확인: `grep -n "prevBodyRef" client/src/pages/document/use-markdown-document-session.ts`
    - applyBufferReplace 위임 제거 확인: `grep -A2 "section-update" client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts | grep -v "applyBufferReplace"` — applyBufferReplace 없음
    - 빌드 성공: `cd client && npm run build 2>&1 | grep -c "error"` → 0
    - 단위 테스트 통과: `cd client && npm run test:unit 2>&1 | grep -E "markdown-section-update"` — PASS
  </acceptance_criteria>
  <done>
    - markdown-yjs-document-adapter.ts: applySectionUpdate 메서드 추가, Y.Doc.transact 내부 diff 적용
    - markdown-document-mutation-applier.ts: section-update → applySectionUpdate 경로 (applyBufferReplace 위임 제거)
    - use-markdown-document-session.ts: setEditorBuffer가 buildSectionCommands 기반으로 커맨드 선택
    - npm run build 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 3: RemotePendingBanner section-aware 자동 수락 로직 추가 (D-07, D-08)</name>
  <files>client/src/components/collaboration/RemotePendingBanner.tsx</files>
  <read_first>
    - client/src/components/collaboration/RemotePendingBanner.tsx (전체 — 현재 3버튼 UI 구조 파악)
    - client/src/pages/document/use-markdown-document-session.ts (currentSectionId 또는 cursor 위치 제공 방식 파악)
    - .planning/phases/01-markdown-incremental-sync/01-CONTEXT.md (D-07, D-08 결정 확인)
  </read_first>
  <action>
**목적 (D-07, D-08):**
- 원격 `markdown:section-update` 수신 시, 변경된 `sectionId`가 현재 편집 중인 section과 **다르면** → 배너 없이 자동 수락
- 변경된 `sectionId`가 현재 편집 중인 section과 **같으면** → 기존 3버튼 UI(수락/거절/병합) 표시

**구현 방식:**

`RemotePendingBanner.tsx`가 현재 편집 중인 section ID를 알 수 있도록 `activeSectionId` prop을 추가한다 (부모가 전달). 원격 mutation의 key가 `'markdown:section-update'`이고 payload의 `sectionId !== activeSectionId`이면 즉시 `onAccept()`를 호출하고 배너를 렌더링하지 않는다.

```typescript
/**
 * 현재 편집 중인 section ID.
 * undefined이면 section-aware 판단을 건너뛰고 항상 배너를 표시한다 (비마크다운 문서 호환).
 */
activeSectionId?: string;
```

컴포넌트 내부 추가 로직 (JSX 렌더링 전):

```typescript
// D-07: 원격 변경이 다른 section이면 배너 없이 자동 수락
useEffect(() => {
  if (
    remoteMutation?.key === 'markdown:section-update' &&
    activeSectionId !== undefined &&
    remoteMutation.payload?.sectionId !== activeSectionId
  ) {
    onAccept();
  }
}, [remoteMutation, activeSectionId, onAccept]);
```

**중요 사항:**
- `useEffect`의 의존성 배열을 정확히 지정한다 (`remoteMutation`, `activeSectionId`, `onAccept`).
- `onAccept`는 `useCallback`으로 메모이제이션된 함수로 전달받아야 무한 루프를 방지한다 (호출 측 책임).
- `activeSectionId`가 `undefined`이면 기존 동작 유지 (비마크다운 문서 하위 호환).
- 자동 수락 시에는 JSX를 `null` 반환하거나, useEffect가 실행될 때까지의 1-프레임 깜빡임을 방지하기 위해 조건부 렌더링으로 `null`을 반환한다:

```typescript
// 자동 수락 대상이면 렌더링하지 않음 (D-07)
if (
  remoteMutation?.key === 'markdown:section-update' &&
  activeSectionId !== undefined &&
  remoteMutation.payload?.sectionId !== activeSectionId
) {
  return null;
}
```

**`activeSectionId` 공급처:** `use-markdown-document-session.ts`에서 현재 커서가 위치한 section ID를 추적하는 `currentSectionId` 상태(또는 ref)를 추가하고, 이를 `RemotePendingBanner`의 부모 컴포넌트(`MarkdownDocumentPage` 또는 해당 컨테이너)에서 전달한다.

**코어 수정 제로 원칙 준수:** ChangeBus, DocumentStore 수정 금지. props 전달로만 해결.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - activeSectionId prop 추가 확인: `grep -n "activeSectionId" client/src/components/collaboration/RemotePendingBanner.tsx`
    - section-update 자동 수락 조건 확인: `grep -n "markdown:section-update" client/src/components/collaboration/RemotePendingBanner.tsx`
    - null 반환 조건 확인: `grep -n "return null" client/src/components/collaboration/RemotePendingBanner.tsx`
    - 빌드 성공: `cd client && npm run build 2>&1 | grep -c "error"` → 0
  </acceptance_criteria>
  <done>
    - RemotePendingBanner: activeSectionId prop 추가 (undefined이면 하위 호환 유지)
    - D-07: remoteMutation.key === 'markdown:section-update' && sectionId !== activeSectionId → null 반환 + onAccept 호출
    - D-08: 같은 section 충돌 시 기존 3버튼 UI 표시 유지
    - npm run build 오류 없음
  </done>
</task>

</tasks>

<verification>
Phase 1 Plan 03 완료 기준:

1. `grep -n "applySectionUpdate" client/src/collaboration/yjs/markdown-yjs-document-adapter.ts` — 출력 있음
2. `grep -n "buildSectionCommands" client/src/pages/document/use-markdown-document-session.ts` — 출력 있음
3. `grep -A2 "'markdown:section-update'" client/src/collaboration/plugins/markdown/markdown-document-mutation-applier.ts | grep "applySectionUpdate"` — 출력 있음
4. `cd client && npm run test:unit 2>&1 | grep "markdown-section-update"` — PASS
5. `cd client && npm run build 2>&1 | grep -c "error"` → 0
6. `grep -n "activeSectionId" client/src/components/collaboration/RemotePendingBanner.tsx` — 출력 있음
7. `grep -n "markdown:section-update" client/src/components/collaboration/RemotePendingBanner.tsx` — 출력 있음
</verification>

<success_criteria>
- applyIncrementalTextUpdate: diff-match-patch → Y.Text 증분 적용 (transact 내부 보장)
- buildSectionCommands: 구조 변경/다수 section 변경 → body-replace, 단일 section → section-update
- MutationApplier: markdown:section-update → applySectionUpdate 경로 (applyBufferReplace 위임 제거됨)
- setEditorBuffer: buildSectionCommands 기반 section-aware 커맨드 발행
- RemotePendingBanner: 다른 section 원격 변경 → 자동 수락(D-07), 같은 section → 3버튼 UI(D-08)
- npm run build 오류 없음, 단위 테스트 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-markdown-incremental-sync/01-03-SUMMARY.md`
</output>
