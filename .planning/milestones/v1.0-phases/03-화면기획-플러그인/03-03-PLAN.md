---
phase: 03-화면기획-플러그인
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .planning/phases/03-화면기획-플러그인/03-VALIDATION.md
  - .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md
  - .planning/phases/03-화면기획-플러그인/SUMMARY.md
autonomous: false
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04

must_haves:
  truths:
    - "D-01: `03-VERIFICATION.md` records both automated test-profile evidence and dev-profile manual browser QA"
    - "D-02: `03-VALIDATION.md` and `03-VERIFICATION.md` are required closeout outputs"
    - "D-03: each of SPEC-01, SPEC-02, SPEC-03, and SPEC-04 must have at least one concrete evidence item before completion"
    - "D-10: `ScreenSpecCollaborationPlugin.validationHook()` no-op remains documented as intentional v1 policy"
    - "D-11: no TODO/backlog/backend structure-validation implementation is added for v1 closeout"
    - "D-12: validation rationale cites frontend model normalization, mutation policy/applier tests, scope resolver tests, and E2E/browser evidence"
    - "D-13: artifact flow remains `03-CONTEXT.md` -> `$gsd-plan-phase 3` -> closeout execution -> `03-VALIDATION.md`/`03-VERIFICATION.md`"
    - "D-14: `SUMMARY.md` is updated after closeout and links to canonical evidence in `03-VALIDATION.md` and `03-VERIFICATION.md`"
    - "03-VALIDATION.md maps SPEC-01, SPEC-02, SPEC-03, SPEC-04 to concrete evidence"
    - "03-VERIFICATION.md records exact automated commands, profiles, target URLs/ports, browser QA notes, and pass/fail results"
    - "SUMMARY.md status is updated only after evidence exists and links to 03-VALIDATION.md and 03-VERIFICATION.md"
    - "If PNG or PDF export fails, SPEC-04 remains incomplete"
    - "If three-account lock/conflict UX fails, SPEC-03 remains incomplete"
    - "If dev-profile manual browser QA cannot actually be performed, closeout remains incomplete even when automated test-profile commands pass"
    - "No task may set `nyquist_compliant: true`, mark `SUMMARY.md` Complete, or mark SPEC statuses complete unless automated evidence and dev-profile manual QA both pass"
    - "ScreenSpecCollaborationPlugin.validationHook() no-op remains documented as intentional v1 policy"
  artifacts:
    - path: ".planning/phases/03-화면기획-플러그인/03-VALIDATION.md"
      provides: "requirement evidence matrix and validation policy"
    - path: ".planning/phases/03-화면기획-플러그인/03-VERIFICATION.md"
      provides: "commands, browser QA, and evidence log"
    - path: ".planning/phases/03-화면기획-플러그인/SUMMARY.md"
      provides: "final Phase 3 closeout summary"
---

<objective>
Convert Phase 3 execution evidence into canonical closeout artifacts. This plan is intentionally `autonomous: false` because dev-profile manual browser QA is mandatory; it records whether SPEC-01 through SPEC-04 are actually proven and leaves the phase incomplete when automated evidence or manual QA is missing.

Purpose: complete the GSD closeout record for Phase 3.
Output:
- finalized `03-VALIDATION.md`
- new `03-VERIFICATION.md`
- final `SUMMARY.md`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-화면기획-플러그인/03-CONTEXT.md
@.planning/phases/03-화면기획-플러그인/03-RESEARCH.md
@.planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
@.planning/phases/03-화면기획-플러그인/03-VALIDATION.md
@.planning/phases/03-화면기획-플러그인/SUMMARY.md
@client/e2e/smoke/screen-spec-authoring-export.spec.ts
@client/e2e/smoke/screen-spec-three-account-collaboration.spec.ts
@src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecCollaborationPlugin.java
</context>

<threat_model>
T-03-07: Documentation marks Phase 3 complete despite missing browser evidence.
Mitigation: `03-VALIDATION.md` and `SUMMARY.md` must leave any requirement incomplete when its command/manual evidence is absent or failed.

T-03-08: Manual QA evidence is ambiguous and cannot be reproduced.
Mitigation: `03-VERIFICATION.md` must record exact profile, ports, URL, account labels, document ID/name, and steps performed.

T-03-09: Backend validation no-op is misread as an accidental TODO.
Mitigation: document the no-op policy with rationale and supporting evidence; do not add TODO/backlog language.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: run and record targeted automated verification</name>
  <files>
    .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/03-화면기획-플러그인/03-VALIDATION.md
    - client/package.json
    - README.md
    - client/e2e/shared/diagram-e2e.ts
  </read_first>
  <action>
Create or update `03-VERIFICATION.md` with an automated verification section. Run these commands in the current execution environment; if a command is not runnable, record the exact blocker and keep the related requirement incomplete. Do not write "run or record" as a substitute for execution.

1. `./gradlew test --tests '*ScreenSpecScopeResolverTest' --tests '*DiagramServiceTest'`
2. `cd client && npm run test:unit -- screen-spec screen-design`
3. `cd client && npm run test:e2e -- e2e/smoke/screen-spec-authoring-export.spec.ts --browser=chromium --workers=1 --retries=0`
4. `cd client && npm run test:e2e -- e2e/smoke/screen-spec-three-account-collaboration.spec.ts --browser=chromium --workers=1 --retries=0`
5. `cd client && npm run build`
6. `git diff --check`

For each command, record:
- date/time
- profile or target endpoints used
- pass/fail
- relevant output summary
- Playwright report path if generated
- trace/video/screenshot path if generated
- downloaded PNG filename, byte size, and PNG signature result
- downloaded PDF filename, byte size, `%PDF` signature result, and page marker/structural sanity result
- document ID/name used in the E2E fixture

If a command cannot run because servers are down, record the blocker and do not mark related SPEC evidence complete.
  </action>
  <verify>
    <manual>Open `.planning/phases/03-화면기획-플러그인/03-VERIFICATION.md` and confirm all five command rows have pass/fail status.</manual>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "ScreenSpecScopeResolverTest" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "screen-spec-authoring-export" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "screen-spec-three-account-collaboration" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "npm run build" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "git diff --check" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "Playwright report\\|trace\\|screenshot\\|video" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "PNG.*bytes\\|PDF.*bytes\\|%PDF" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: dev-profile manual browser QA evidence</name>
  <files>
    .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md
  </files>
  <read_first>
    - README.md
    - .planning/phases/03-화면기획-플러그인/03-CONTEXT.md
    - .planning/phases/03-화면기획-플러그인/03-UI-SPEC.md
  </read_first>
  <action>
Using the dev-profile servers `http://127.0.0.1:4503` and `http://localhost:9503` when available, perform manual browser QA and record it in `03-VERIFICATION.md`:

1. Open or create a screen-spec document.
2. Add/rename a screen.
3. Place a master instance.
4. Move or resize the instance.
5. Save/re-enter and verify persistence.
6. Run a three-account or three-context collaboration check if dev accounts are available.
7. Export PNG and PDF from the browser.
8. Record account labels, document name/ID, URL, screenshots or notes, and pass/fail status.

If dev accounts or browser access are unavailable, record the exact blocker, keep manual QA unchecked, keep `nyquist_compliant: false`, and keep `SUMMARY.md` out of Complete status.
  </action>
  <verify>
    <manual>`03-VERIFICATION.md` contains a "Dev-profile Manual QA" section with URL, profile, account labels, and pass/fail rows.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "Dev-profile Manual QA" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "4503\\|9503" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "PNG\\|PDF" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
    - `grep -n "3계정\\|three-account\\|three context" .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: finalize validation matrix and no-op validation policy</name>
  <files>
    .planning/phases/03-화면기획-플러그인/03-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/03-화면기획-플러그인/03-VALIDATION.md
    - .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md
    - .planning/phases/03-화면기획-플러그인/03-CONTEXT.md
    - src/main/java/com/smarterd/domain/diagram/collaboration/ScreenSpecCollaborationPlugin.java
  </read_first>
  <action>
Update `03-VALIDATION.md` from draft strategy to evidence matrix:

1. For SPEC-01, link to authoring/export E2E evidence row proving document open, screen create/rename, instance placement, save/re-entry.
2. For SPEC-02, link to master/instance propagation evidence from E2E or unit tests.
3. For SPEC-03, link to three-account collaboration evidence and lock/conflict UX result.
4. For SPEC-04, link to PNG and PDF download evidence separately.
5. Set task statuses to green only where commands and manual QA passed.
6. Keep `nyquist_compliant: false` unless all four SPEC rows have concrete passing automated evidence and dev-profile manual QA is complete.
7. Preserve the DomainValidationHook no-op policy and explicitly state it is intentional for v1 closeout.
8. State whether `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` statuses are updated in this phase. If not updated, record that traceability status updates are deferred to `$gsd-verify-work` or milestone audit and do not silently imply they changed.
  </action>
  <verify>
    <manual>Review `03-VALIDATION.md` and confirm SPEC-01 through SPEC-04 each have evidence or a failing/blocker status.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "SPEC-01" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "SPEC-02" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "SPEC-03" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "SPEC-04" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "validationHook.*no-op\\|no-op" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "PNG" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "PDF" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
    - `grep -n "REQUIREMENTS.md\\|ROADMAP.md\\|traceability" .planning/phases/03-화면기획-플러그인/03-VALIDATION.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: update final Phase 3 summary</name>
  <files>
    .planning/phases/03-화면기획-플러그인/SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/03-화면기획-플러그인/SUMMARY.md
    - .planning/phases/03-화면기획-플러그인/03-VALIDATION.md
    - .planning/phases/03-화면기획-플러그인/03-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Update `SUMMARY.md`:

1. Change `Updated:` to the execution date.
2. Set status based on evidence:
   - `Complete` only if SPEC-01 through SPEC-04 all have passing automated evidence and dev-profile manual QA is passed.
   - Otherwise keep `검증/마감 진행 중` and list exact blockers.
3. Add links to:
   - `03-VALIDATION.md`
   - `03-VERIFICATION.md`
   - E2E spec files created in plans 03-01 and 03-02.
4. Update the Requirement Status table so each SPEC row names the actual evidence.
5. Preserve explicit non-goals and no-op validation hook policy.
  </action>
  <verify>
    <manual>Review `SUMMARY.md`; it must not claim complete if any mandatory evidence is missing.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "03-VALIDATION.md" .planning/phases/03-화면기획-플러그인/SUMMARY.md`
    - `grep -n "03-VERIFICATION.md" .planning/phases/03-화면기획-플러그인/SUMMARY.md`
    - `grep -n "screen-spec-authoring-export" .planning/phases/03-화면기획-플러그인/SUMMARY.md`
    - `grep -n "screen-spec-three-account-collaboration" .planning/phases/03-화면기획-플러그인/SUMMARY.md`
    - `grep -n "SPEC-01\\|SPEC-02\\|SPEC-03\\|SPEC-04" .planning/phases/03-화면기획-플러그인/SUMMARY.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Review `03-VALIDATION.md` for SPEC evidence matrix.
2. Review `03-VERIFICATION.md` for command/manual QA evidence.
3. Review `SUMMARY.md` status against evidence.
</verification>

<success_criteria>
- No requirement is marked complete without passing automated evidence and passed dev-profile manual QA.
- `03-VERIFICATION.md` contains automated and dev-profile manual QA sections.
- `SUMMARY.md` links to the canonical validation and verification artifacts.
- `git diff --check` is recorded in verification evidence.
</success_criteria>

<output>
After completion, Phase 3 is ready for `$gsd-verify-work 3` or final milestone audit.
</output>
