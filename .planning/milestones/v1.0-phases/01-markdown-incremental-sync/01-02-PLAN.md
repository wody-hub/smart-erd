---
phase: 01-markdown-incremental-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java
  - src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "백엔드에서 markdown:section-update 커맨드가 section/'{id}' scope로 해석된다"
    - "markdown:body-replace 커맨드는 document/root EXCLUSIVE scope로 해석된다"
    - "`pluginId = 'markdown'`인 채널에서 MarkdownCollaborationPlugin의 scopeResolver가 사용된다"
  artifacts:
    - path: "src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java"
      provides: "markdown command → section/{id} 또는 document/root scope 해석"
      contains: "SECTION_UPDATE_KEY = \"markdown:section-update\""
    - path: "src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java"
      provides: "BaseCollaborationPlugin 구현 — markdown pluginId, MarkdownScopeResolver 제공"
      contains: "implements BaseCollaborationPlugin"
  key_links:
    - from: "MarkdownCollaborationPlugin"
      to: "CollaborationPluginRegistry"
      via: "@Component Spring Bean 등록"
      pattern: "@Component"
    - from: "MarkdownScopeResolver.resolve()"
      to: "ScopeRef(\"section\", sectionId, ScopeLockMode.EXCLUSIVE)"
      via: "markdown:section-update 케이스"
      pattern: "ScopeRef.*section.*EXCLUSIVE"
---

<objective>
백엔드에 `MarkdownScopeResolver`와 `MarkdownCollaborationPlugin`을 신규 구현한다.
`markdown:section-update` 커맨드를 `section/{id}` 단위 scope로 해석하여, 다른 section을 편집하는 사용자 간 scope가 분리된다.

Purpose: FE가 발행하는 `markdown:section-update` 커맨드를 BE가 section-level scope로 해석해야 다른 section 동시 편집 시 충돌 없이 독립 동작한다 (D-04 결정).
Output:
- `MarkdownScopeResolver.java` — command key → ScopeRef 해석
- `MarkdownCollaborationPlugin.java` — BaseCollaborationPlugin 구현, @Component 등록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-markdown-incremental-sync/01-CONTEXT.md
@.planning/phases/01-markdown-incremental-sync/01-RESEARCH.md

<interfaces>
<!-- 기존 BE 계약 — 구현 전 반드시 파악 -->

```java
// src/main/java/com/smarterd/collaboration/plugin/ScopeResolver.java
public interface ScopeResolver {
    @NonNull
    Collection<ScopeRef> resolve(@NonNull String commandKey, @Nullable Map<String, ?> payload);
}

// src/main/java/com/smarterd/collaboration/plugin/ScopeRef.java
public record ScopeRef(@NonNull String kind, @NonNull String id, @NonNull ScopeLockMode mode) {}

// src/main/java/com/smarterd/collaboration/plugin/ScopeLockMode.java
public enum ScopeLockMode { SHARED("shared"), EXCLUSIVE("exclusive"); }

// src/main/java/com/smarterd/collaboration/plugin/BaseCollaborationPlugin.java
public interface BaseCollaborationPlugin {
    @NonNull String pluginId();
    int schemaVersion();
    @NonNull Set<String> supportedEngineIds();
    @NonNull ScopeResolver scopeResolver();
    @NonNull DomainValidationHook validationHook();
}

// src/main/java/com/smarterd/collaboration/plugin/DomainValidationHook.java
// (읽어서 no-op 구현 패턴 파악 필요)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarkdownScopeResolver + MarkdownCollaborationPlugin 구현</name>
  <files>
    src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java,
    src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java
  </files>
  <read_first>
    - src/main/java/com/smarterd/collaboration/plugin/ScopeResolver.java
    - src/main/java/com/smarterd/collaboration/plugin/ScopeRef.java
    - src/main/java/com/smarterd/collaboration/plugin/ScopeLockMode.java
    - src/main/java/com/smarterd/collaboration/plugin/BaseCollaborationPlugin.java
    - src/main/java/com/smarterd/collaboration/plugin/DomainValidationHook.java
    - src/main/java/com/smarterd/domain/diagram/collaboration/DiagramCollaborationChannelPlugin.java (패턴 참고)
    - src/main/java/com/smarterd/collaboration/plugin/CollaborationPluginRegistry.java
    - .planning/phases/01-markdown-incremental-sync/01-RESEARCH.md (Pattern 4 전체)
  </read_first>
  <behavior>
    - Test 1: resolve("markdown:section-update", Map.of("sectionId", "api-설계")) → [ScopeRef("section", "api-설계", EXCLUSIVE)]
    - Test 2: resolve("markdown:section-update", Map.of("sectionId", "")) → [ScopeRef("document", "root", EXCLUSIVE)] (빈 sectionId는 root fallback)
    - Test 3: resolve("markdown:section-update", null) → [ScopeRef("document", "root", EXCLUSIVE)]
    - Test 4: resolve("markdown:body-replace", ...) → [ScopeRef("document", "root", EXCLUSIVE)]
    - Test 5: resolve("markdown:frontmatter-update", ...) → [ScopeRef("document", "root", EXCLUSIVE)]
    - Test 6: resolve("unknown:command", ...) → [ScopeRef("document", "root", EXCLUSIVE)]
    - Test 7: MarkdownCollaborationPlugin.pluginId() → "markdown"
    - Test 8: MarkdownCollaborationPlugin.supportedEngineIds() → Set.of("yjs")
  </behavior>
  <action>
**`MarkdownScopeResolver.java`** — RESEARCH.md Pattern 4를 기반으로 구현:

```java
package com.smarterd.domain.diagram.collaboration;

import com.smarterd.collaboration.plugin.ScopeRef;
import com.smarterd.collaboration.plugin.ScopeLockMode;
import com.smarterd.collaboration.plugin.ScopeResolver;
import java.util.Collection;
import java.util.List;
import java.util.Map;
import org.springframework.lang.NonNull;
import org.springframework.lang.Nullable;

/**
 * markdown document command를 section/document scope로 해석한다.
 *
 * <p>markdown:section-update는 sectionId 기준 section scope로 해석하여
 * 다른 section을 편집하는 사용자 간 scope를 분리한다.</p>
 */
public class MarkdownScopeResolver implements ScopeResolver {

    private static final String SECTION_UPDATE_KEY = "markdown:section-update";
    private static final String BODY_REPLACE_KEY = "markdown:body-replace";
    private static final String FRONTMATTER_UPDATE_KEY = "markdown:frontmatter-update";

    @Override
    @NonNull
    public Collection<ScopeRef> resolve(@NonNull String commandKey, @Nullable Map<String, ?> payload) {
        return switch (commandKey) {
            case SECTION_UPDATE_KEY -> {
                final var sectionId = payload != null ? payload.get("sectionId") : null;
                if (!(sectionId instanceof String sid) || sid.isBlank()) {
                    yield List.of(rootScope());
                }
                yield List.of(new ScopeRef("section", sid, ScopeLockMode.EXCLUSIVE));
            }
            case BODY_REPLACE_KEY, FRONTMATTER_UPDATE_KEY -> List.of(rootScope());
            default -> List.of(rootScope());
        };
    }

    private static ScopeRef rootScope() {
        return new ScopeRef("document", "root", ScopeLockMode.EXCLUSIVE);
    }
}
```

**`MarkdownCollaborationPlugin.java`** — BaseCollaborationPlugin 구현, @Component 등록:

```java
package com.smarterd.domain.diagram.collaboration;

import com.smarterd.collaboration.plugin.BaseCollaborationPlugin;
import com.smarterd.collaboration.plugin.DomainValidationHook;
import com.smarterd.collaboration.plugin.ScopeResolver;
import java.util.Set;
import org.springframework.lang.NonNull;
import org.springframework.stereotype.Component;

/**
 * markdown 문서 플러그인.
 *
 * <p>section-update 커맨드를 section-level scope로 해석하는
 * MarkdownScopeResolver를 제공한다.</p>
 */
@Component
public class MarkdownCollaborationPlugin implements BaseCollaborationPlugin {

    private static final String PLUGIN_ID = "markdown";
    private static final int SCHEMA_VERSION = 1;
    private static final Set<String> SUPPORTED_ENGINE_IDS = Set.of("yjs");

    private final MarkdownScopeResolver scopeResolver;
    private final DomainValidationHook validationHook;

    /**
     * 기본 생성자.
     *
     * @param scopeResolver markdown command scope resolver
     * @param validationHook 도메인 검증 훅
     */
    public MarkdownCollaborationPlugin(
            MarkdownScopeResolver scopeResolver,
            DomainValidationHook validationHook) {
        this.scopeResolver = scopeResolver;
        this.validationHook = validationHook;
    }

    @Override
    @NonNull
    public String pluginId() {
        return PLUGIN_ID;
    }

    @Override
    public int schemaVersion() {
        return SCHEMA_VERSION;
    }

    @Override
    @NonNull
    public Set<String> supportedEngineIds() {
        return SUPPORTED_ENGINE_IDS;
    }

    @Override
    @NonNull
    public ScopeResolver scopeResolver() {
        return scopeResolver;
    }

    @Override
    @NonNull
    public DomainValidationHook validationHook() {
        return validationHook;
    }
}
```

**중요:** DomainValidationHook 인터페이스를 먼저 읽어 no-op 구현 패턴 또는 기존 빈을 확인한다. 만약 DomainValidationHook이 인터페이스라면 no-op 내부 클래스 또는 람다로 대체하거나, 기존 no-op 구현 빈이 있으면 주입받는다.

**Sonar 준수:**
- 모든 switch 케이스에 default 포함 (이미 포함됨)
- List.of() 사용 (Collections.emptyList() 금지)
- 미사용 import 없음
- @SuppressWarnings("null") src/main/ 금지

테스트 파일 위치: `src/test/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolverTest.java`
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd && ./gradlew test --tests "com.smarterd.domain.diagram.collaboration.MarkdownScopeResolverTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `test -f src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java && echo OK`
    - 파일 존재: `test -f src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java && echo OK`
    - @Component 어노테이션 확인: `grep "@Component" src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java`
    - section scope 구현 확인: `grep -n "section.*EXCLUSIVE\|EXCLUSIVE.*section" src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java`
    - SECTION_UPDATE_KEY 상수 확인: `grep "markdown:section-update" src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java`
    - 컴파일 성공: `./gradlew compileJava 2>&1 | grep -E "ERROR|error:" | grep -v "Note:"` — 결과 없음
    - 단위 테스트 PASS: `./gradlew test --tests "com.smarterd.domain.diagram.collaboration.MarkdownScopeResolverTest"` — BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>
    - MarkdownScopeResolver: section-update → section/{id} EXCLUSIVE, 그 외 → document/root EXCLUSIVE
    - MarkdownCollaborationPlugin: pluginId="markdown", supportedEngineIds={"yjs"}, @Component 등록
    - MarkdownScopeResolverTest: 8개 케이스 모두 GREEN
    - ./gradlew compileJava 오류 없음
  </done>
</task>

</tasks>

<verification>
Phase 1 Plan 02 완료 기준:

1. `./gradlew compileJava 2>&1 | grep -c "error:"` → 0
2. `./gradlew test --tests "com.smarterd.domain.diagram.collaboration.MarkdownScopeResolverTest" 2>&1 | grep "BUILD SUCCESSFUL"`
3. `grep -n "section.*EXCLUSIVE" src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownScopeResolver.java` — 출력 있음
4. `grep "@Component" src/main/java/com/smarterd/domain/diagram/collaboration/MarkdownCollaborationPlugin.java` — 출력 있음
</verification>

<success_criteria>
- MarkdownScopeResolver: markdown:section-update → ScopeRef("section", sectionId, EXCLUSIVE) 반환
- MarkdownCollaborationPlugin: @Component, pluginId="markdown", supportedEngineIds={"yjs"}
- 단위 테스트 8개 케이스 GREEN
- ./gradlew compileJava + test 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/01-markdown-incremental-sync/01-02-SUMMARY.md`
</output>

## Review Feedback (Gemini)

### [MEDIUM] `computeSectionBoundaries` 파싱 부하

**우려:** 매 `setEditorBuffer` 호출(debounce 300ms)마다 전체 body에 대해 `computeSectionBoundaries`가 실행된다. 수만 라인 대형 문서에서 메인 스레드 지연 가능성이 있다.

**제안:** 결과 캐싱(prevBody 동일 시 이전 결과 재사용) 또는 변경 부분 근처만 부분 파싱.

**판정: Phase 1 scope -- 구현 태스크에 반영**

이 Plan(01-02)은 BE ScopeResolver만 다루므로 직접 해당되지 않으나, `computeSectionBoundaries`를 호출하는 FE 코드(01-03의 `buildSectionCommands`)에서 캐싱 전략을 적용해야 한다. 구체적으로:
- `buildSectionCommands` 내부에서 `prevBody`가 동일하면 이전 `prevBoundaries` 결과를 재사용하는 메모이제이션 추가
- 최소 구현: `prevBody` 해시 기반 1-entry 캐시 (LRU 불필요, 직전 값만 보관)
- 01-03 실행 시 Task 1 action에 이 캐싱을 포함할 것
