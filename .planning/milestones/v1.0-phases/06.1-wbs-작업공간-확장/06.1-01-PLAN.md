---
phase: 06.1-wbs-작업공간-확장
plan: 01
type: execute
wave: 1
depends_on:
  - 06
files_modified:
  - client/src/constants/routes.ts
  - client/src/App.tsx
  - client/src/pages/diagram/DiagramsPage.tsx
  - client/src/pages/project/ProjectWbsPage.tsx
  - client/src/components/wbs/WbsTab.tsx
  - client/src/components/wbs/WbsWorkspaceContent.tsx
  - client/src/components/wbs/WbsInlineCreateRow.tsx
  - client/src/components/wbs/WbsItemFormDialog.tsx
  - client/src/components/wbs/SortableWbsRow.tsx
  - client/src/components/wbs/wbs-tree-utils.ts
  - client/src/i18n/locales/ko/translation.json
  - client/src/i18n/locales/en/translation.json
  - client/test/unit/wbs-inline-create.test.ts
autonomous: true
requirements:
  - WBS-02
  - WBS-06
  - WBS-07

must_haves:
  truths:
    - "planning package (`ROADMAP.md`, `06.1-CONTEXT.md`, `06.1-UI-SPEC.md`, `06.1-REVIEWS.md`)는 Phase 6.1이 `WBS-02`, `WBS-06`, `WBS-07`의 owner이며 다음 단계가 `$gsd:execute-phase 6.1`이라는 점에 일치한다"
    - "현재 `DiagramsPage`의 `wbs` 탭은 유지되고, 사용자는 visible CTA로 dedicated WBS workspace route로 이동할 수 있다"
    - "dedicated route는 `/teams/:teamId/projects/:projectId/wbs`이며 `App.tsx`와 `ROUTES`에 정식 등록된다"
    - "전용 WBS 페이지는 `max-w-none` 기반 full-width workspace지만 decorative preview card처럼 보이지 않는다"
    - "WBS query/mutation, dialog, DnD, milestone rail의 canonical data flow는 하나로 유지되고 tab/page가 공용 content를 재사용한다"
    - "assignee 편집은 신규 API 없이 기존 `createWbsItem`/`updateWbsItem` payload의 `assigneeUserId`를 사용한다"
    - "assignee option source는 `fetchMembers(teamId)` + `queryKeys.teams.members(teamId)`이며 팀 외 사용자 노출은 없다"
    - "WBS table은 `Item / Assignee / Period / Progress / Est. M/M / Milestone / Actions` semantic column order를 공유한다"
    - "quick-add row는 dedicated workspace에서만 보이고, compact hub tab과 dialog에는 나타나지 않는다"
    - "quick-add create는 `name` + `parentId`만 받아 기존 POST 경로를 사용하며 다른 필드는 null/default로 둔다"
    - "quick-add 성공 후에는 같은 hierarchy input에 focus가 유지되고 append 반복이 가능하다"
    - "member fetch 실패가 assignee field만 제한할 수는 있어도 dialog 전체 create/edit flow를 막아서는 안 된다"
  artifacts:
    - path: "client/src/pages/project/ProjectWbsPage.tsx"
      provides: "dedicated WBS workspace page shell + top band"
    - path: "client/src/components/wbs/WbsWorkspaceContent.tsx"
      provides: "tab/page shared WBS query, mutations, dialog, table, milestone rail"
    - path: "client/src/components/wbs/WbsInlineCreateRow.tsx"
      provides: "root/child footer quick-add UX"
    - path: "client/src/components/wbs/WbsItemFormDialog.tsx"
      provides: "assignee select 포함 full editor"
    - path: "client/src/components/wbs/wbs-tree-utils.ts"
      provides: "quick-add placement helper and tree ordering reuse"
    - path: "client/test/unit/wbs-inline-create.test.ts"
      provides: "inline append placement/helper regression tests"
  key_links:
    - from: "client/src/pages/diagram/DiagramsPage.tsx"
      to: "client/src/pages/project/ProjectWbsPage.tsx"
      via: "Open workspace CTA / route navigation"
      pattern: "ROUTES.PROJECT_WBS|navigate("
    - from: "client/src/pages/project/ProjectWbsPage.tsx"
      to: "client/src/components/wbs/WbsWorkspaceContent.tsx"
      via: "page shell -> shared authoring body"
      pattern: "<WbsWorkspaceContent"
    - from: "client/src/components/wbs/WbsTab.tsx"
      to: "client/src/components/wbs/WbsWorkspaceContent.tsx"
      via: "compact tab -> shared authoring body"
      pattern: "<WbsWorkspaceContent"
    - from: "client/src/components/wbs/WbsWorkspaceContent.tsx"
      to: "client/src/components/wbs/WbsInlineCreateRow.tsx"
      via: "dedicated-only footer quick-add rows"
      pattern: "<WbsInlineCreateRow"
    - from: "client/src/components/wbs/WbsWorkspaceContent.tsx"
      to: "client/src/api/teamApi.ts"
      via: "team member query for assignee select"
      pattern: "fetchMembers|queryKeys.teams.members"
---

<objective>
현재 compact WBS 탭은 유지하면서 더 넓은 dedicated WBS workspace route를 추가하고, assignee 배정 UX와 dedicated-only inline append를 닫는다. Phase 5의 WBS/마일스톤 API와 현재 DnD tree contract를 그대로 재사용해, Phase 7 인력 계획 전에 입력 surface를 안정화한다.

Review delta note:
- RIS-130에서 지적된 roadmap/UI-spec drift는 이 planning pass에서 이미 정리됐다. execute-phase는 문서 ownership/status를 다시 논쟁하지 말고 아래 task breakdown을 구현 기준으로 사용한다.

Output:
- dedicated WBS route/page shell
- shared WBS workspace content for hub tab + page
- assignee column + dialog select wiring
- dedicated-only root/child quick-add rows
- focused regression coverage for inline append placement
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.1-wbs-작업공간-확장/06.1-CONTEXT.md
@.planning/phases/06.1-wbs-작업공간-확장/06.1-UI-SPEC.md
@.planning/phases/06.1-wbs-작업공간-확장/06.1-REVIEWS.md
@.planning/phases/05-wbs-milestone/SUMMARY.md
@.planning/phases/06-간트-차트/SUMMARY.md
@client/src/pages/diagram/DiagramsPage.tsx
@client/src/components/wbs/WbsTab.tsx
@client/src/components/wbs/WbsItemFormDialog.tsx
@client/src/components/wbs/SortableWbsRow.tsx
@client/src/components/wbs/wbs-tree-utils.ts
@client/src/components/milestone/MilestonePanel.tsx
@client/src/api/wbsApi.ts
@client/src/api/teamApi.ts
@client/src/constants/query-keys.ts
@client/src/constants/routes.ts
@client/src/hooks/useProjectQueryInvalidation.ts
@client/src/types/wbs.ts
@client/src/types/team.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: route + layout split</name>
  <files>
    client/src/constants/routes.ts,
    client/src/App.tsx,
    client/src/pages/diagram/DiagramsPage.tsx,
    client/src/pages/project/ProjectWbsPage.tsx,
    client/src/components/wbs/WbsTab.tsx,
    client/src/components/wbs/WbsWorkspaceContent.tsx
  </files>
  <read_first>
    - client/src/constants/routes.ts
    - client/src/App.tsx
    - client/src/pages/diagram/DiagramsPage.tsx
    - client/src/components/wbs/WbsTab.tsx
    - client/src/components/layout/Header.tsx
    - .planning/phases/06.1-wbs-작업공간-확장/06.1-UI-SPEC.md
  </read_first>
  <action>
다음 exact state로 hub tab과 dedicated page를 분리한다.

1. `client/src/constants/routes.ts`
   - `PROJECT_WBS_PATTERN`
   - `PROJECT_WBS(teamId, projectId)`
   를 추가한다.
   - 기존 `DIAGRAMS`/`PROJECTS` naming pattern을 따른다.

2. `client/src/App.tsx`
   - `ProjectWbsPage`를 lazy import 한다.
   - protected route로 `ROUTES.PROJECT_WBS_PATTERN`을 등록한다.
   - 기존 diagrams/document routing 순서는 유지한다.

3. `client/src/pages/project/ProjectWbsPage.tsx`를 새로 만든다.
   - `Header` + `workspaceContext`를 사용한다.
   - `fetchTeam`, `fetchProject`, `queryKeys.teams.detail`, `queryKeys.projects.detail`로 top-band context를 가져온다.
   - 본문 wrapper는 `workspace-shell` / `workspace-container max-w-none`을 사용한다.
   - 상단 compact band에는 다음만 둔다:
     - project hub로 돌아가는 back button (`ROUTES.DIAGRAMS(teamId, projectId)`)
     - eyebrow/title/description
     - page-level `Add WBS item` action
   - hero-style `ProjectWorkspaceHero`는 쓰지 않는다.

4. `client/src/components/wbs/WbsWorkspaceContent.tsx`로 현재 `WbsTab`의 data/query/mutation/table/dialog/milestone rail 핵심을 추출한다.
   - tab/page가 같은 query/mutation source를 쓰게 한다.
   - shared content는 wrapper가 같은 create dialog state를 열 수 있도록 single trigger surface를 제공한다.
   - shared content는 `variant: 'tab' | 'page'`를 받아 compact vs dedicated behavior만 분기한다.

5. `client/src/components/wbs/WbsTab.tsx`
   - compact tab shell만 남긴다.
   - header 우측에 `Open workspace` outline CTA와 기존 `Add WBS item` CTA를 함께 배치한다.
   - actual table/mutation/dialog/milestone rendering은 `WbsWorkspaceContent`로 위임한다.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROJECT_WBS_PATTERN\\|PROJECT_WBS" client/src/constants/routes.ts`
    - `grep -n "ProjectWbsPage" client/src/App.tsx`
    - `test -f client/src/pages/project/ProjectWbsPage.tsx`
    - `grep -n "max-w-none" client/src/pages/project/ProjectWbsPage.tsx`
    - `grep -n "wbs.workspace.open\\|ROUTES.PROJECT_WBS" client/src/components/wbs/WbsTab.tsx`
    - `test -f client/src/components/wbs/WbsWorkspaceContent.tsx`
  </acceptance_criteria>
  <done>
    - compact hub tab stays in place
    - dedicated route can be opened directly and refreshed safely
    - page-level layout is full-width without bloating `DiagramsPage`
  </done>
</task>

<task type="auto">
  <name>Task 2: assignee UX close-out on shared table/dialog</name>
  <files>
    client/src/components/wbs/WbsWorkspaceContent.tsx,
    client/src/components/wbs/WbsItemFormDialog.tsx,
    client/src/components/wbs/SortableWbsRow.tsx,
    client/src/i18n/locales/ko/translation.json,
    client/src/i18n/locales/en/translation.json
  </files>
  <read_first>
    - client/src/components/wbs/WbsWorkspaceContent.tsx
    - client/src/components/wbs/WbsItemFormDialog.tsx
    - client/src/components/wbs/SortableWbsRow.tsx
    - client/src/api/teamApi.ts
    - client/src/constants/query-keys.ts
    - client/src/types/team.ts
    - client/src/types/wbs.ts
    - .planning/phases/06.1-wbs-작업공간-확장/06.1-UI-SPEC.md
  </read_first>
  <action>
다음 exact state로 assignee 입력/표시를 닫는다.

1. `WbsWorkspaceContent.tsx`
   - `fetchMembers(teamId)` query를 추가한다.
   - query key는 반드시 `queryKeys.teams.members(teamId)`를 사용한다.
   - 이 query 결과를 dialog와 table rendering에 공용으로 전달한다.

2. `WbsItemFormDialog.tsx`
   - `WbsItemFormValues`에 `assigneeUserId`를 추가한다.
   - select field를 parent 바로 다음에 배치한다.
   - first option은 `Unassigned`다.
   - option label은 `member.name`을 기본으로 보여주고 `loginId`는 보조 텍스트로만 사용한다.
   - member query loading 중에는 select를 disabled 처리하고 loading placeholder를 보여준다.
   - member query 실패 시에는 assignee field만 disabled + helper copy를 보여주고, 나머지 create/edit submit은 계속 가능해야 한다.

3. create/update payload wiring
   - dialog submit은 selected `assigneeUserId`를 그대로 `createWbsItem`/`updateWbsItem` payload에 넣는다.
   - inline name/progress edit path도 현재 item의 `assigneeUserId`를 유지한다.
   - edit initial state는 기존 item의 `assigneeUserId`를 반영한다.

4. `SortableWbsRow.tsx`
   - assignee column을 `Item` 뒤에 추가한다.
   - assigned state는 bordered pill, unassigned state는 muted text로 렌더링한다.
   - inline assignee editor는 추가하지 않는다.

5. table header widths
   - shared table는 `Item / Assignee / Period / Progress / Est. M/M / Milestone / Actions` 순서를 사용한다.
   - compact tab은 horizontal scroll을 허용하고, dedicated page도 동일 semantic column set을 유지한다.

6. 번역 키 추가/수정
   - `wbs.workspace.open`
   - `wbs.field.assignee`
   - `wbs.field.unassigned`
   - `wbs.form.assignee`
   - `wbs.form.assigneePlaceholder`
   - `wbs.form.unassigned`
   - member loading/failure helper copy가 필요하면 같은 `wbs.form` namespace에 짧게 둔다.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchMembers\\|queryKeys.teams.members" client/src/components/wbs/WbsWorkspaceContent.tsx`
    - `grep -n "assigneeUserId" client/src/components/wbs/WbsItemFormDialog.tsx`
    - `grep -n "wbs.field.assignee\\|wbs.field.unassigned" client/src/components/wbs/SortableWbsRow.tsx`
    - `grep -n '"assignee"' client/src/i18n/locales/ko/translation.json`
    - `grep -n '"assignee"' client/src/i18n/locales/en/translation.json`
  </acceptance_criteria>
  <done>
    - WBS-02의 assignee 입력/표시가 신규 API 없이 닫힌다
    - dialog와 table이 같은 assignee contract를 공유한다
    - compact tab과 dedicated page 모두 현재 배정 상태를 읽을 수 있다
  </done>
</task>

<task type="auto">
  <name>Task 3: dedicated workspace inline append</name>
  <files>
    client/src/components/wbs/WbsWorkspaceContent.tsx,
    client/src/components/wbs/WbsInlineCreateRow.tsx,
    client/src/components/wbs/wbs-tree-utils.ts,
    client/test/unit/wbs-inline-create.test.ts,
    client/src/i18n/locales/ko/translation.json,
    client/src/i18n/locales/en/translation.json
  </files>
  <read_first>
    - client/src/components/wbs/WbsWorkspaceContent.tsx
    - client/src/components/wbs/wbs-tree-utils.ts
    - client/src/components/wbs/WbsTab.tsx
    - client/test/unit/gantt-adapter.test.ts
    - client/test/unit/gantt-update-guards.test.ts
    - .planning/phases/06.1-wbs-작업공간-확장/06.1-UI-SPEC.md
  </read_first>
  <action>
다음 exact state로 dedicated-only quick-add를 구현한다.

1. `client/src/components/wbs/WbsInlineCreateRow.tsx`를 새로 만든다.
   - collapsed prompt state:
     - root: `wbs.quickAdd.root`
     - child: `wbs.quickAdd.child`
   - editing state:
     - first cell input
     - other cells는 `wbs.quickAdd.setLater` helper text
     - action cell에 `Add` / `Cancel`
   - keyboard contract:
     - `Enter` submit
     - `Escape` cancel
     - empty submit block
   - inline validation text를 surface하고 placeholder-only failure에 의존하지 않는다.

2. `client/src/components/wbs/wbs-tree-utils.ts`
   - quick-add row placement을 계산하는 pure helper를 추가한다.
   - helper는 최소한 다음 두 케이스를 결정 가능해야 한다:
     - table bottom root row
     - expanded parent group footer row
   - 기존 `buildChildrenByParent` / `flattenTreeItems` ordering을 재사용한다.

3. `WbsWorkspaceContent.tsx`
   - `variant === 'page'`일 때만 quick-add rows를 렌더링한다.
   - root footer row는 항상 렌더링한다.
   - child footer rows는 expanded parent group 뒤에만 렌더링한다.
   - quick-add submit payload는 기존 `createWbsItem` 경로를 그대로 사용한다:
     - `name`
     - `parentId`
     - `assigneeUserId: null`
     - `startDate: null`
     - `endDate: null`
     - `progressRate: 0`
     - `estimatedMm: null`
     - `milestoneId: null`
   - 성공 시:
     - query invalidation
     - success toast
     - typed value clear
     - 같은 hierarchy input focus 유지
     - 새 item brief highlight
   - 실패 시:
     - typed value 유지
     - row 자동 종료 금지

4. compact tab guard
   - `variant === 'tab'`에서는 quick-add row가 절대 렌더링되지 않는다.
   - compact tab은 dialog + reorder + inline name/progress만 유지한다.

5. 번역 키 추가
   - `wbs.quickAdd.root`
   - `wbs.quickAdd.child`
   - `wbs.quickAdd.add`
   - `wbs.quickAdd.cancel`
   - `wbs.quickAdd.setLater`
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f client/src/components/wbs/WbsInlineCreateRow.tsx`
    - `grep -n "quickAdd" client/src/components/wbs/WbsWorkspaceContent.tsx`
    - `grep -n "variant === 'page'\\|variant !== 'page'" client/src/components/wbs/WbsWorkspaceContent.tsx`
    - `grep -n "progressRate: 0\\|parentId" client/src/components/wbs/WbsWorkspaceContent.tsx`
    - `grep -n "buildChildrenByParent\\|flattenTreeItems" client/src/components/wbs/wbs-tree-utils.ts`
    - `test -f client/test/unit/wbs-inline-create.test.ts`
    - `cd client && npm run test:unit`
  </acceptance_criteria>
  <done>
    - WBS-07 dedicated inline append path가 열린다
    - quick-add는 table rhythm을 유지하고 반복 입력이 가능하다
    - placement logic은 pure helper regression test로 고정된다
  </done>
</task>

<task type="auto">
  <name>Task 4: verification and regression boundary</name>
  <files>
    client/test/unit/wbs-inline-create.test.ts,
    client/src/pages/project/ProjectWbsPage.tsx,
    client/src/components/wbs/WbsWorkspaceContent.tsx,
    client/src/components/wbs/WbsItemFormDialog.tsx
  </files>
  <read_first>
    - client/package.json
    - client/test/unit/gantt-adapter.test.ts
    - client/test/unit/gantt-update-guards.test.ts
    - client/src/components/wbs/WbsWorkspaceContent.tsx
  </read_first>
  <action>
검증 범위를 다음 exact state로 고정한다.

1. Automated verification
   - `cd client && npm run build`
   - `cd client && npm run test:unit`
   - unit test는 새 pure helper(quick-add placement) 회귀를 다룬다.
   - 이 repo에는 React component test harness가 표준화돼 있지 않으므로, browser component harness를 신규 도입하지 않는다.

2. Manual smoke checklist
   1. project hub의 `wbs` tab에 `Open workspace` CTA가 보인다.
   2. CTA로 `/teams/:teamId/projects/:projectId/wbs` dedicated route에 진입할 수 있다.
   3. dedicated page에서 `Add WBS item` dialog를 열고 assignee를 지정/해제할 수 있다.
   4. 저장 후 table assignee column에 이름 또는 `Unassigned`가 반영된다.
   5. dedicated page root quick-add로 item을 연속 append할 수 있다.
   6. expanded parent group footer quick-add로 child append가 가능하다.
   7. compact tab에는 quick-add row가 보이지 않는다.
   8. milestone rail, reorder, inline name/progress edit가 회귀하지 않는다.

3. Scope guards
   - assignee inline cell editor는 이번 phase에 넣지 않는다.
   - new backend endpoint / DB migration / team-member search virtualization은 추가하지 않는다.
   - WBS/Gantt split view는 구현하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/j.jaeyo/Project/ETC/smart-erd/client && npm run build && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `cd client && npm run build`
    - `cd client && npm run test:unit`
    - manual smoke list covers CTA, route, assignee save, root append, child append, compact-tab no-quick-add
  </acceptance_criteria>
  <done>
    - execute-phase 6.1 can start without reopening scope decisions
    - verification target is explicit and proportional to the repo’s existing test harness
  </done>
</task>

</tasks>

<verification>
1. `cd client && npm run build`
2. `cd client && npm run test:unit`
3. Manual smoke:
   - compact tab `Open workspace` CTA
   - dedicated route load
   - assignee create/edit/clear
   - root quick-add repeated append
   - child quick-add append under expanded parent
   - compact tab no quick-add regression
   - milestone rail / reorder / inline name-progress edit intact
</verification>

<success_criteria>
- WBS-06: compact tab remains and dedicated WBS route is reachable
- WBS-02: assignee is editable in dialog and visible in the table
- WBS-07: dedicated page supports root and expanded-parent-group inline append
- RIS-130 review findings no longer leave roadmap/UI-spec contradictions in the execution package
- files/ownership/dependency order are explicit enough that `$gsd:execute-phase 6.1` can start immediately
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-wbs-작업공간-확장/SUMMARY.md`
</output>
