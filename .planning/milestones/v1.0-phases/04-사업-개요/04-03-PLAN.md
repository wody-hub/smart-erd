# 04-03-PLAN: UI 구현 — DiagramsPage 탭 도입 + BusinessOverviewTab

## 1. 파일 목록

### 신규 생성

| 파일 경로 | 역할 |
|---|---|
| `client/src/components/workspace/DocumentHubTabContent.tsx` | DiagramsPage에서 문서 목록 영역 추출 |
| `client/src/components/project/BusinessOverviewTab.tsx` | 사업 개요 탭 (읽기/편집 모드) |

### 수정

| 파일 경로 | 변경 내용 |
|---|---|
| `client/src/pages/diagram/DiagramsPage.tsx` | 탭 셸 도입, 기존 문서 목록을 DocumentHubTabContent로 위임 |

---

## 2. 구현 순서

```
1단계: DocumentHubTabContent 추출 (DiagramsPage 리팩터링)
2단계: BusinessOverviewTab 구현
3단계: DiagramsPage 탭 셸 조립
```

---

## 3. 상세 명세

### 3-1. DocumentHubTabContent (추출)

**DiagramsPage에서 이동할 코드:**
- `useQuery(diagrams)`, `useQuery(dictionarySets)`
- `useDiagramDocumentHubActions` 훅
- `documentItems` useMemo
- surface-operational 밴드 (DocumentTypeBadge + 카운트)
- 로딩/에러/빈 상태 분기 + 문서 목록
- `CreateDocumentDialog`, `ConfirmDialog`

**Props:**
```typescript
/** 문서 허브 탭 콘텐츠 Props */
interface DocumentHubTabContentProps {
  /** 팀 ID */
  teamId: string;
  /** 프로젝트 ID */
  projectId: string;
  /** 편집 권한 여부 */
  canEdit: boolean;
  /** 생성 다이얼로그 열림 상태 (Radix controlled open 패턴) */
  createDialogOpen: boolean;
  /** 생성 다이얼로그 상태 변경 콜백 */
  onCreateDialogOpenChange: (open: boolean) => void;
}
```

- Radix UI의 controlled open 컨벤션을 따름 (`dialogOpen` + `onDialogOpenChange`)
- 기존 `openTrigger` 카운터 패턴 대신 선언적 상태 제어

### 3-2. BusinessOverviewTab

**Props:**
```typescript
/** 사업 개요 탭 Props */
interface BusinessOverviewTabProps {
  /** 팀 ID */
  teamId: string;
  /** 프로젝트 ID */
  projectId: string;
  /** 편집 권한 여부 */
  canEdit: boolean;
}
```

> **설계 변경 (리뷰 반영):** `documentCount` prop 제거 — BE `BusinessOverviewResponse`에 이미 `memberCount`, `documentCount`, `progressRate`가 포함되어 있으므로 자체 API 응답에서 직접 사용.

**코드 순서** (Page Component Code Ordering 준수):
```
1  useQueryClient
2  useTranslation → t, i18n
3  useState: isEditing
4  useState: form (BusinessOverviewFormState)
5  useQuery: fetchBusinessOverview → BusinessOverviewResponse (memberCount, documentCount 포함)
6  useMutation: updateBusinessOverview (PATCH, UpdateBusinessOverviewPayload)
7  핸들러: handleEditStart, handleCancel, handleSave
8  useEffect: isEditing → form 초기화
9  조건부 리턴: isLoading → Spinner
10 JSX
```

**JSX 구조:**
```
<div className="space-y-6">
  ┌─ 제목 + 편집 버튼 (canEdit && !isEditing)
  ├─ [빈 상태] WorkspaceEmptyState (isDataEmpty && !isEditing)
  │   └─ action: CTA 버튼 → handleEditStart (setIsEditing(true))
  ├─ [요약 카드 3장] grid grid-cols-1 sm:grid-cols-3 (memberCount, documentCount, progressRate)
  │   └─ data는 자체 useQuery 응답(BusinessOverviewResponse)에서 직접 사용
  ├─ [읽기 모드] <dl> grid cols-2 — 6개 필드
  ├─ [편집 모드] <form onSubmit={handleSave}> grid cols-2 — Label+Input 6개 필드
  │   └─ 저장: <button type="submit">, 취소: <button type="button" onClick={handleCancel}>
  └─ [편집 액션] 취소/저장 버튼
</div>
```

**읽기 모드:**
- `<dl>` 시맨틱 마크업, `grid grid-cols-1 md:grid-cols-2 gap-6`
- `projectScope`는 `col-span-full`, `whitespace-pre-wrap`
- 금액: `formatCurrency(data.contractAmount)` (함수가 이미 "원" 접미사 포함 — 호출부에서 추가 금지)
- 날짜: `formatProjectDate(data.projectStartDate, i18n.language)`
- null 필드: `t('businessOverview.field.empty')` ("미등록")

**편집 모드:**

| 필드 | 입력 컴포넌트 | 특이사항 |
|---|---|---|
| clientCompany | `<Input>` | 텍스트, max 200 |
| contractorCompany | `<Input>` | 텍스트, max 200 |
| contractAmount | `<Input type="text" inputMode="numeric">` | 숫자 입력 |
| projectStartDate | `<Input type="date">` | native date picker |
| projectEndDate | `<Input type="date">` | native date picker |
| projectScope | `<Textarea rows={4}>` | col-span-full, resize-none |

각 필드: `<Label htmlFor={id}>` + `<Input id={id}>` (a11y).

**요약 카드** — 파일 내 로컬 서브컴포넌트:
```typescript
/** 요약 지표 카드 — 사업 개요 탭 상단에 표시되는 숫자 카드 */
function SummaryCard({ label, value, unit }: { label: string; value: string | number; unit?: string }) {
  return (
    <div role="group" className="rounded-xl border border-border bg-card p-4 shadow-operational" aria-label={label}>
      <p className="text-sm text-muted-foreground">{label}</p>
      <p className="mt-1 text-2xl font-semibold text-foreground">
        {value}
        {unit && <span className="ml-1 text-sm font-normal text-muted-foreground">{unit}</span>}
      </p>
    </div>
  );
}
```

**isDataEmpty 파생값:**
```typescript
const isDataEmpty = !data || (
  data.clientCompany == null && data.contractorCompany == null &&
  data.contractAmount == null && data.projectStartDate == null &&
  data.projectEndDate == null && data.projectScope == null
);
```

**handleSave 흐름:**
1. `event.preventDefault()` (form onSubmit)
2. `contractAmountRaw` → `Number()` 변환, `isNaN` 또는 음수 시 `toast.error` + 리턴
3. `isDatePairValid(form.startDate, form.endDate)` → 실패 시 `toast.error(t('businessOverview.validation.datePair'))` + 리턴
4. `isDateOrderValid(form.startDate, form.endDate)` → 실패 시 `toast.error(t('businessOverview.validation.dateOrder'))` + 리턴
5. `saveMutation.mutate(payload)` — `UpdateBusinessOverviewPayload` 변환 (6개 필드 전체)
6. onSuccess: `invalidateQueries(businessOverview)` + `setIsEditing(false)` + `toast.success`
7. onError: `toast.error(getErrorMessage(err, t('businessOverview.save.failed')))` — **`isEditing`은 `true` 유지** (사용자가 재시도 가능)

**편집 폼 초기화:**
```typescript
useEffect(() => {
  if (!isEditing || !data) return;
  setForm({
    clientCompany: data.clientCompany ?? '',
    contractorCompany: data.contractorCompany ?? '',
    contractAmountRaw: data.contractAmount != null ? String(data.contractAmount) : '',
    startDate: data.projectStartDate ?? '',
    endDate: data.projectEndDate ?? '',
    projectScope: data.projectScope ?? '',
  });
}, [isEditing, data]);
```

### 3-3. DiagramsPage 탭 셸 재구성

**DiagramsPage에 남길 부분:**
- `useParams`, `useNavigate`, `useQueryClient`, `useTranslation`
- `useState<'documents' | 'overview'>`: activeTab (기본값: 'documents') — `useTranslation` 뒤, `useQuery` 앞에 배치
- `useState<boolean>`: createDialogOpen (기본값: false) — 생성 다이얼로그 상태 소유
- `useQuery(team)`, `useQuery(project)`, `useTeamRole`
- Header, Back 버튼, ProjectWorkspaceHero
- Tabs 셸

> **설계 변경 (리뷰 반영):** `useQuery(diagrams)`를 DiagramsPage에서 제거. `documentCount`는 `BusinessOverviewTab`이 자체 BE 응답에서 직접 사용. `diagrams` 쿼리는 `DocumentHubTabContent` 내부로 완전 이동.

**히어로 분기:**
- `activeTab === 'documents'`: primaryAction = 문서 생성 버튼, utilityActions 유지
- `activeTab === 'overview'`: primaryAction = undefined, utilityActions = undefined

**Tabs 구조:**
```tsx
<Tabs value={activeTab} onValueChange={setActiveTab} className="mt-6">
  <TabsList>
    <TabsTrigger value="documents">
      <FileText className="mr-2 h-4 w-4" />
      {t('businessOverview.documentsTab')}
    </TabsTrigger>
    <TabsTrigger value="overview">
      <ClipboardList className="mr-2 h-4 w-4" />
      {t('businessOverview.tab.title')}
    </TabsTrigger>
  </TabsList>

  <TabsContent value="documents">
    <DocumentHubTabContent ... />
  </TabsContent>
  <TabsContent value="overview">
    <BusinessOverviewTab teamId={teamId} projectId={projectId} canEdit={canEdit} />
  </TabsContent>
</Tabs>
```

**TabsList 위치:** ProjectWorkspaceHero 직하, surface-operational 밴드는 DocumentHubTabContent 내부로 이동.

---

## 4. 컴포넌트 트리

```
DiagramsPage
├── Header
└── <main>
    └── <div class="workspace-container max-w-5xl">
        ├── Button (Back)
        ├── ProjectWorkspaceHero [tone="documents"]
        └── Tabs [value=activeTab]
            ├── TabsList
            │   ├── TabsTrigger "documents" (문서)
            │   └── TabsTrigger "overview" (사업 개요)
            ├── TabsContent "documents"
            │   └── DocumentHubTabContent
            │       ├── surface-operational 밴드
            │       ├── 문서 목록 / 빈 상태 / Spinner
            │       ├── CreateDocumentDialog
            │       └── ConfirmDialog
            └── TabsContent "overview"
                └── BusinessOverviewTab
                    ├── 제목 + 편집 버튼
                    ├── WorkspaceEmptyState (빈 상태)
                    ├── SummaryCard × 3
                    ├── ReadView (<dl>) / EditForm (<form>)
                    └── 취소/저장 버튼
```

---

## 5. 상태 흐름

```
[초기 진입] activeTab='documents'
  → diagrams/team/project 쿼리 발화
  → BusinessOverview 쿼리 미발화

[탭 전환 → overview]
  → BusinessOverviewTab 마운트 → useQuery 발화
  → 미등록: WorkspaceEmptyState 렌더링
  → 등록됨: ReadView 렌더링

[편집 진입] handleEditStart → setIsEditing(true) → useEffect → form 초기화

[저장] handleSave → 날짜 검증 → mutate → onSuccess: invalidate + setIsEditing(false)

[취소] handleCancel → setIsEditing(false) (form은 다음 편집 시 재초기화)
```

---

## 6. 체크리스트

### DocumentHubTabContent 추출
- [ ] DiagramsPage에서 문서 관련 쿼리/훅/JSX 이동 (diagrams 쿼리 포함)
- [ ] `createDialogOpen` + `onCreateDialogOpenChange` controlled open 패턴 (카운터 패턴 사용 금지)
- [ ] CreateDocumentDialog, ConfirmDialog 내부 유지
- [ ] Props 인터페이스 필드별 JSDoc
- [ ] lint + tsc 통과

### BusinessOverviewTab
- [ ] SummaryCard 서브컴포넌트 (role="group", aria-label, shadow-operational, JSDoc)
- [ ] `<dl>` 시맨틱 마크업 (읽기 모드)
- [ ] `<form onSubmit>` + `<button type="submit">` / `<button type="button">` (편집 모드)
- [ ] `<label htmlFor>` 연결 (편집 모드 전 필드)
- [ ] 편집 버튼 `aria-label={t('businessOverview.editAriaLabel')}`
- [ ] 빈 상태 CTA → `handleEditStart` (`setIsEditing(true)`) 연결
- [ ] `documentCount` prop 제거 — BE 응답(`BusinessOverviewResponse`)에서 직접 사용
- [ ] handleSave: contractAmount NaN/음수 검증 → toast.error
- [ ] handleSave: `isDatePairValid` → toast.error
- [ ] handleSave: `isDateOrderValid` → toast.error
- [ ] onError 시 `isEditing` true 유지 (사용자 재시도 가능)
- [ ] `formatCurrency` 호출 시 "원" 중복 추가 금지
- [ ] `useMutation` onSuccess: invalidateQueries + setIsEditing(false) + toast
- [ ] `isDataEmpty` 파생값 정확성 (JSDoc 포함)
- [ ] canEdit=false 시 편집 버튼 미표시
- [ ] Props 인터페이스 필드별 JSDoc

### DiagramsPage 탭 셸
- [ ] activeTab 기본값 'documents' — `useTranslation` 뒤, `useQuery` 앞 배치
- [ ] createDialogOpen 상태 소유 — DocumentHubTabContent로 전달
- [ ] 히어로 primaryAction/utilityActions 탭별 분기
- [ ] diagrams 쿼리 DiagramsPage에서 제거 (DocumentHubTabContent로 완전 이동)
- [ ] 디자인 토큰: 하드코딩 색상 없음
- [ ] 요약 카드 반응형: `grid-cols-1 sm:grid-cols-3`
- [ ] 모바일(1열) / md 이상(2열) 반응형 레이아웃

### 통합 검증
- [ ] 탭 전환 시 첫 overview 진입에서만 API 호출
- [ ] 미등록 → 빈 상태 UI → CTA 클릭 → 편집 모드
- [ ] 편집 → 날짜 한쪽만 입력 → toast.error (저장 안 됨)
- [ ] 편집 → 날짜 역전 → toast.error (저장 안 됨)
- [ ] 편집 → 금액 비숫자 → toast.error (저장 안 됨)
- [ ] 저장 실패(서버 에러) → isEditing 유지 + toast.error
- [ ] 저장 성공 → 읽기 모드 + toast.success
- [ ] 히어로 버튼 → DocumentHubTabContent 다이얼로그 열림
